# --- nucleo (obbligatorie) ---
PyQt6>=6.6
requests>=2.31

# --- consigliate (migliorano i tool, l'app funziona anche senza) ---
psutil>=5.9
pywinctl>=0.3
keyboard>=0.13
send2trash>=1.8
pycaw>=20240210
comtypes>=1.2

# --- voce ---
# Registrazione e riproduzione. Il resto (ascolto e sintesi) passa da
# ElevenLabs via HTTP, quindi non serve nessun modello in locale.
sounddevice>=0.4
# Solo se si vuole trascrivere senza far uscire l'audio dal PC:
# stt_engine: "faster-whisper" nella configurazione.
# faster-whisper>=1.0
# Per vedere un artifact reso invece del suo sorgente. Pesa (Chromium),
# ma senza, QTextBrowser disegnerebbe una pagina moderna sbagliata
# invece di disegnarla approssimata: l'harness allora mostra il codice.
PyQt6-WebEngine>=6.6
# I colori del codice nell'harness. Cinquecento linguaggi invece dei
# quattro che avremmo scritto a mano - e sbagliato su tutti gli altri.
Pygments>=2.15
