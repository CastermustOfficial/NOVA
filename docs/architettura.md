# NOVA — Documento di architettura

> Stato: documento vivo. Raccoglie le premesse, le decisioni prese e il perche'.
>
> Il documento ha due livelli, e vanno tenuti distinti. Le **premesse**
> (sezione 1) sono invarianti: se una scelta le viola, si cambia la scelta.
> Le **decisioni** (sezione 7) sono revocabili: se cambiano i vincoli si
> riscrivono, annotando il motivo. Un documento che registra solo l'esito e
> non la ragione invecchia male, perche' nessuno sa piu' cosa si puo'
> rimettere in discussione.

## 0. Cosa è NOVA

Un assistente che sta sul PC dell'utente e **fa le cose**, non solo le spiega.
L'immagine di riferimento è un tecnico competente seduto accanto: guarda,
capisce, agisce, e continua a lavorare mentre tu lavori.

Tre proprietà che lo distinguono da una chat:

- **Agisce.** Apre programmi, compila moduli, scrive file, esegue comandi.
- **Non ruba il posto.** Lavora in una finestra sua, sull'albero di
  accessibilità e non su mouse e tastiera, quindi non interrompe l'operatore.
- **Vede lo stato reale.** Legge il PC — finestre aperte, hardware, servizi —
  invece di indovinare.

## 1. Premesse: cosa non barattiamo

Queste non sono linee guida, sono **invarianti**. Un principio si adatta al
contesto; una premessa no: se una scelta la viola, si cambia la scelta.

Ognuna e' scritta insieme a **cio' che staremmo barattando**, perche' una
premessa che nessuno ha mai avuto la tentazione di rompere non e' una
premessa: e' un'ovvieta'. Il valore sta nel riconoscere l'affare quando
arriva, e rifiutarlo lo stesso.

---

### N1 — NOVA non ha confini di capacita'

Se l'utente lo consente, NOVA puo' fare tutto cio' che l'utente stesso
potrebbe fare sul proprio computer. Non si costruiscono sandbox, non si
amputano capacita' per rendere qualcosa «piu' sicuro».

**Baratto rifiutato:** «con un ambiente isolato dormiremmo tranquilli». Si',
e NOVA diventerebbe un altro prodotto — uno dei tanti che consigliano invece
di fare. La sicurezza si ottiene con reversibilita' e osservabilita' (N2),
non togliendo poteri.

### N2 — Prima la reversibilita', poi il permesso

Ogni volta che si puo' sostituire una richiesta di conferma con un
annullamento, si sostituisce. Un'operazione reversibile non ha bisogno di
essere temuta.

**Baratto rifiutato:** «basta chiedere conferma, e' piu' semplice da
scrivere». Vero, ed e' anche il motivo per cui quasi tutti gli assistenti
restano timidi: senza rete di recupero, ogni azione va giustificata prima, e
un agente che deve avere ragione al primo colpo non prova mai niente.

### N3 — I dati dell'utente non lasciano la sua macchina

Memoria, credenziali e configurazione vivono in `%APPDATA%\NOVA`. Non nel
repository, non in telemetria, non in un servizio nostro. Se l'utente sceglie
un cervello o una voce remoti, quello che esce e' **solo** cio' che serve a
quella chiamata, e glielo si dice in chiaro.

**Baratto rifiutato:** «un po' di telemetria anonima aiuterebbe a capire i
guasti». Aiuterebbe noi. Il patto con chi installa un programma che vede
tutto il suo computer e' che quel programma non riferisca a nessuno.

### N4 — Il segreto non passa dal modello

Quando NOVA compila una password, il valore va dall'archivio cifrato al campo
di destinazione **senza entrare nel contesto del modello**. E' una proprieta'
dell'architettura, non una buona abitudine: la capacita' accetta il *nome*
della credenziale, non il suo valore.

**Baratto rifiutato:** «sarebbe molto piu' semplice passargliela e basta».
Si'. E ogni conversazione, ogni log, ogni riassunto in memoria diventerebbe un
posto dove quella password puo' riaffiorare.

### N5 — Conta la conseguenza, non il gesto

Un'azione non e' compiuta perche' e' stata invocata. Verificare sul modulo che
si e' appena compilato non vale: vale l'effetto. La verifica e' strutturale,
non un passo che si salta quando si va di fretta.

**Baratto rifiutato:** «verificare raddoppia i tempi». Talvolta e' vero. Ma un
sistema che dichiara successi che non ci sono e' peggio di uno lento: e' uno
di cui non ti puoi fidare, e a quel punto ricontrolli tutto a mano.

### N6 — NOVA non ruba il posto all'operatore

Si agisce sull'albero di accessibilita', non su mouse e tastiera sintetici.
Cosi' NOVA lavora su una finestra in secondo piano mentre l'utente scrive in
un'altra. Lo screenshot e l'input simulato restano il **ripiego** per cio' che
non espone struttura: non diventano mai il canale principale.

**Baratto rifiutato:** «guardare lo schermo e cliccare e' molto piu' facile da
implementare». E' vero, ed e' anche il modo piu' rapido per uccidere il
co-working: nel momento in cui NOVA muove il tuo cursore, non potete piu'
lavorare insieme.

### N7 — Si puo' sempre fermare

Ogni operazione lunga e' interrompibile. «Ferma» significa fermare **l'azione
in corso**, non chiudere il programma. L'interruzione non e' una funzione di
emergenza: e' cio' che rende accettabile l'autonomia alta.

**Baratto rifiutato:** «lo aggiungiamo dopo, tanto le operazioni sono brevi».
Le operazioni brevi diventano lunghe il giorno in cui arriva il pianificatore.
L'interruzione va prima, sempre.

### N8 — Nessuna morte silenziosa

Un componente che si arrende lo dice, e lo dice dove qualcuno guarda. Nessun
guasto puo' assomigliare alla quiete.

**Baratto rifiutato:** nessuno — questa l'abbiamo pagata. Il supervisor si
arrendeva per sempre dopo cinque riavvii, e l'unico ad ascoltare l'evento era
una finestra che il progetto rende deliberatamente chiudibile. Per giorni
«il modello non e' attivo» ha avuto la faccia di una cosa normale.

### N9 — Il confine e' una manopola dell'utente

NOVA non decide quanto puo' osare. Parte cauta e si apre quanto l'utente
vuole. I livelli di autonomia limitano **quanto processo decisionale si
delega**, mai quali capacita' esistono: lo stesso strumento e' disponibile a
ogni livello, cambia solo chi decide se usarlo.

**Baratto rifiutato:** «certe cose non dovrebbe proprio poterle fare». Quel
giudizio spetta a chi possiede il computer.

### N10 — Ogni richiesta dichiara il proprio costo

Quando NOVA chiede qualcosa, dice cosa costa. Una rinuncia non si presenta
mai come un vantaggio.

**Baratto rifiutato:** «se lo formuliamo meglio accettano piu' spesso». Si
chiama raggiro, e funziona una volta sola.

### N11 — Se la strada non cede se ne prende un'altra; se non c'e', si costruisce

Un comando che non risponde non e' un muro: e' un modo che non funziona. NOVA
non insiste, cambia. E quando nessuna strada esistente arriva all'obiettivo,
se ne fabbrica una — un indirizzo composto a mano, un file scritto e
riconsegnato, uno script, un'automazione nuova. La capacita' di costruire il
pezzo mancante e' parte del mestiere, non un caso limite.

Il pavimento resta N6: qualunque via si inventi deve funzionare in secondo
piano. Una strada nuova che si prende tastiera e mouse non e' una strada, e'
un'interruzione con un altro nome.

**Baratto rifiutato:** «insistere e' tenacia». Insistere e' l'unica forma di
pigrizia che sembra impegno, ed e' costosa in modo misurabile: in un lavoro
vero, quindici tentativi su un filtro che non ha mai ceduto, mentre cambiare
fonte — quello che alla fine ha risolto — ne e' costati due.

## 2. Principi operativi

**P1 — Conta la conseguenza, non il gesto.** Un'azione non è compiuta perché è
stato premuto un pulsante. Verificare sul modulo appena compilato non vale:
vale controllare l'effetto.

**P2 — «Non posso» va qualificato.** Quasi mai è vero in assoluto. Le forme
oneste sono «non posso da qui», «non posso adesso», «non posso senza il tuo
permesso» — e ognuna dice all'utente cosa fare dopo. Un «non posso» secco
dove esisteva una strada è un difetto, non prudenza.

**P3 — Il confine è una manopola dell'utente.** NOVA non decide da sé quanto
può osare. Di fabbrica parte cauta; l'utente la apre quanto vuole.

**P4 — Degradare con grazia.** Ogni funzione che dipende da un permesso, da
un componente o da hardware specifico deve poter mancare senza rompere il
resto. Si rileva la capacità, si offre solo ciò che esiste.

**P5 — Dichiarare il costo.** Quando NOVA chiede qualcosa, dice cosa costa in
chiaro. Nessuna richiesta va presentata come un vantaggio se è una rinuncia.

## 3. Il cervello

NOVA non è legata a un modello. Il router sceglie il gradino; il gradino è
configurazione, non codice.

### 2.1 Le tre vie

| Via | Chi la usa | Stato |
|---|---|---|
| **Chiave API a consumo** | chi vuole la qualità massima e sa cosa paga | **predefinita** |
| **Modello locale** | chi vuole gratuito, offline, privato | consigliata a chi ha l'hardware |
| **CLI di un abbonamento** | utenti avanzati | opt-in, avvisata |

### 2.2 Perché la chiave API è la predefinita

È l'unico canale **esplicitamente previsto** dai fornitori per un uso
programmatico. L'utente sa cosa paga e quanto.

### 2.3 Perché la CLI di abbonamento non è la predefinita

Gli abbonamenti consumer (Claude, ChatGPT e simili) sono pensati per l'uso
interattivo dell'abbonato. Usare la loro CLI come motore di un'applicazione
terza è, per la maggior parte dei termini di servizio, fuori perimetro.

Il rischio non lo corre il progetto: lo corre l'utente, che può vedersi
limitare o sospendere l'account. Un progetto che fa sospendere l'abbonamento
a chi lo installa perde la fiducia una volta sola.

Resta disponibile, per chi sa cosa sta facendo, con l'avviso in chiaro.

### 2.4 Il modello locale e il manifest

«Il migliore in commercio» cambia ogni mese. **Non può stare nel codice**,
o ogni modello nuovo diventa una release.

Sta in `models.json`: nome esatto, taglia, quantizzazione, VRAM e RAM
richieste, fonte del download, qualità dichiarata e data dell'ultima
revisione. Il codice sceglie *come* scaricare e avviare; il manifest dice
*cosa*. Aggiornare la classifica è modificare un file di dati — e NOVA può
rileggerlo da sola.

Il flusso: controllo dei requisiti, proposta del modello migliore che quella
macchina regge davvero, **dichiarazione onesta della qualità attesa**,
download, verifica dell'hash.

Sulla dichiarazione di qualità: dire «più lento e meno capace dei modelli a
pagamento, ma sufficiente per X e Y» costruisce fiducia. Promettere parità
la distrugge al primo confronto.
## 4. Distribuzione e fiducia

Il problema: un eseguibile non firmato che automatizza l'interfaccia, lancia
processi e conserva credenziali **verrà** segnalato da SmartScreen e da
Defender. È il muro più sottovalutato dei progetti di questo tipo: senza
risposta, metà degli utenti non arriva al primo avvio.

### 3.1 Cosa si fa

1. **Firmare il codice.** Un certificato EV dà fiducia SmartScreen da subito;
   uno OV la costruisce col tempo. È la voce di bilancio numero uno.
2. **Segnalare i falsi positivi** ai fornitori di antivirus. Gratis, previsto,
   funziona.
3. **Distribuire via winget**, che ha reputazione propria.
4. **Build riproducibili e hash pubblicati.** È il vantaggio dell'open source:
   chiunque può verificare che il binario corrisponda al sorgente.

### 3.2 Cosa non si fa

**L'installer non chiede mai un'esclusione antivirus.** Un'app che come primo
atto chiede di essere tolta dai controlli è indistinguibile da un malware, e
l'utente fa bene a chiudere tutto. All'installazione la fiducia è zero: si
guadagna, non si chiede in prestito.

Questo **non** vieta di chiedere un permesso più avanti — vedi 4.3.

## 5. Permessi

### 4.1 Il valore sta nella descrizione

Predefinito: **conferma sempre**.

Ma una richiesta generica è peggio di nessuna richiesta: dopo tre giorni di
«consentire operazione su file?» si clicca sì a occhi chiusi, e il presidio
è evaporato mentre sembra ancora in piedi.

- No: «Consentire operazione su file?»
- Sì: «Sto per eliminare 4 file in Download: [elenco]. Procedo?»

Il bottone non protegge nessuno. Protegge la frase.

### 4.2 Il permesso più stretto che sblocca la funzione

Quando serve un permesso di sistema, si chiede **il più mirato possibile**.

Su Windows le cose che bloccano davvero l'automazione legittima non sono le
scansioni antivirus, ma l'**accesso alle cartelle controllate** e le regole
**ASR**. Entrambe hanno liste di app consentite per singola applicazione.

«Lasciami scrivere in Documenti» è più onesto, più mirato e molto più facile
da concedere di «escludimi dai controlli».

### 4.3 Il momento conta più della richiesta

La stessa domanda è legittima o sospetta a seconda di quando arriva.

- **All'installazione**: no. Fiducia zero, nessun contesto.
- **Dopo, per una funzione precisa e nominata**: accettabile, se opt-in, se
  dichiara il costo (P5) e se il rifiuto lascia il resto funzionante (P4).

Forma corretta della richiesta:

> «Per fare X devo essere esclusa dai controlli di Windows. Vuol dire che
> Windows non mi controllerà più. Se preferisci di no, X non lo faccio e
> tutto il resto continua a funzionare uguale.»

Mai chiamarla «protezione aggiuntiva»: un'esclusione toglie controlli, non
li aggiunge. Presentare una rinuncia come un vantaggio è la frase che un
domani viene citata contro il progetto.

### 4.4 Nota sul fare da antivirus

Le esclusioni non sono lo strumento con cui si costruisce un prodotto di
sicurezza. La strada vera è registrarsi nel **Centro sicurezza di Windows** e
usare **AMSI**. È una strada da fornitore riconosciuto, oggi fuori scala.
Annotata perché la porta è quella, non l'esclusione.
## 6. Hardware e BIOS

### 5.1 Il livello sempre disponibile: diagnosi, guida, verifica

Funziona su **ogni** macchina, senza permessi speciali e a rischio zero.
NOVA legge scheda madre, BIOS, RAM, CPU; riconosce le configurazioni
sbagliate; **guida passo per passo** con istruzioni cucite su quella scheda;
e dopo il riavvio **verifica** che sia andata.

Esempio reale, dalla macchina di sviluppo: RAM DDR5 a 4800 MT/s su Ryzen
7600X, cioè il ripiego JEDEC — EXPO disattivato, banda lasciata sul tavolo.
Rilevabile in una query, correggibile in trenta secondi, invisibile a chi
non sa dove guardare.

Questo è il 95% del valore, ed è quello che fa l'esperto seduto accanto: non
ti sfila la tastiera, ti dice cosa c'è che non va e controlla dopo.

### 5.2 Il livello opt-in: scrittura diretta

**Perché non può essere la strada principale**

- Gli offset della variabile di setup cambiano **per ogni scheda e per ogni
  versione di BIOS**. Non esiste mappa universale.
- La firma dell'applicazione UEFI richiede una chiave che in open source
  **non si può distribuire**: ogni utente dovrebbe generarla e registrarla
  nel proprio BIOS.
- Un errore non produce un file corrotto: produce **un PC che non si avvia**,
  recuperabile solo aprendo il case. Su una macchina altrui è inaccettabile
  come comportamento predefinito.
- Il premio è piccolo: un utente normale tocca il BIOS forse quattro volte
  nella vita del PC (EXPO, virtualizzazione, TPM, ordine di boot).

**Come funzionerebbe**

Applicazione UEFI sulla partizione EFI, lanciata con `BootNext`. Gira in fase
DXE/BDS — lo stesso contesto del setup del BIOS — quindi senza le protezioni
SMM che bloccano le scritture a sistema operativo avviato.

Architettura obbligata: **cervello sopra, mani sotto.** Nella fase firmware
non esistono Python, modello o rete: ci sono poche centinaia di KB di C.
NOVA ragiona dove c'è un OS, lascia un'istruzione precisa, riavvia, l'agente
minuscolo esegue, si torna su, NOVA verifica. È come lavora un tecnico: non
pensa dentro il BIOS, decide fuori ed entra a eseguire.

**Reti di sicurezza**

- `BootNext` è **monouso**: il firmware la consuma e la cancella. Se l'agente
  fallisce, il riavvio successivo torna a Windows da solo.
- Dump completo della variabile **prima** di scriverla, quindi ripristinabile.
- AM5 ha il fallback automatico sul training della memoria.
- Ultimo gradino, dichiarato in anticipo: reset del CMOS a mano.

**Rilevamento di capacità** (P4)

All'avvio NOVA guarda scheda madre, versione BIOS, stato di Secure Boot, e
cerca se esiste una mappa IFR per quella combinazione. In base a cosa trova
offre la via automatica o quella guidata. Le mappe crescono nel tempo,
contribuite dagli utenti: chi ha una scheda non coperta la genera e la manda
al repo.

Il predefinito resta **sempre** la guida. L'automatico è opt-in, con l'avviso
in chiaro e la procedura di recupero detta prima, non dopo.

**Primo passo, a rischio zero**: scaricare il file del BIOS pubblicato dal
produttore, estrarne l'IFR con UEFITool, ottenere la mappa. È solo parsing di
un file — non tocca la macchina — e dice se la strada esiste prima di
scrivere una riga di codice UEFI.

### 5.3 Livelli scartati

| Livello | Perché no |
|---|---|
| Driver kernel (ring 0) | HVCI attivo blocca i driver non firmati. Ed è «sotto» come privilegi, non come tempo. |
| Hypervisor sotto l'OS | Su Windows moderno il posto è **già occupato** da Hyper-V/VBS. Liberarlo significa spegnere proprio ciò che protegge le credenziali. |
| Flash del firmware modificato | Nessun potere in più sulle impostazioni, e l'errore non si recupera col reset del CMOS: serve un programmatore hardware. |

## 7. Decisioni registrate

| # | Decisione | Motivo |
|---|---|---|
| D1 | Chiave API come via predefinita | unico canale esplicitamente previsto per uso programmatico |
| D2 | CLI di abbonamento come opt-in avvisato | il rischio di sospensione ricade sull'utente |
| D3 | Catalogo modelli in `models.json`, non nel codice | «il migliore» cambia ogni mese |
| D4 | Firma del codice invece di esclusioni antivirus | l'esclusione all'installazione è il pattern del malware |
| D5 | Conferma sempre come predefinito | il confine è una manopola dell'utente (P3) |
| D6 | BIOS: guida sempre, scrittura opt-in con rilevamento capacità | il rischio è un PC che non si avvia; il premio è piccolo |
| D7 | Permesso più stretto invece di esclusione totale | più onesto, più facile da concedere |
| D8 | Catalogo modelli in `models.json`, scelto sulla VRAM reale | `AdapterRAM` mente sopra i 4 GB: si legge nvidia-smi o il registro |
| D9 | Reversibilita' prima del permesso (N2) | un'operazione annullabile non va temuta, e permette a NOVA di osare |
| D10 | Interruzione prima del pianificatore | senza stop, l'autonomia alta e' inaccettabile a ragione |
| D11 | Le premesse N1-N10 sono invarianti, non linee guida | senza un livello che non si negozia, ogni scelta scomoda erode il progetto |
| D12 | Il prompt di sistema non si traduce: gli si dice in che lingua rispondere. Si traducono i nomi e i titoli | il modello capisce un'istruzione in italiano e risponde in coreano; l'interfaccia no, li' non c'e' nessun modello in mezzo. Tradurre il prompt vorrebbe dire N copie di un testo che cambia a ogni funzione nuova |
| D15 | Il dizionario dell'interfaccia ha l'italiano come chiave, non un identificatore | con chiavi astratte una voce dimenticata lascia la chiave o il vuoto; con l'italiano lascia una frase di senso compiuto |
| D16 | Un solo posto sa cosa serve a ogni funzione e come procurarlo (`nova/componenti.py`) | quando l'unico programma capace di scaricare era l'installer, ogni ripensamento costava una reinstallazione |
| D17 | NOVA puo' riparare il proprio codice, ma solo passando da un banco: copia, prova, confronta, applica | il modo piu' rapido di rompere un assistente in maniera irreparabile e' lasciare che si ripari da solo mentre e' rotto |
| D18 | Il criterio non e' «tutte le prove verdi», e' «nessuna prova che era verde diventa rossa» | con una prova gia' rossa la regola severa non lascerebbe passare niente, mai, e l'unica via d'uscita sarebbe spegnere il controllo |
| D19 | Una prova sparita conta come regressione | cancellare il file che ti accusa fa tornare tutto verde |
| D20 | Si registra la **procedura**, mai la risposta | «hai tre mail nuove» e' vero per dieci minuti; «si apre mail.google.com e si leggono le non lette» vale per mesi |
| D21 | La procedura la scrive il modello, non la deduciamo dalle chiamate | con un cervello agentico le chiamate non passano da noi: osservarle avrebbe funzionato per meta' dei cervelli e per l'altra meta' mai |
| D22 | Riconoscimento lessicale pesato, non embedding | l'embedder predefinito e' a hash e non sa che «guarda se ho posta» e «controlla le mail» sono la stessa cosa: darebbe somiglianze a caso, e una procedura sbagliata proposta con sicurezza e' peggio di nessuna |
| D23 | La procedura si suggerisce, non si esegue da sola | un riconoscimento lessicale che facesse partire azioni prima o poi manderebbe la mail sbagliata alla persona sbagliata |
| D24 | Un'automazione si collauda **prima** di salvarla: se la prova non gira, non nasce | un'automazione rotta ma salvata verrebbe riproposta come funzionante, e la volta dopo il guasto sembrerebbe venire da un'altra parte |
| D25 | Nessun filtro sul contenuto del codice generato | sarebbe un recinto alle capacita' di NOVA (N1). Le difese sono altre: creazione rischiosa quindi visibile, codice leggibile e cancellabile, esecuzione in un processo fermabile |
| D26 | Il modello scrive solo il corpo; il guscio lo mette NOVA | il contratto - parametri in, testo fuori, errori riportati - lo deve garantire il programma, non la buona volonta' del codice generato |
| D27 | Le automazioni compaiono come strumenti normali (`auto_*`) | se il modello dovesse ricordarsi di chiamare un «esegui_automazione» generico, tornerebbe a essere una decisione, e le decisioni sono la parte che costa |
| D13 | Cercare i modelli, riconoscerli e pilotare le CLI sta in Python, non nell'installer | una seconda copia in PowerShell diverge in silenzio: si aggiunge una cartella a una sola delle due e nessuno se ne accorge |
| D14 | Chi ha gia' un modello, un abbonamento o un server acceso viene servito prima di chi deve scaricare | far scaricare 13 GB a chi li ha gia' e' il modo piu' rapido per far chiudere l'installer |
| D28 | Un guasto si dice in italiano; il traceback va nel file, non sullo schermo | «PermissionError: [Errno 13]» non e' un messaggio, e' il nome di una classe: chi lo legge capisce solo che il programma e' rotto. Il traceback non sparisce, cambia posto: va dove serve a chi ripara |
| D29 | Il valore di una credenziale non entra in nessun messaggio, nemmeno in quello d'errore che arriva da fuori | il fornitore rimanda indietro la chiave dentro il proprio errore («Incorrect API key provided: sk-...»), e da li' finiva in chat e nel registro. Si copre prima di guardare cosa c'e' scritto |
| D30 | Quota finita e' `LimiteUso`, non un errore del compito | sono due notizie diverse: «non ci riesco» e «riprova piu' tardi». Detta cosi', il router mette in pausa il gradino e ripiega; detta come errore generico il ripiego non parte mai, ed e' quello che succedeva per i gradini a consumo |
| D31 | Il verificatore dell'harness confronta con prima, non con il verde assoluto | e' D18 applicata al codice dell'utente invece che al proprio: con una prova gia' rossa la regola severa rifiuterebbe ogni modifica, e il verificatore non si accenderebbe mai |
| D32 | Lo stato mentre lavora si dice a parole ed e' vivo, ma non finge di sapere quanto manca | un testo fermo e un programma fermo si somigliano troppo; una barra di avanzamento che si inventa una percentuale mente. Da quanto sta andando lo sappiamo, e quello si dice |
| D33 | Di interfacce ce n'e' una sola: l'orb | due interfacce non sono una scelta in piu' per l'utente, sono due posti dove le cose si scollano - e alla domanda «cosa vede uno appena installato» danno due risposte, cioe' nessuna |
| D34 | Il porting in Rust e' un progetto di distribuzione e robustezza, non di prestazioni; e si scrive contro il trait di `nova-platform` anche dove l'unico backend e' Windows | il Python costa ventotto millisecondi per turno: come progetto di velocita' non sta in piedi. Meta' della lista compatibilita' pero' sparisce se sul PC dell'utente non c'e' piu' Python. E scrivere contro il trait e' la differenza fra avere macOS e Linux a una implementazione di distanza e doverli rifare da capo |
| D35 | Il disinstallatore riconosce le proprie cose dall'inizio del nome, e non tocca il fascicolo | «contiene NOVA» cancellerebbe l'attivita' pianificata di qualcun altro chiamata «Innovation backup»; e cancellare il CV di qualcuno perche' ha disinstallato un programma sarebbe imperdonabile |
| D36 | Chi cerca sul disco non sa cosa sia un disco: le radici da percorrere si passano da fuori | e' la riga che rende il codice provabile su qualsiasi sistema con una cartella finta invece che solo sulla macchina di chi l'ha scritto. La domanda «quali dischi sono fissi» e' di piattaforma e sta in `nova-platform`, accanto a «quali schermi ci sono» |
| D37 | Un modello e' utilizzabile se arriva fin dove la sua tabella dei tensori dice, non se comincia con GGUF | l'intestazione sta all'inizio del file: uno scaricamento fermo al sessanta per cento ce l'ha tutta. Il controllo dei quattro byte prometteva di riconoscerlo e non poteva. Si guarda dove comincia l'ultimo tensore e non quanto e' lungo: la tabella dei tipi di ggml cambia fra le versioni, e un falso allarme su un modello sano e' peggio di un file a meta' non riconosciuto |
| D38 | Quanta memoria video c'e' lo dice DXGI, non `nvidia-smi`; e il libero non supera mai il dedicato | `nvidia-smi` e' il programma di NVIDIA: su una Radeon o su una Arc non esiste, il comando fallisce e la stima torna zero - cioe' «tutto in CPU» - senza dirlo. E il budget di DXGI comprende la RAM che la scheda puo' farsi prestare: su una integrata sono quindici gigabyte su quattrocento megabyte suoi, e crederci vorrebbe dire caricare il modello in RAM chiamandola GPU |
| D39 | Il calcolo degli strati e' dovuto sempre: senza memoria video leggibile si va sul processore, non a `-ngl` tirato a caso. E il numero viaggia con quanto crederci | NOVA deve stare su tutti i PC, e un numero mancante non e' un'informazione neutra: tre funzioni piu' in la' diventa un parametro deciso dal caso, proprio sulla macchina che non conosciamo. Lento di sicuro si dice e si rimedia; finto veloce no. La distinzione fra misurata e dedotta serve a non pagare quella prudenza due volte: su una deduzione si tiene margine doppio, ma la si usa |
| D40 | Cio' che si promette a chi non ha una scheda video e' un MoE, non «un modello qualsiasi, piu' lento» | misurato: sul processore si paga per i parametri che si accendono, e fra due modelli della stessa taglia ci sono 7,6 contro 1,8 token al secondo. Sopra i sette si legge mentre arriva e NOVA si usa; sotto i due una risposta arriva in tre quarti di minuto. E' la differenza fra un programma utilizzabile e uno che si installa e non si apre piu' |
| D41 | Senza scheda video leggibile l'installatore non offre il modello locale: salta il passo e suggerisce di riprovare piu' avanti con un modello leggero da leggere | «servono N GB di VRAM: andra' piano» e' troppo gentile per un denso da 27B: non e' piu' piano, e' un programma che si installa dopo tredici gigabyte e non si apre piu'. Un avvertimento piu' grosso non basta - la scelta va tolta |
| D42 | Il costo di un modello si misura in **byte letti per token**, non in gigabyte di file ne' in parametri attivi | generare un token a una richiesta per volta non e' calcolo, e' leggere i pesi dalla memoria: la velocita' e' banda diviso byte letti, e i byte letti sono i parametri che si accendono per i bit che ciascuno occupa. Misurato: 15,7 GB/token danno 1,8 tok/s e ~3,7 GB/token ne danno 7,6, con la stessa banda implicita. E' la formula che spiega perche' un MoE quantizzato e un denso a un bit finiscono nella stessa categoria |
| D43 | Che un formato sia supportato lo dice il binario che si ha, non il README di chi lo pubblica | la scheda di un modello mandava a clonare la fork del suo autore «perche' a monte i kernel non ci sono»; `llama-quantize --help` sulla build gia' installata elencava il formato alla riga 40. Una documentazione non e' una misura, meno che mai quando ha una preferenza su dove mandarti |
| D44 | Un percorso e' una struttura, non una stringa: «dentro» si chiede ai componenti, e un nome si riconosce a parole intere | tre difetti nello stesso file, tutti da questo: `runtime-vecchio` passava per `runtime` e si prendeva la precedenza assoluta; `cuda` cercato dentro tutto il percorso classificava come NVIDIA il motore di chi si chiama Cudale; la versione presa da tutto il percorso leggeva quella di una cartella qualunque piu' in alto. E' lo stesso errore del `bin/` nel `.gitignore` |
| D45 | Sul processore il modello sta in RAM, tutto: la velocita' non basta, ci vuole anche lo spazio | i byte letti per token dicono se e' abbastanza veloce, non se ci sta. Sulla GPU il file vive in VRAM; sul processore vive in RAM insieme al sistema e al browser, e un modello che entra sulla carta manda la macchina a paginare su disco - lentissimo con la ventola accesa. Sono due rifiuti diversi e meritano due frasi diverse: chi ha poca RAM puo' comprarne, chi ha un modello troppo denso no |
| D46 | Di cosa e' fatto NOVA sta in un dato, non in quattro elenchi scritti a mano, e una prova lega quel dato ai bersagli veri del workspace | gli elenchi erano quattro - CI, installatore, disinstallazione, compilazione - e si erano gia' disallineati: `nova-schede` era stato aggiunto a uno solo, senza che nulla lo segnalasse, perche' su chi sviluppa il binario c'e' comunque. Correggerli e' facile; impedire che diventino cinque no, e quella e' la prova |
| D47 | Una configurazione scritta male non deve poter mandare fuori casa ogni compito | il routing scarta gia' la categoria senza parole e senza soglia, che scatterebbe sempre. Ne mancava una scritta in un modo che sembra innocuo: `parole: ["*"]` diventava il pattern «\b» e basta, che trova un confine in qualunque testo. Il difetto non e' che l'utente sbaglia: e' che sbagliando manda i suoi dati fuori dal PC senza che nulla lo dica |
| D48 | Quando si accorcia la conversazione si scende fino a un fondo, non ci si ferma sul filo | fermandosi sul filo il turno dopo supera di nuovo e si taglia di nuovo: dal trentesimo turno in poi si tagliava a OGNI turno, la cache del prefisso non si riformava mai e ogni risposta pagava il prompt da capo per il resto della sessione. Misurato: 1.748 ms a turno contro 231. Non e' un flag a curarlo - `--cache-reuse` non cambia niente perche' la divergenza e' subito dopo il messaggio di sistema, e prima non c'e' niente da riusare |
| D49 | La conversazione si accorcia contando i **token**, non i messaggi | il limite del modello e' in token e la finestra si contava in messaggi: due unita' che non si parlavano. Dodici scambi con dentro il contenuto di un file fanno 102.953 token contro i 16.384 del contesto, e il taglio a messaggi non scattava nemmeno - erano venticinque messaggi. All'utente arrivava un JSON in inglese |
| D50 | Un messaggio piu' grande di tutto lo spazio si accorcia dichiarandolo, non si butta e non si tiene intero | buttarlo perde proprio la cosa di cui l'utente ha chiesto conto; tenerlo intero sfonda il contesto. Si tiene l'inizio, che dice cos'era, e la fine, che spesso porta la conclusione, e in mezzo si scrive quanto si e' tolto: un taglio dichiarato il modello lo capisce, uno silenzioso gli fa credere che il file finisca li' |
| D51 | Due implementazioni che concordano non sono due implementazioni verificate: la prova chiede «e' rimasto qualcosa di segreto?», non «dicono la stessa cosa?» | `Authorization: Bearer <token>` e' il modo piu' comune in cui una chiave finisce in un messaggio d'errore, e non era coperto ne' dal Python ne' dal Rust. Erano d'accordo, quindi qualunque confronto fra i due sarebbe passato. L'ha trovato una prova che guarda il risultato invece dell'accordo |
| D52 | I dodici gigabyte dei modelli non vanno in una cartella che sincronizza qualcun altro, e se ci vanno lo si dice prima | non e' il percorso a rompersi: e' che NOVA si installa dove l'utente ha scompattato il file, e se e' Documenti sincronizzato partono verso il cloud, il vault fa copie in conflitto, e i file vengono «liberati» restando in elenco come segnaposti vuoti - l'ultima mesi dopo, a NOVA che funzionava |
| D53 | Una prova che passa va guardata come una che fallisce: si chiede perche' passa | i percorsi oltre i 260 caratteri funzionano qui perche' `LongPathsEnabled` e' acceso su questa macchina e spento di fabbrica. Un verde che non si sa spiegare e' «da me funziona» con un bollino sopra, cioe' la versione che si difende da sola |
| D54 | Due modelli diversi che sbagliano identico non sono due modelli sbagliati: e' il prompt | a «ricordati che il mio gatto si chiama Ugo» sia Gemma sia Qwen chiamano `kb_search` invece di `kb_note`. Un banco che confrontasse i due l'avrebbe scritto come «sono d'accordo, tutto bene»: e' la prova che chiede se la scelta e' **giusta**, non se e' concorde, a vederlo |
| D55 | Un disinstallatore non cancella niente che non abbia creato lui: fuori dalla propria cartella elenca invece di rimuovere | «i tuoi dati» erano tre posti, non uno: `%APPDATA%\NOVA`, il fascicolo in Documenti e il vault dove l'ha messo l'utente. `-ConIDati` cancellava il primo e taceva sugli altri due, quindi chi disinstallava per ricominciare pulito si ritrovava la memoria intatta. Ma il vault puo' essere una cartella di Obsidian con dentro anche le note di chi la usa, e il modello sono sedici gigabyte che servono anche a LM Studio: si dice dove sono, con quanto pesano, e decide chi possiede il file |
| D56 | «Sta dentro questa cartella?» si chiede ai **nomi**, non a dove portano | `resolve()` segue i punti di reinnesto: misurato per sbaglio, un PowerShell dentro un pacchetto MSIX vede meta' di `%APPDATA%\NOVA` reindirizzata sotto `Packages\<pacchetto>\LocalCache`, e quattro file della stessa cartella risultavano **fuori** da essa. La domanda vera e' «se cancello questa cartella sparisce anche questo file?», e chi cancella una cartella cancella i nomi che ci stanno sotto. Con il separatore in fondo, se no `NOVA-vecchio` sta dentro `NOVA` |
| D57 | Una versione minima dichiarata si prova, non si spera: la grammatica con `feature_version`, la libreria standard cercando per nome | «Python 3.10 o superiore» era scritto in tre posti e provato in nessuno: la CI girava su una sola versione, e le tre non provate erano quelle su cui l'utente e' da solo. `ast.parse(..., feature_version=(3,10))` fa rifiutare la sintassi arrivata dopo, quindi quattro grammatiche si provano con un interprete solo; per la libreria non basta, perche' `import tomllib` si compila su 3.10 e poi non parte, e allora si cercano per nome le poche cose che uno usa senza accorgersi perche' sul suo PC ci sono gia' |
| D58 | Non si firma: si spiega, e la spiegazione va dove capita il fatto | una firma per un editore nuovo non toglie l'avviso di SmartScreen finche' non ha reputazione, quindi si pagherebbe per non risolvere il problema. La differenza fra tacere e spiegare non e' un paragrafo nel README: e' che «l'archivio non conteneva tutti i binari» era la diagnosi sbagliata nel caso piu' probabile — l'archivio li conteneva, li ha tolti l'antivirus dopo — e le due cure sono opposte |
| D59 | Il contrassegno «scaricato da Internet» si toglie **dopo** aver confrontato le impronte, mai prima | senza toglierlo SmartScreen chiede conferma a ogni avvio, non una volta. Toglierlo prima del controllo sarebbe zittire l'avviso di Windows su un file di cui non si sa ancora niente, che e' il comportamento di un installer malevolo: la stessa riga letta nei due ordini dice due cose opposte, e a separarle c'e' solo l'ordine. Una prova legge il sorgente e lo pretende |
| D60 | La memoria ha due strade, e quella che conta legge la domanda dell'utente non solo la risposta | «Ricordati che...» a volte non fa chiamare kb_note: il modello risponde a parole. Ma l'estrattore in sottofondo rilegge lo scambio e scrive il fatto da se', quindi il fatto arriva in memoria comunque. Il banco misurava solo lo strumento scelto in un turno; la domanda vera e' «e' finito nel vault?», e la risposta misurata end-to-end e' si' |
| D61 | Sul tool calling di un modello locale piccolo, alzare la voce nel prompt e' controproducente | rafforzare «usa kb_note, non limitarti a promettere» ha peggiorato la scelta da 3/4 a 1/6: il testo insistente sposta il modello verso il registro delle parole invece delle chiamate. Non ha un capo a cui obbedire, ha una distribuzione da seguire |
| D62 | Alla **seconda** occorrenza una cosa condivisa si mette in comune, non alla quarta | `giorni_del_mese` era privata dentro `nova-registro` e serviva di nuovo alla pianificazione. Le tre duplicazioni precedenti — l'elenco dei binari, le cartelle sincronizzate, i posti dei dati — si sono scoperte tutte **dopo** che si erano disallineate. Vederla prima e' l'unica differenza che conta: e' nato `nova-calendario` |
| D63 | Quando una prova fallisce, la prima cosa da verificare e' che abbia ragione lei | l'andata e ritorno di `piu_giorni` falliva su «366 giorni indietro dal primo gennaio 2027». Il codice diceva 31 dicembre 2025 ed era giusto: il 2026 non e' bisestile, ha 365 giorni. Avevo scritto 366 per analogia con «un anno», che vale solo negli anni bisestili. Cercare il difetto nel riporto sui mesi avrebbe rotto un calendario che funzionava |
| D64 | Sbattere contro un muro e girare a vuoto sono due guasti diversi: il primo si cura salendo di gradino, il secondo facendolo notare | il gradino sopra rifarebbe lo stesso giro, quindi far salire chi ripete la stessa chiamata riuscendo ogni volta costa e non risolve. E la ripetizione produce un promemoria, **mai** un divieto: la decisione resta al modello, e una ripetizione legittima non viene bloccata da niente |
| D65 | Una manopola a zero **spegne** la sua soglia, non la accende | `passi_prima_di_salire` a zero con un `>=` scritto senza pensarci diventa «passi >= 0», sempre vero: la manopola che serve a disattivare la funzione la farebbe scattare a ogni primo giro. C'e' una prova apposta |
| D66 | Nel porting si confrontano le decisioni, non le serializzazioni | far produrre al Rust la stessa stringa a byte di `json.dumps(sort_keys=True)` — spaziature, `ensure_ascii`, `default=str` — sarebbe lavoro vero per far combaciare un testo che non esce mai dal processo e serve solo a dire «uguale alla precedente». La serializzazione resta fuori, come i fusi in `nova-calendario` e il disco in `nova-registro` |
| D67 | Su un ingresso che viene da fuori, `unwrap_or_default()` non e' prudenza: e' una risposta inventata | una domanda illeggibile diventava una famiglia vuota, e la famiglia vuota produceva un verdetto perfettamente formato — «legge almeno 0.0 GB per token, non te lo faccio scaricare». L'installatore avrebbe rifiutato ogni modello dando all'utente una ragione inventata, e nessuno se ne sarebbe accorto perche' rifiutare e' la risposta giusta in molti casi veri |
| D68 | Un binario chiamato da PowerShell tollera il BOM: incontrare chi chiama e' compito suo | `Set-Content -Encoding UTF8` su Windows PowerShell 5.1 mette tre byte davanti al primo `{`, e serde si ferma con «expected value at line 1 column 1» — vero e inutile. Non e' sciatteria del chiamante: quel binario esiste **per** essere chiamato da li' |
| D69 | Il banco misura la traduzione, non il giunto: la cosa va chiamata almeno una volta da dove verra' chiamata davvero | 216 verdetti confrontati col Python erano verdi **con dentro** due difetti, perche' il confronto manda JSON di `json.dumps`: mai un BOM, sempre valido. Le prove parlavano al binario in una lingua che l'installatore non usa. Un minuto a mano ha trovato piu' di 216 casi automatici |
| D70 | Una funzione provata e mai invocata e' come non averla, e le prove non lo dicono | `solo_segnaposto` rilevava il **peggiore** dei guai della cartella sincronizzata — il modello «liberato» che resta in elenco con zero byte — ed era chiamata solo dalla sua prova. Una funzione pura, corretta e scollegata ha tutte le prove verdi che si possono desiderare: dicono «se la chiami risponde bene», non «la chiama qualcuno». Adesso una prova legge `install.ps1` e pretende la chiamata |
| D71 | Lo stesso servizio si scrive sempre allo stesso modo, e il nome si tiene in tabella invece di calcolarlo | «OneDrive» dalla variabile d'ambiente e «Onedrive» dal nome della cartella, per via di un `.title()`: due grafie nella stessa installazione. Quel messaggio deve convincere qualcuno a spostare una cartella, e una maiuscola sbagliata lo fa sembrare generato invece che scritto, proprio dove deve essere creduto |
| D72 | Una lezione imparata in un posto non si sposta da sola: quando si chiude un difetto, si cerca a mano in tutti i moduli che fanno la stessa domanda | `Authorization: Bearer <token>` era stato trovato e chiuso nei messaggi d'errore (D51) e passava indisturbato nel guardiano del vault, che e' il posto peggiore: cio' che entra li' finisce nel prompt a ogni turno, per sempre. Non c'e' un modo automatico di accorgersene — le prove di ciascun modulo passano, perche' ciascuno prova se stesso |
| D73 | Se due moduli chiedono «e' un segreto?», la domanda si fa in un posto solo | `guasti` conosceva il Bearer e le chiavi `sk-`, il vault conosceva AWS, Slack, le credenziali negli indirizzi e i numeri di carta: un errore con dentro `AKIA...` finiva in chiaro nel giornale. Aggiungere le voci mancanti a tutti e due avrebbe fatto due elenchi da allineare a mano, come gia' successo con gli eseguibili e le cartelle sincronizzate. `nova/forme_riservate.py` importa `re` e nient'altro, perche' lo carica anche l'installatore |
| D74 | Un confronto vale quanto le domande che fa, e zero confronti si stampano come zero divergenze | `test_guasti_rust.py` era verde mentre le due implementazioni divergevano su sei forme: il corpus conteneva solo le quattro che conoscevano entrambe. E una sonda scritta di fretta ha detto «0 divergenze su 14» con l'uscita vuota, perche' `zip` su una lista vuota non itera. Si contano le risposte prima di crederci |
| D75 | Il valore fatto di parole comuni e' comunque un segreto, se la chiave dice che lo e' | il controllo sulla densita' distingue «la password e' cambiata» da «la password e' Tramonto2026», e cosi' lasciava passare `seed phrase: abandon ability able...`, che e' sei parole di vocabolario. Ed e' l'unica cosa dell'elenco che non si puo' cambiare dopo: non esiste un «reimposta» per una seed phrase |
| D76 | Il mascheramento sta sulla porta, non nei chiamanti | il giornale delle azioni ha quindici punti che ci scrivono, e uno di loro registra il testo che NOVA **digita nei campi**: NOVA sa compilare un modulo di accesso, quindi prima o poi li' c'e' una password. Si maschera dentro `annota`, come il vault si chiude su `upsert`: un chiamante che si dimentica non e' un'ipotesi, e' una certezza |
| D77 | Se l'etichetta annuncia un segreto, il valore non si scrive — anche quando il valore da solo non sembra niente | «scritto in #password» in un campo e «Tramonto2026!» in un altro: separati non sono nulla, insieme sono una credenziale, e un filtro che guarda un campo per volta non puo' collegarli. Resta la riga, che dice cosa e' successo e dove, e sparisce il contenuto. Ma «scritto in #utente» con «giovanni.rossi» resta: un registro che copre tutto e' un registro che non si legge piu' |
| D78 | `spiega` maschera sul varco, non nei rami | il ramo finale restituiva `str(e)` grezzo, e il messaggio di un'eccezione porta il valore che l'ha causata: «Incorrect API key provided: sk-...» e' il caso vero di D29, dove si era chiuso il corpo HTTP e non questo. I rami sono quindici e crescono; il varco e' uno |
| D79 | Nel giornale d'avvio la domanda dell'utente non si maschera: si omette | il filtro prende le forme note e le coppie etichettate, non una parola qualunque che per l'utente e' un segreto — «Tramonto2026» da sola non somiglia a niente. E a quel log servono i flag, non il contenuto: `--ask <49 caratteri>` risponde alla stessa domanda diagnostica. Il modo sicuro di non scrivere una cosa e' non scriverla |
| D80 | Portare non e' migliorare: se il porting produce un risultato «migliore», e' un difetto | «Œuvre» dava `oeuvre` in Rust e `uvre` in Python, perche' `Œ` in Unicode non ha nessuna decomposizione. Il nome del file non e' una preferenza estetica: e' un contratto gia' firmato con i file che stanno nel vault, e cambiarlo li rinomina tutti — da quel momento lo stesso nodo esiste sotto due nomi. Si migliora dopo, in tutte e due le meta' insieme, pensando ai file gia' scritti |
| D81 | Un formato condiviso si confronta **a byte**, e nei due versi | la confidenza `1.0` che Rust stampa `1` e Python `1.0`: un carattere su seicento. Confrontando «i campi rileggendoli» sarebbe passato, e si sarebbe scoperto con un `diff` su un vault toccato da tutti e due, trovando ogni file modificato senza motivo. Il banco pretende anche che ciascuno rilegga il file dell'altro come il proprio |
| D82 | La confidenza sale quando un fatto torna **identico**, non quando torna riformulato | alzarla a ogni testo diverso premiava la variazione lessicale, cioe' esattamente il caso in cui NOVA non ha imparato niente di nuovo. E chi l'ha detto conta piu' di quante volte e' stato detto: l'osservazione automatica non declassa cio' che ha affermato l'utente |
| D83 | Un pezzo che passa il porting al primo colpo dice dove **non** stanno i difetti | `fusione` non ha nessun giunto — non incontra PowerShell, non legge un file, non parla con un processo — e le sue quattro regole erano gia' difetti trovati e capiti mesi fa. Tutti gli inciampi delle serate scorse stavano nel punto in cui il codice incontra il mondo, o in codice mai messo alla prova |
| D84 | ~~All'avvio l'orb sta sul principale~~ **RITIRATA lo stesso giorno.** La premessa era che il secondo monitor fosse spento: e' invece uno schermo che l'utente vede e su cui tiene l'orb apposta, e la regola gliela spostava a ogni accensione. Resta la lezione che l'ha smontata: **ci sono domande a cui nessuna misura risponde**, perche' il fatto non e' nel computer. Se uno schermo si vede lo sa una persona sola, e non e' quella che scrive il codice — trattare una deduzione come un dato misurato e' peggio che non misurare, perche' arriva con la stessa faccia |
| D85 | Un binario si cerca **accanto a se stesso** prima che dove lo mette cargo | `cli_nova()` guardava solo in `core/target/release`, che esiste sulla macchina di chi sviluppa e su nessun'altra: chi installa da una release ha i binari in `bin\`, e il pannello gli diceva per sempre «il client del demone non e' compilato». Stesso difetto che ha fatto nascere `binari.json`, e `demone.rs` faceva gia' la cosa giusta per `novad` |
| D86 | Le coordinate delle finestre si confrontano nello stesso sistema: Tauri lavora in pixel **fisici**, Windows Forms in **logici** | su un monitor al 125% sono numeri diversi per lo stesso punto (2330 fisici = 1864 logici). Misurando da PowerShell mentre il codice ragionava in fisici ho confrontato mele con pere per tre giri, e ogni volta la conclusione sembrava sensata |
| D87 | L'avvio automatico e' un'attivita' pianificata «all'accesso», non la chiave Run | misurato: PC acceso alle 10:16:10, NOVA comparsa alle 10:17:15. Sessantacinque secondi, e non per lentezza sua — fra «guscio in avvio» e «demone acceso» passano 0,65 secondi — ma perche' Windows ritarda apposta i programmi in Run. Per chi guarda, un minuto di niente non e' un ritardo: e' «non e' partita», ed e' il difetto peggiore per un programma che deve stare li' ad aspettarti |
| D88 | Cercare in un posto e non trovare non e' «non c'e'»: e' «non c'e' li'» | il registro del guscio esisteva da sempre in `runtime/guscio.log` e conteneva la risposta con l'ora. L'avevo cercato in `%APPDATA%\NOVA\logs`, non trovato, e concluso che non ci fosse niente — poi mezza giornata di deduzioni, una regola sbagliata e una finestra spostata a chi la teneva dove voleva. Quando qualcosa non torna la prima domanda non e' «cosa sara' successo», e' «chi lo ha scritto da qualche parte?» |
| D89 | Uno slug occupato si numera solo se il tipo e' **incompatibile**, mai se e' semplicemente diverso | `persona-anna` occupato da una persona e un «fatto» nuovo su Anna: se si numerasse, ogni annotazione automatica creerebbe `persona-anna-2`, `-3`, e la memoria si sbriciolerebbe in copie che non si parlano. Senza errori e senza log — il file viene scritto lo stesso. Percio' il banco chiede anche «un fatto su Anna finisce dentro Anna?», non solo «le due meta' sono d'accordo?» (D51) |
| D90 | Si scrive su un file temporaneo e poi si sposta: mai `open(..., "w")` diretto su un file che conta | `open(p, "w", newline="\n")` scritto male solleva `ValueError` **dopo** aver troncato: il file viene aperto in scrittura prima che l'argomento sia validato. Ho perso cosi' sedicimila caratteri di `test_nodi_rust.py`, recuperati da git solo perche' erano committati. `os.replace` o riesce del tutto o non fa niente. Vale soprattutto per i file dell'utente: il divieto di cancellare non protegge, se la scrittura e' permessa |
| D91 | Nel banco, il campo che sceglie la domanda non puo' chiamarsi come un campo della domanda | l'etichetta serde e' `tipo`; la domanda su dove vive un nodo aveva bisogno del tipo del **nodo**, e il nome ovvio era ancora `tipo`. Due campi con lo stesso nome: serde non protesta, sceglie, e il confronto avviene su una domanda diversa da quella che si credeva di aver fatto. Si chiama `tipo_nodo` |
| D92 | Il budget di DXGI e' un tetto, non una misura: dove c'e' NVML si prende il minore dei due | DXGI risponde «quanto il sistema sarebbe disposto a darti», contando di poter sfrattare chi non sta disegnando adesso. Con un gioco aperto: DXGI 15.341 MiB, la scheda 12.699. Undici strati di modello in piu' di quanti ce ne stiano — che non falliscono, finiscono in memoria condivisa, cioe' nel rallentamento da dieci volte. Lo scarto non scala con la scheda, scala con quanto usano gli altri: **una riserva fissa non puo' coprirlo per costruzione**. NVML e' una DLL del driver, non un programma da lanciare: non riapre la porta a `nvidia-smi`, perche' DXGI resta la risposta per tutti e NVML puo' solo abbassare |
| D93 | Con due schede NVIDIA non si corregge niente | il numero di NVML e' di una delle due e non si sa quale. Accoppiarle vorrebbe dire attraversare `nvmlPciInfo_t`, che ha due buffer di caratteri a dimensione fissa: sbagliarne uno di un byte vuol dire farsi scrivere nello stack dal driver. Restare ottimisti come ieri e' peggio di niente ma meglio di correggere la scheda sbagliata |
| D94 | A parita' di punteggio decide la **freschezza**, poi lo slug, in tutti e tre gli ordinamenti del retrieval | erano `sorted(..., reverse=True)` stabili, e l'ordine di arrivo risaliva a `set(tokenizza(query))` e a `postings[t]`: due insiemi di stringhe, e Python randomizza l'hash delle stringhe a ogni processo. Misurato sul vault vero, 444 domande in due processi: 11 ordini diversi, 3 in cui cambiava **quale nodo veniva ricordato** — fra queste «progetto». Lo slug e' arbitrario ma stabile e leggibile. Se a parita' debba vincere il nodo piu' recente e' una scelta su come NOVA ricorda, non una correzione: sta a Gio |
| D95 | Un difetto che vive dentro un processo si prova con piu' processi | l'ordine dei pari merito e' fisso per tutta la vita del processo e cambia al successivo: una prova normale sarebbe passata col difetto dentro. La prova apre quattro processi con quattro `PYTHONHASHSEED` e confronta le classifiche — e l'ho vista fallire col difetto rimesso, prima di crederle |
| D96 | L'insieme dei caratteri «di parola» resta quello che e', difetti compresi | contiene otto lettere accentate e non le altre, cosi' `Nunez` con gli accenti diventa `n` e `ez`. L'indice non e' una funzione pura: e' un contratto con i nodi che stanno nel vault adesso. Allargarlo cambierebbe la tokenizzazione di ogni nodo gia' scritto e la ricerca smetterebbe di trovare cio' che trovava ieri — far sparire ricordi credendo di migliorare. Se un giorno si allarga, da tutte e due le parti insieme e con un reindicizzamento |
| D97 | Un binario ricostruito che si comporta come prima non e' un mistero: non e' stato ricostruito | `Copy-Item` conserva la data di modifica dell'originale, cargo decide cosa ricompilare dalle date. Sorgente giusto, binario ancora rotto, e la prova misurava un programma che non esisteva piu'. Se non avessi avuto in mano un risultato che mi aspettavo diverso, avrei cercato il guasto nel codice. Guardare la riga di cargo prima del proprio codice |
| D98 | Un banco che riordina in uscita puo' nascondere il disordine di cio' che prova | il banco di `nova-memoria` ordinava per (punteggio, slug) all'uscita, con tanto di commento sul perche'. Il confronto tornava pulito e la lotteria dell'ordine restava dentro la libreria, che e' il codice che va in produzione. Il riordino nel banco serve — un confronto non deve dipendere dall'ordine di cio' che prova — ma non sostituisce lo stesso criterio dentro. Sistemare il termometro non e' curare la febbre |
| D99 | Prima di scrivere un pezzo nuovo, la domanda e' «cosa c'e' gia' qui», non «cosa manca la'» | ho scritto `nova-ricerca` — BM25, RRF, banco, sedici confronti verdi — guardando cosa restasse da portare **nel Python**. In Rust c'era gia' `nova-memoria`, che e' la stessa cosa. In un porting la cui unica ragione e' avere una risposta in un posto, la duplicazione e' il danno peggiore possibile. Se ne e' accorta solo la suite, perche' aveva una prova che confrontava tutte e due col Python |
| D100 | Lo spareggio del recupero sta dentro l'RRF, non sull'ordine finale | l'RRF trasforma la posizione in punteggio: due nodi a pari merito ne escono con punteggi gia' diversi, e un criterio applicato piu' a valle non trova piu' nessun pareggio da sciogliere. Sarebbe stata una regola scritta, documentata, e mai eseguita. Sta in una funzione sola, `in_ordine`, chiamata in tutti e tre i punti dove il recupero mette qualcosa in fila — prima erano tre `sort` scritti a mano in tre posti (D72) |
| D101 | La freschezza dirime al giorno, non all'ora, e va detto | `aggiornato` e' `AAAA-MM-GG`: nello stesso giorno il pareggio resta e decide lo slug. Con quarantacinque nodi seminati tutti il 21 agosto, fra loro la freschezza non dirime niente. Mettere l'ora vorrebbe dire cambiare il frontmatter di ogni file del vault, che e' un contratto con i file che ci stanno adesso — la stessa ragione di D96 |
| D102 | Un file che conta si scrive **di fianco e poi si rinomina**, mai aprendolo in scrittura | aprire in scrittura tronca, e fra il tronca e lo scrivi c'e' una finestra in cui il file e' vuoto: se il processo muore li' dentro, il contenuto di prima e' perso. `Vault.upsert` scriveva cosi' le note dell'utente, e ci passa `MemoryWriter` da un thread di sfondo dopo quasi ogni scambio — una nota vuota che alla ricerca dopo c'e' ma non dice piu' niente. `ricette.salva` lo faceva gia' giusto, col commento giusto, e da solo (D72): ora sta in `nova/scrittura.py` e chi scrive un file che conta chiama quello. Protegge da tutto cio' che interrompe il processo; il `fsync` prima della rinomina copre anche la corrente che va via. Non protegge da due processi sullo stesso file: quello e' un problema di serrature |
| D103 | Il filesystem sta dietro un tratto, cosi' la macchina a stati si prova con un disco finto | la parte difficile del vault non e' leggere un file: e' decidere chi ricaricare, chi dimenticare, e chi vince quando due file con lo stesso nome in cartelle diverse si contendono uno slug. Con un disco in memoria sono undici casi in un millisecondo e ripetibili; con un disco vero se ne provano tre e si spera. Il banco gira lo stesso scenario due volte — il Python su una cartella vera, il Rust sulla finta — cosi' il disco finto e' un modello di cui si e' verificata la fedelta', non una comodita' |
| D104 | Un banco prova solo le strade che il suo corpus percorre | il titolo di ripiego non era mai stato confrontato perche' ogni nodo del corpus aveva gia' un `title:` nel frontmatter: il Rust non applicava `capitalize()` da settimane e il banco era verde. Terza volta con la stessa forma — il banco dei guasti verde mentre le due meta' divergevano su sei forme, il banco della memoria che nascondeva il disordine riordinando in uscita. Le strade che il corpus non percorre non sono «coperte per simmetria»: sono scoperte |
| D105 | Una prova che confronta un'implementazione con se stessa difende il difetto invece di scoprirlo | `controlla("un nodo senza titolo prende il nome dal file", ... == "il mio nodo")` — quella stringa non l'aveva detta il Python, era cio' che il Rust faceva, e stava in una sezione intitolata «le cose che devono essere vere comunque». Ha certificato come regola del formato il difetto di una delle due meta'. Il valore atteso si chiede all'altra meta', o a una regola scritta da qualche parte: mai all'uscita di cio' che si sta provando |
| D106 | Il guardiano dei segreti sta **dentro** la porta, dietro un tratto che chi scrive deve passare | e' l'unica porta da cui si entra in memoria — ci passano l'apprendimento automatico, le note a mano e il seeding — e se il controllo stesse nel giudizio di chi chiama basterebbe un chiamante distratto. Il guardiano vero resta da portare ed e' la cosa che meno di tutte va fatta a meta': uno che si dimentica una forma e' peggio di nessuno, perche' da' l'idea di proteggere |
| D107 | Nel banco, la data si fissa **lontana da oggi** | `to_markdown` legge l'orologio e la data finisce nel frontmatter. Con la data di oggi il confronto passa anche se il blocco dell'orologio non funziona, e non lo si scopre fino al giorno dopo — o mai. Variante peggiore di D104: la strada il corpus la percorreva, ma atteso e reale coincidevano per un motivo che non c'entrava col codice |
| D108 | Togliere i doppioni conservando l'ordine non e' ordinare | il Python usa `dict.fromkeys` sugli archi dopo una fusione. Ordinare alfabeticamente sembra equivalente e riscrive il frontmatter di **ogni nodo collegato** con una modifica che nessuno ha chiesto, e che l'utente si vede comparire in Obsidian come se qualcuno gli avesse toccato le note |
| D109 | Mascherare e rifiutare usano le stesse forme e soglie diverse | coprire di troppo in un registro costa una parola illeggibile; rifiutare di troppo costa un ricordo che NOVA non avra' mai, e l'utente non capisce perche'. Stessa tabella (`chiavi::PREFISSI`, una sola — D73), due minimi: otto per mascherare, la soglia vera del fornitore per rifiutare (sedici OpenAI, venti GitHub, dieci Slack). Prima era un numero solo, e andava bene solo perche' nessuno lo usava per rifiutare |
| D110 | Un tratto senza un'implementazione vera attaccata e' una promessa, non una porta | `Guardiano` obbliga `salva` a chiedere, ma se nessuno gli passa il guardiano vero la porta e' aperta e sembra chiusa. C'e' una prova che verifica proprio l'attacco: rifiuta una password e lascia passare un fatto normale |
| D111 | Un percorso con `..` dentro non si scrive mai | un percorso arriva da uno slug, lo slug da un titolo, e il titolo puo' arrivare da un testo che NOVA ha letto da qualche parte. Non c'e' nessun caso legittimo in cui un nodo scriva fuori dalla cartella dell'utente. Per la stessa ragione l'elenco non segue i collegamenti simbolici: seguirli vuol dire leggere come nodo un file che sta fuori dal vault, e poi riscriverlo li' |
| D112 | La descrizione di uno strumento e' codice, non prosa: si estrae, non si ricopia | sono 26.573 caratteri di schema — circa 7.600 token, il 42% del contesto in certe configurazioni — che il modello rilegge a ogni richiesta e su cui sceglie quale strumento usare. Una parola diversa e' un comportamento diverso, e nessun tipo se ne accorge: e' gia' successo di peggiorare `kb_note` da 3 su 4 a 1 su 6 «rafforzandone» il testo. Le sessanta dichiarazioni sono state estratte dal registro vivo, e il banco le confronta carattere per carattere |
| D113 | L'ordine delle chiavi nello schema conta: `items` prima di `description` | stesso JSON per un parser, due prompt diversi per un modello. L'ordine e' quello in cui la dichiarazione Python e' scritta, e va riprodotto — per questo lo schema si compone a mano invece di passare da una libreria che riordina |
| D114 | Un estrattore che lavora sul codice sorgente deve verificare se stesso, e non col proprio risultato | ricavare un modello di anteprima da una lambda con espressioni regolari e' comodo e inaffidabile: quattro f-string su piu' righe venivano prese a meta', e il risultato era plausibile e sbagliato. La verifica rende il modello e lo confronta con l'anteprima vera — ma costruiva gli argomenti di prova dai campi che il **modello** nominava, cioe' da cio' che stava provando, e un modello a cui manca un campo non poteva essere smentito. D104 dentro lo strumento scritto per evitare D104 |
| D115 | «Sta dentro questa cartella?» si chiede in un posto solo: `nova/percorsi.py` | la domanda ricorre nel disinstallatore, nella guardia di scrittura e nel vault. La guardia la sbagliava in **tre** modi insieme, tutti e tre gia' scritti in D56: percorso risolto contro protetti non risolti (due spazi diversi, e sotto un punto di reinnesto la protezione sparisce); barra rovescia a mano (fuori da Windows non scatta mai); e nessun separatore sulle cartelle autorizzate, cosi' `C:\dati` autorizzava `C:\dati-altrui` — dimostrato, non dedotto |
| D116 | Un ripiego indulgente maschera un valore sbagliato, e piu' e' ragionevole meglio lo maschera | avevo scritto `ask_all` invece di `always_ask`: il ripiego «prudente» mandava chi chiedeva «conferma sempre» su «conferma solo se rischioso», senza errori. Adesso `capisci()` torna `None` per un nome sconosciuto, `dal_nome()` ripiega, e una prova pretende che tutti e tre i nomi veri si capiscano. Nei banchi i nomi si prendono dalla configurazione: riscriverli a mano era proprio l'errore |
| D117 | Un divieto che non compila non si butta in silenzio | i comandi vietati sono espressioni regolari configurabili, e il Python fa `except re.error: continue`: il divieto svanisce e chi l'aveva scritto crede di essere protetto. In Rust il motore e' un altro e la sintassi non coincide del tutto, quindi capita di piu'. `Guardie` tiene ed espone l'elenco dei motivi che non ha capito |
| D118 | Le due domande sui percorsi non sono la stessa, e in una guardia valgono **tutte e due** | «se cancello questa cartella sparisce anche questo file?» si risponde sui nomi (D56); «questa scrittura tocca un posto protetto?» no — misurato con `mklink /J`: passare da una giunzione aggirava la protezione. Vale l'unione: dentro se lo dice il nome **o** la destinazione. Sui protetti protegge di piu'; sulle cartelle autorizzate rifiuta di meno, ed e' giusto lo stesso perche' sotto MSIX `resolve()` porta i file fuori dalla loro stessa cartella. In Rust la destinazione e' un parametro obbligatorio: uno che si puo' dimenticare si dimentica |
| D119 | Un banco sui corpi confronta il **disco**, non solo quello che dicono | due implementazioni possono raccontare la stessa cosa e lasciare due cartelle diverse. Trentuno operazioni su due cartelle identiche fino ai byte, poi il confronto file per file. E' D51 applicato a uno strumento che agisce: la domanda non e' «dicono lo stesso?» ma «hanno fatto lo stesso?» |
| D120 | Le guardie sono il **primo argomento** di ogni corpo che scrive | non un controllo interno, non un parametro opzionale: un permesso che si puo' dimenticare si dimentica. Nel modulo dei file non e' possibile scrivere uno strumento che scrive senza aver chiesto, ed e' la stessa forma di D106 (il guardiano dentro la porta del vault) e D110 (il tratto attaccato per davvero) |
| D121 | `read_file` ripiega su cp1252, e nel corpus del banco ci va un file cp1252 | sui PC italiani i `.txt` salvati dal Blocco note dieci anni fa non sono UTF-8, e senza il ripiego NOVA direbbe «non e' un file di testo» di un file di testo. La tabella porta i cinque byte che in cp1252 **non esistono**: un file che li contiene non e' cp1252, e leggerlo comunque vorrebbe dire inventare caratteri. Senza il file nel corpus quel ramo sarebbe verde e mai percorso (D104) |
| D122 | Un fuso orario e' un **tratto**, non un numero | avevo calcolato lo spostamento oggi e applicato a gennaio: due istanti su quattro sbagliati di un'ora, trovati dal banco. Un fuso cambia due volte l'anno, quindi un file modificato d'inverno ed elencato d'estate esce con l'ora sbagliata — e non se ne accorge nessuno finche' non guarda lo stesso file in due stagioni. `Fuso::secondi_in(istante)` obbliga chi chiama a rispondere alla domanda «quando?»; un numero si passa senza pensarci. Stessa forma della destinazione obbligatoria in D118 |
| D123 | Un controllo che sembra proteggere e non protegge e' peggio di nessun controllo | `run_python` non passa dalla guardia dei comandi, e non va «sistemato»: i motivi vietati sono espressioni regolari per il testo di una shell, e su del codice Python darebbero falsi allarmi mancando comunque quelli veri, perche' quel codice lancia qualunque cosa per vie che nessuna regola sul testo intercetta. La difesa li' e' il rischio dichiarato — Pericoloso, quindi si ferma a chiedere. Il limite si scrive in cima al modulo: chi vede un controllo smette di guardare |
| D124 | Una combinazione di soli modificatori si rifiuta | `press_keys` tradotto male non fallisce: preme **altri tasti**, nella finestra che ha il fuoco, cioe' quella dove l'utente sta lavorando. `ctrl`, `ctrl+alt`, la stringa vuota e un `+` da solo devono dare errore invece di mandare qualcosa a caso |
| D125 | «Cosa dice questa pagina» si chiede in un posto solo | erano due funzioni scritte in due momenti — una toglieva `<svg>`, l'altra `<template>`, e solo una schiacciava lo spazio unificatore, lasciando nel contesto del modello un carattere che sembra uno spazio e non lo e'. Nessuna differenza era voluta. `nova/html_a_testo.py` e' l'unione, che e' meglio di tutte e due |
| D126 | Nel corpus di un banco si mette **apposta** cio' che si sospetta manchi | otto entita' HTML che sapevo non essere nella tabella scritta a mano: 27 pagine su 28 identiche, e la ventottesima l'avevo messa per farla fallire. Vedere dove finisce una tabella e' meglio che scoprirlo — scoprirlo vuol dire che un giorno il modello legge `&copy;` in mezzo a una frase. Poi la tabella e' stata estratta invece che scritta: 2.125 voci (D112) |
| D127 | Un numero plausibile non e' un numero verificato | la prima estrazione delle entita' ne prendeva 106, perche' scartavo i nomi col punto e virgola e nel dizionario di Python quasi tutti stanno solo in quella forma. Centosei e' abbastanza plausibile da non far sospettare niente: senza sapere che le entita' HTML sono migliaia, l'avrei accettato. Un conteggio si confronta con cio' che ci si aspetta **prima** di guardarlo |
| D128 | Per arrotondare un decimale si usa il formattatore; per numeri esatti va bene il conto a mano | `0.955` in binario e' `0.95499999999999996`, e Python formatta il valore vero: `0.95`. Moltiplicando per cento il prodotto arriva esattamente a `95.5` e l'arrotondamento al pari da' `0.96`. L'errore non e' nell'arrotondamento, e' nella moltiplicazione. Per la misura di un file vale il contrario e il conto a mano va bene, perche' li' i valori sono interi divisi per potenze di 1024, esatti in binario. Le due scelte sembrano incoerenti: sta scritto in tutti e due i posti perche' chi ne legge uno solo penserebbe che l'altro sbaglia |
| D129 | Un taglio si dichiara sempre | un corpo accorciato in silenzio fa credere al modello di aver letto tutto, ed e' peggio di un corpo accorciato. Vale per il corpo di un ricordo, per la lettura di un file e per l'elenco di una cartella: ovunque si tolga qualcosa, si dice che manca |
| D130 | **I tratti li dichiara NOVA, non la piattaforma**: Windows si appoggia a NOVA, non il contrario | deciso da Gio il 5 settembre. La differenza non e' di stile: in molti punti PowerShell non e' uno strumento che NOVA usa, e' cio' che la regge — gli appunti *erano* `Get-Clipboard`, il volume *era* `SendKeys`, l'elenco delle finestre aperte *e'* `Get-Process` con un `ConvertTo-Csv` in coda. Vuol dire tre dipendenze in mezzo (un processo da avviare, una shell che interpreta, una stringa da comporre col rischio di virgolette), e vuol dire che quelle capacita' **non esistono** dove PowerShell non c'e'. La regola: il tratto lo dichiara il modulo che ne ha bisogno, con i verbi di NOVA («copia questo testo negli appunti»), mai con quelli del sistema («chiama SetClipboardData»); il backend Windows chiama l'API **diretta**, senza shell in mezzo; e ogni capacita' ha una risposta anche dove non c'e' Windows, fosse solo «qui non si puo', e te lo dico». Cosi' la forma di NOVA non cambia quando cambia il sistema sotto. *Il conto esatto sta in D136: «quattordici» era una stima a memoria, e uno dei tre esempi che avevo scritto era falso.* |
| D131 | Un tubo di testo ha un'**encoding**, e se nessuno la dichiara la sceglie qualcun altro | il ripiego degli appunti chiamava `Get-Clipboard` e leggeva la risposta come UTF-8. Ma PowerShell scrive su stdout con la tabella codici della console, non in UTF-8: «perché città però — «virgolette» e un'emoji 😀» tornava con i punti interrogativi al posto delle lettere. Senza un errore, senza un codice di uscita diverso da zero, senza che nessuno se ne accorgesse — e per un utente italiano vuol dire quasi ogni riga. Il guasto e' **vecchio quanto la funzione**, e si e' visto solo perche' D130 mi ha portato a mettere la strada nuova accanto alla vecchia e a confrontarle. Due lezioni: da `_ps` passano molte capacita' e la riga sull'encoding sta li' una volta sola (D62) — e non erano nemmeno quelle: le chiamate a una shell erano dieci in cinque moduli (D135), il conto vero sta in D136; e una capacita' che si appoggia a una shell eredita anche i suoi valori di fabbrica, non solo il suo costo |
| D132 | Un ripiego che **cambia il significato** della richiesta e' peggio di un ripiego che manca | lo strumento promette `mute: true` = silenzia; il ripiego mandava il tasto «muto» di Windows, che *inverte*, e a chi chiedeva «silenzia» con l'audio gia' muto lo riaccendeva. Stessa famiglia di `ask_all`/`always_ask`: un ripiego che sembra prudente e risponde a un'altra domanda. E lo stesso ripiego per il livello manda cinquanta pressioni simulate di «volume giu'» e poi N di «volume su» — fino a novanta secondi — e poi risponde «impostato a circa 50%» senza aver mai **letto** il volume. Dove non si puo' fare la cosa giusta si dice cosa si e' fatto davvero: «stato muto invertito», non «silenziato» |
| D133 | Una prova che tocca uno stato globale si mette in fila con le altre che lo toccano | `ferma()` alza una generazione che vale per tutto il processo, e cargo fa girare le prove in parallelo: se scatta mentre un'altra prova sta fra il suo `gettone()` e il suo `select!`, quella si vede interrompere un lavoro gia' finito e diventa rossa senza che nulla sia rotto. Rossa a caso vuol dire ignorata, che e' peggio di assente. Il punto vero: la stessa osservazione era **gia' scritta** dieci righe piu' sotto, per il contatore «in corso», e non si era mossa da sola sulle altre tre prove dello stesso modulo (D72). E' venuta a galla settimane dopo, per caso, sotto carico |
| D134 | Prima di riscrivere una capacita' si chiede **cosa costa davvero**, perche' non e' sempre la shell | per le notifiche il processo PowerShell erano 300 ms su 9.300: il resto era `Start-Sleep 9`, e non e' sciatteria — il fumetto dell'area di notifica muore insieme a chi possiede l'icona, quindi qualcuno **deve** restare li'. Riscrivere la chiamata in Rust avrebbe tolto il 3% e lasciato NOVA ferma nove secondi lo stesso. Cio' che si cambia non e' l'attesa: e' **chi aspetta**. Un processo suo che nasce, mostra e muore; a NOVA restano 5 ms misurati. La stessa domanda va fatta prima di ognuno dei pezzi che restano: se il costo e' la shell si toglie la shell, se il costo e' un'attesa dovuta si sposta l'attesa, e se non si guarda si riscrive la cosa sbagliata |
| D135 | Chi chiede a un programma cosa ha da dire deve sapere **in che lingua lo dice**, e la risposta non e' la stessa per tutti | riparata la codifica in `_ps`, cercando le altre chiamate ne sono uscite dieci, in cinque moduli, con tre difetti diversi: PowerShell letto come UTF-8 mentre scrive nella tabella della console («perche' citta' pero'» con i punti interrogativi); PowerShell letto senza dire niente, cioe' in cp1252 («perchÃ© cittÃ  perÃ²») — erano i **titoli delle finestre aperte** e i nomi delle applicazioni; e cmd.exe letto come UTF-8 mentre scrive in OEM cp850. Piu' il Python figlio, che senza console ripiega sulla codifica locale. Nessuno dei quattro solleva niente: uscita zero, lettere sbagliate. Le cure non sono la stessa: a PowerShell si **chiede** UTF-8, a cmd.exe si legge nella sua tabella (cambiargliela vorrebbe dire cambiare l'ambiente del comando dell'utente), al Python figlio si dice `PYTHONIOENCODING`. Il posto solo e' `nova/powershell.py`, e la prova non guarda una funzione: conta i posti da cui si avvia una shell, perche' cio' che tiene ferma la riparazione non e' una funzione giusta ma che non ce ne siano altre |
| D136 | Il conto di cio' che si sta per riscrivere si **conta**, non si ricorda (gli errori di chi scrive stanno raccolti in [dove_ho_sbagliato.md](dove_ho_sbagliato.md)) | avevo scritto «quattordici punti» in D130, nel diario, nei commenti del codice e in tre messaggi di commit, e fra gli esempi «la cattura dello schermo *e'* `Add-Type System.Drawing`». Contato con un analizzatore di sintassi: le funzioni che passano da una shell sono **24**, di cui 13 strumenti esposti al modello — e la cattura dello schermo non e' fra loro affatto, usa `mss` e `PIL`. Quel «System.Drawing» che ricordavo e' delle notifiche, cioe' il pezzo che avevo appena riscritto. Il numero sbagliato e' innocuo; l'esempio falso no, perche' avrebbe mandato a riscrivere una cosa che non aveva quel problema (e ne ha un altro: due pacchetti Python). «Quattordici» suonava misurato e non lo era: un numero preso a memoria porta con se' la stessa sicurezza di uno contato, e nessuno dei due segnale su di se' quale dei due e' |
| D137 | Un numero destinato a un modello esce **come numero**; a scriverlo per una persona pensa chi lo mostra | `system_info` rispondeva «RAM_GB: 31,1» con la virgola e, tre righe piu' sotto, «72.5GB liberi» con il punto: due formattatori diversi di PowerShell nella stessa risposta, tutti e due nella lingua dell'utente. Chi legge quella riga e' un modello che ci deve fare un conto, e «31,1» lo puo' leggere come 311. Adesso passa JSON con interi di byte e la scrittura la fa `dati.pesa`, che di regole ne ha una. Nella stessa occasione: la descrizione dello strumento prometteva «batteria, rete» e non dava ne' l'una ne' l'altra — e la descrizione **e' testo che il modello legge e su cui decide**, quindi una promessa non mantenuta li' e' un difetto, non una svista di documentazione. La batteria adesso c'e' (e «non ce n'e' una» e' scritto, perche' il silenzio direbbe «non lo so»); la rete non c'e' e la descrizione non la promette piu' |
| D138 | Una fonte piu' **diretta** non e' per forza una fonte piu' **vera**: la strada nuova si confronta con la vecchia, non si presume migliore | leggendo `ProductName` dal registro — la fonte diretta, quella «giusta» — la risposta era «Windows 10 Pro» su una macchina con Windows 11. Non e' un guasto: Microsoft ha lasciato fermo quel valore apposta, perche' i programmi che lo leggevano per decidere non si rompessero. La query WMI che stavo sostituendo diceva «Windows 11 Pro», e diceva giusto. Me ne sono accorto solo perche' avevo la vecchia risposta sotto gli occhi; misurando solo il tempo — 41 ms contro 1.543 — avrei archiviato la sostituzione come un miglioramento puro. Il numero di build e' il dato che non mente, e 22000 e' la prima build di Windows 11 |
| D139 | Due elenchi si confrontano **riga per riga**, non a insiemi: chi legge un elenco lo legge dall'alto | portando `list_installed_apps` la prima verifica diceva «stessi elementi» e passava. Ma `Sort-Object` di PowerShell ordina secondo la lingua del sistema, e un ordinamento per punto di codice metterebbe «Zoom» prima di «Ärger» su una macchina tedesca: stessi elementi, elenco diverso, e nessuna prova che lo dica. Il modello legge le prime righe e sulle prime righe decide. Le 229 applicazioni ora si confrontano in posizione, e l'ordinamento in Rust ha un secondo criterio — a parita' di lettere, il nome com'e' scritto — perche' altrimenti quale dei due doppioni sopravvive lo deciderebbe l'ordine in cui il registro risponde, cioe' niente |
| D140 | Prima di riscrivere una capacita' si guarda **che domanda fa davvero**, non solo quanto costa | `list_windows` chiedeva a `Get-Process` i processi con una finestra principale: **una finestra per programma**. Lo strumento dichiarava «elenca le finestre aperte». Un browser con tre finestre ne mostrava una, e NOVA non vedeva la propria seconda finestra — misurato, `io.nova.assistente-siw` c'era e non compariva. `EnumWindows` risponde alla domanda dichiarata. Non e' un porting piu' veloce: e' una risposta diversa, e va confrontata come tale invece di aspettarsi che combaci (il contrario di D139, dove combaciare era il requisito). Nella stessa occasione si e' recuperata l'informazione che l'ordinamento alfabetico buttava via: `EnumWindows` restituisce la **pila**, e chi chiede «cosa ho aperto» quasi sempre intende quella davanti |
| D141 | Un dato dell'utente non entra mai in un **linguaggio di modelli**, e chi approva un'azione pericolosa deve vedere **cosa** sta approvando | `close_application` incollava il nome dentro un `-like` di PowerShell. Misurato: `*`, `?` e `[a-z]` selezionavano **292 processi su 292**, e con `force` erano `Stop-Process -Force` sull'intero sistema da un argomento di un carattere. Lo strumento e' pericoloso, quindi una persona approva — ma l'anteprima diceva «Termina FORZATAMENTE '*'», e in quella riga non c'era niente che dicesse «292 processi». Il rimedio non e' un modello piu' prudente: e' togliere il modello (ricerca per sottostringa: `*` si cerca alla lettera) e **separare la selezione dall'azione** — si elenca, si guarda, si chiude un pid, e un numero non ha caratteri jolly. L'anteprima adesso nomina processi, pid e **titoli delle finestre**, e segnala l'asterisco del «non salvato», perche' e' l'unica cosa con cui una persona puo' decidere davvero. Sulla macchina di prova `close_application("notepad", force)` avrebbe portato via una nota aperta e non salvata, e l'anteprima non lo diceva |
| D142 | Il valore di ritorno di una chiamata che puo' **rifiutare** si guarda: rifiutare non e' fallire, e tacerlo e' peggio di tutti e due | `SetForegroundWindow` non lascia che un programma qualunque rubi il primo piano: quando rifiuta non solleva niente, fa lampeggiare l'icona nella barra e torna `false`. Nessuno guardava quel valore, e `focus_window` rispondeva «Finestra in primo piano: ...» anche quando la finestra era rimasta dov'era. Adesso si chiede a `GetForegroundWindow` chi c'e' davvero davanti, e la risposta distingue tre casi: fatto, «Windows non l'ha permesso, l'icona sta lampeggiando», e l'errore vero. Il binario li distingue anche nel codice di uscita — 0, 3, 1 — perche' chi chiama deve poterlo dire all'utente |
| D143 | Un'azione che colpisce «dove capita» si fa solo dopo aver guardato **dove**, e la risposta nomina il bersaglio | `SendInput` — e prima di lui `SendKeys`, e la libreria `keyboard` — non hanno un bersaglio: mandano al sistema, che consegna a chi ha il fuoco *in quel millisecondo*. `type_text` scriveva e rispondeva «Digitati 42 caratteri nella finestra attiva»: vero, e inutile, perche' non dice **quale** — quindi nessuno, ne' il modello ne' l'utente, poteva accorgersi che il testo era finito altrove. L'ho scoperto sbagliandolo: misurando la strada vecchia ho scritto una riga di prova senza verificare il fuoco, e quella riga e' finita in una finestra che non era la mia. Ora il fuoco si legge prima, il suo handle si passa al binario che **ricontrolla** e rifiuta se nel frattempo e' cambiato (uscita 4 — fra l'approvazione di una persona e il momento in cui i tasti partono passa del tempo), e sia l'anteprima sia la risposta chiamano la finestra per nome. Stessa forma di D141: il potere resta, cambia che si veda dove va a finire |
| D144 | Una copia che **funziona** non da' fastidio finche' non diverge, e allora diverge in silenzio: si cerca prima, non dopo | portando la semina del vault e' venuto fuori che chiedeva a PowerShell cose che NOVA sapeva gia': CPU e RAM con una query WMI mentre `nova-sistema` le legge dalle API, e le applicazioni installate con **la stessa identica query di registro** di `list_installed_apps`. Due fonti per lo stesso dato, e nessun modo di accorgersi se un giorno rispondono diverso. Accanto, la quarta copia di «dove sta questo binario»: `runtime.py` aveva ancora la sua, e `nova/binari.py` era nato apposta perche' la terza non diventasse la prima di dieci (D85). Nessuna delle due dava fastidio — funzionavano. Ora `nova/macchina.py` e' l'unico posto da cui si chiede a questo PC com'e' fatto. Nella stessa passata: i comandi `git` non passano piu' da PowerShell (git e' un programma, si chiama; una cartella con l'apostrofo nel nome rompeva la stringa e la semina perdeva quel progetto in silenzio) e una voce di registro chiamata `${{arpDisplayName}}` — un modello mai riempito — smette di entrare nei ricordi come se fosse un'applicazione |
| D145 | **NOVA non si sovrappone a cio' che fa l'utente: lavora separatamente.** Non aver bisogno del fuoco non basta — non bisogna nemmeno **prenderlo** | detto da Gio il 5 settembre, e mi ha corretto una scelta di fondo: stavo perfezionando `type_text`, che con `SendInput` prende la tastiera per costruzione e si puo' rendere onesta ma non separata. La strada separata e' `ui.set_text`, che parla all'applicazione. Misurato pero': scrivendo in un campo di una finestra **non** in primo piano, il primo piano passava a quella finestra — non lo fa NOVA, lo fa il fornitore di accessibilita' di Windows, che per i controlli classici implementa la scrittura con un `SetFocus`; ma da dove sta l'utente quella differenza non esiste. Ora `ui.set_text` e `ui.click` guardano chi c'e' davanti prima, fanno la cosa, e **rimettono a posto il primo piano** se se lo sono preso; se Windows non lo permette non insistono, perche' una lotta per il primo piano e' peggio di un primo piano spostato. Corollario sulle descrizioni: quella di `ui.set_text` prometteva «non dipende da quale finestra ha il fuoco», vero per l'ingresso e falso per l'effetto — e una promessa a meta' e' testo su cui il modello decide (D137) |
| D146 | Uno strumento che nessuna prova chiama puo' non aver **mai** funzionato, e la sua descrizione continua a prometterlo | `create_reminder` componeva un comando PowerShell dentro una stringa, quella dentro l'argomento `/TR` di `schtasks`, e il tutto dentro un `cmd /c`: tre livelli di virgolette annidate. Misurato l'8 settembre: `schtasks` rispondeva «Opzione o argomento non valido: '-NoProfile'» per **tutti e otto** i messaggi di prova, compreso «chiamare il dentista». Non era rotto sui casi difficili: era rotto sempre. Nessuna prova lo chiamava, quindi il difetto non aveva modo di farsi vedere — e intanto la descrizione dello strumento, che e' testo su cui il modello decide (D137), prometteva una capacita' inesistente. Rifatto con un XML dell'Utilita' di pianificazione: programma e argomenti sono due campi distinti, il messaggio dell'utente sta in un file e non entra nella riga di comando (D141), e l'orario e' ISO 8601 invece che nel formato della lingua del sistema — `03/09` e' il 3 settembre in Italia e il 9 marzo negli Stati Uniti |
| D147 | La strada che protegge dev'essere la piu' **solida**, non la piu' comoda: se il ripiego di sicurezza si rompe, si perde la sicurezza | il Cestino e' la premessa N2 — prima la reversibilita', poi il permesso — e passava da un comando PowerShell col percorso incollato fra apici. Misurato: su quattro nomi di file, i due che contenevano un apostrofo **non ci finivano**. «L'anno scorso» e' un nome di cartella normale. Il codice si comportava bene — tornava «non ci sono riuscito» e chi lo chiamava si fermava invece di distruggere — ma il motivo per cui l'utente non poteva piu' cancellare quei file era una virgoletta. Adesso passa da `IFileOperation`, quello che usa Esplora risorse: prende il percorso come oggetto. E la prova non si accontenta di «il file non c'e' piu'», che lo otterrebbe anche `unlink`: va a cercarlo **dentro il Cestino di Windows**, perche' cancellato e cestinato sono due cose diverse e la suite passerebbe identica se il codice sbagliasse |
| D148 | Nella memoria, **cio' che resta fuori non lascia traccia**: la politica di selezione si porta con un banco che confronta l'ordine e il *perche'*, non solo i punteggi | `nova-memoria` sapeva gia' pesare — BM25, fusione, spareggio. Mancava la parte che con quei pesi **decide**: chi entra nel contesto del modello. E' la piu' delicata di tutta la memoria per una ragione asimmetrica: un nodo pesato male sta nel posto sbagliato dell'elenco e si vede; un nodo **scartato** no — il modello risponde come se non esistesse, e nessuno ha modo di accorgersene. Percio' il banco non confronta i numeri: confronta l'elenco scelto, il suo ordine e la *via* con cui ogni nodo e' entrato (nominato per nome, trovato dalla ricerca, raggiunto dal grafo), contro il `KBEngine` **vero** e non contro una sua copia. Due nodi giusti nell'ordine sbagliato cambiano cosa il modello legge per primo; e un nodo entrato «da grafo» invece che «esatto» vuol dire che la ricerca ci e' arrivata per un'altra strada, anche se il risultato somiglia. L'embedder resta fuori — i punteggi densi arrivano gia' calcolati — per la stessa ragione per cui resta fuori il disco: questa e' la politica, e la politica e' aritmetica |
| D149 | Riparato un difetto, si va a cercare il **fratello**: chi ha scritto la prima copia ha scritto anche la seconda, con la stessa idea sbagliata | riparato `create_reminder` (D146), `pianifica` faceva la stessa cosa nello stesso modo — e nessuno era andato a guardare. Misurato: «controlla l'agenda» veniva registrata come «controlla l"agenda», un apostrofo diventato virgoletta, e NOVA si sarebbe posta una domanda diversa da quella chiesta senza che niente lo segnalasse. In piu' le virgolette doppie nell'istruzione erano **vietate** — una limitazione dichiarata all'utente per non affrontare l'escaping — e la data usava il formato della lingua del sistema. Tutti e tre spariscono con lo stesso rimedio del promemoria, e ora il rimedio sta in `nova/attivita.py`: alla seconda occorrenza la cosa condivisa si mette in comune (D62), e qui le due copie erano nate dalla stessa idea sbagliata, quindi condividevano anche i difetti |
| D150 | **Un elenco di file rimasti non e' un elenco di lavoro rimasto.** Un cantiere si dichiara chiuso guardando cosa ogni file *chiede*, non come si chiama | Chiudendo i corpi degli strumenti ho annunciato che dentro CANT-2 restavano cinque file, e li avevo scelti leggendo i nomi nella cartella. Un `grep` sugli import ha detto che nessuno dei cinque appartiene a CANT-2: due chiedono di scegliere librerie Rust per documenti e schermo (CANT-8), due sono fili verso pezzi Rust che esistono gia' (CANT-3), uno e' il browser (CANT-6), e uno — `automazioni.py` — esegue corpi Python **per disegno**, perche' e' il posto dove NOVA si scrive strumenti nuovi mentre gira: non e' codice da tradurre, e' una decisione da prendere quando si sapra' cosa resta di Python. La differenza non e' contabile: «continuare di li'» avrebbe scelto tre librerie di documenti dentro il cantiere sbagliato, cioe' senza la domanda che quel cantiere si porta dietro. E' D99 applicato al proprio piano invece che al codice: prima di chiedersi cosa manca, guardare cosa c'e' |
| D151 | Il **taglio del contesto** si porta con un banco che confronta l'elenco finale *carattere per carattere*, la scritta del taglio compresa, e in piu' **quanti** messaggi sono stati tolti e per quale ragione | E' il pezzo piu' delicato del cantiere, e per la stessa asimmetria di D148: cio' che resta fuori non lascia traccia. Un ordinamento sbagliato si vede — la risposta e' storta; un messaggio buttato no — il modello risponde come se non fosse mai stato detto, con la stessa sicurezza. Percio' non basta che i due elenchi finali coincidano: due implementazioni possono arrivarci per strade diverse e divergere al primo caso che le separa (D51), quindi il banco confronta anche i conteggi per ragione — tolti per numero, orfani scartati, tolti per token, ripescato l'ultimo, accorciati e di quanto. Ventisette scenari, e la verifica che il banco sappia diventare rosso: tre mutazioni fatte apposta nel Rust — la stima a byte invece che a caratteri, l'obiettivo a 0,80 invece di 0,75, lo scarto degli orfani tolto — hanno acceso rispettivamente sette, quattro e cinque verifiche. Un banco che non si e' mai visto fallire non e' un banco, e' una speranza (D53) |
| D152 | **In italiano i caratteri non sono byte, e il taglio conta caratteri.** Ogni misura di lunghezza in `nova-contesto` lavora su `char`, mai su `&str` | In Python `len(testo)` conta caratteri e `testo[:meta]` taglia caratteri; in Rust la stessa scrittura conta byte. Su testo italiano non e' la stessa cosa: «perché la città è così» pesa piu' byte che caratteri, quindi una stima a byte direbbe che il messaggio e' piu' grande del vero e taglierebbe **prima**, in silenzio, proprio nelle conversazioni in italiano — cioe' tutte. E il taglio a byte dentro una lettera accentata non e' un errore di stima: in Rust e' un panico, che arriva all'utente. La mutazione di prova ha acceso sette verifiche su testo accentato e nessuna su testo ASCII: se il banco avesse avuto solo scenari in inglese sarebbe rimasto verde per sempre |
| D153 | Portando si **dichiara** cio' che si scopre, non lo si aggiusta di nascosto: prima le due parti devono essere uguali, poi si discute | Due cose viste solo scrivendo le prove. **Uno**: il taglio a numero non ha la rete che ha il taglio a token — se la coda finisce tutta in risposte di tool orfane, resta il solo messaggio di sistema e la conversazione sparisce senza che niente lo dica, mentre il taglio a token in quel caso ripesca l'ultimo messaggio. Servono quaranta risposte di tool di fila nel punto esatto del taglio: improbabile, non impossibile. **Due**: la funzione che accorcia il messaggio piu' grosso oggi riceve sempre **un solo messaggio**, perche' chi la chiama ci arriva solo dopo che il ciclo di svuotamento ha lasciato la coda vuota; il codice che sceglie fra piu' messaggi non e' morto ma dormiente. Tutte e due sono state portate **uguali**, con una prova che descrive il comportamento vero invece di quello desiderato, e scritte qui. Aggiustare in Rust cio' che il Python fa diversamente vuol dire che il banco non confronta piu' niente, e che il difetto resta in produzione — dove il codice gira ancora oggi |
| D154 | Le chiamate che certi modelli scrivono **dentro il testo** si leggono a mano, non con l'espressione regolare ricopiata: scriverla a mano costringe a dire la regola ad alta voce | E' il punto in cui della prosa diventa un'azione. `<tool_call>\s*(\{.*?\})\s*</tool_call>` dice due cose precise che a leggerla in fretta si perdono: la graffa dev'essere la **prima cosa non bianca** dopo l'apertura, e la chiusura buona e' la prima preceduta (a meno di spazi) da una graffa. Un `<tool_call>` seguito da una parola e poi da un oggetto non e' una chiamata — riconoscerlo come tale vorrebbe dire eseguire qualcosa che il modello non ha chiesto, e non riconoscere una chiamata vera vorrebbe dire ignorare una richiesta. Sta in `nova-strumenti` perche' li' c'e' gia' tutto cio' che riguarda una chiamata: la dichiarazione, la guardia, il racconto (D99) |
| D155 | **Gli argomenti si rendono come li rende Python**, separatori compresi, e il «vuoto» si giudica come lo giudica Python | Quella stringa non e' una rappresentazione: e' cio' che lo strumento **riceve**. `json.dumps` scrive `{"a": 1}`, `serde_json::to_string` scrive `{"a":1}`, e un banco senza uno scenario a due chiavi non vedrebbe mai la differenza (la verifica che lo dice sta nel banco, e fallisce apposta se gli scenari non la distinguono). Stessa storia per la scelta del campo: il Python usa `or`, quindi `"arguments": ""` non e' una stringa vuota da passare avanti ma un campo che **non conta**, e si guarda `parameters`; con un semplice «c'e'/non c'e'» si passerebbe `""` dove il Python passa `{}`. L'ordine delle chiavi si conserva perche' `serde_json` qui e' compilato con `preserve_order` — lo stesso motivo per cui la scala del routing non si riordina da sola |
| D156 | Una prova che aspetta **il mondo** e non il codice dichiara il suo tempo, e il banco glielo concede entro un tetto | `test_promemoria.py` chiede a Windows di eseguire un'attivita' al prossimo minuto tondo e poi aspetta: quanto duri dipende dal **secondo in cui e' partita** — fra i 35 e i 95 secondi, piu' il tempo che Windows si prende per dire com'e' andata. Con i novanta secondi uguali per tutte era una monetina: verde se partiva a meta' minuto, «appesa» se partiva all'inizio. Ed e' il peggior modo di essere rossa, perche' il verde per meta' delle volte lo si crede. Adesso una prova puo' scrivere `# banco: attesa 240` nelle prime righe; il banco lo legge senza importarla — importarla per sapere quanto dura vorrebbe dire eseguirla — e non concede mai meno del minimo (dichiarare cinque secondi sarebbe un modo di rendersi verdi) ne' piu' del tetto (una dichiarazione sbagliata non deve poter bloccare il banco per sempre) |
| D157 | Il prompt di sistema **non si formatta, si sostituisce**: i tre segnaposto conosciuti e nient'altro | Difetto trovato portando, e reale: `str.format` non guarda i tre segnaposto, guarda **tutte** le graffe. Un `system_prompt` personalizzato in `config.json` che contenga un esempio JSON — `{"a": 1}` — o una graffa vuota faceva saltare la composizione, e la composizione sta dentro `Agent.__init__`: NOVA **non partiva**, e quello che l'utente leggeva era `KeyError: '"a"'`. Misurato con quattro prompt. Chi scrive un prompt di sistema ci mette esempi, e gli esempi hanno le graffe — quindi la formattazione generale e' lo strumento sbagliato in partenza. Corretto **da tutte e due le parti insieme**, non solo nel Rust (D153), e il banco confronta il nuovo comportamento su dieci prompt, compresi i quattro che prima esplodevano |
| D158 | I ventimila caratteri del prompt di sistema si **estraggono** dal Python, non si ricopiano, e l'estrattore si rilegge da solo | Stessa ragione delle sessanta dichiarazioni (D112, D114): sono cio' su cui il modello decide come comportarsi, e una parola diversa e' un comportamento diverso che nessun tipo intercetta. `_estrai_prompt.py` scrive `testi.rs` e poi **rilegge il file appena scritto**, sfila il letterale grezzo e lo confronta con la stringa Python — perche' «ho scritto il file» non e' una verifica: un delimitatore sbagliato darebbe un file plausibile e mutilato, e il banco lo confronterebbe con se stesso. Il banco poi ripete il confronto carattere per carattere a ogni esecuzione: una mutazione di **una** lettera dentro i quindicimila caratteri ha acceso nove verifiche, dicendo anche a quale carattere |
| D159 | Una prova che guarda il **codice sorgente** deve cercare una proprieta', non una scrittura: altrimenti diventa rossa per un nome cambiato e verde per un difetto vero | `test_prefisso.py` verifica una cosa che non si vede da fuori — che il prompt di sistema non contenga niente che cambi da solo, perche' basta un contatore o un'ora ricalcolata per invalidare diecimila token di cache a ogni turno. Per farlo legge il sorgente, e cercava la stringa `now=datetime.now()`. Riscritta la composizione, quel nome e' sparito e la prova e' diventata rossa **senza che il difetto ci fosse**: costo puro, e il tipo di rosso che insegna a ignorare i rossi. Adesso conta quante volte l'orologio viene letto dentro `system_prompt` — uno — e verifica che nessun altro pezzo del turno lo rilegga: la stessa domanda, ma posta a cio' che il codice **fa**. Provata con una mutazione che legge l'ora due volte: rossa |
| D160 | Quello che si attacca **in coda alla domanda** sta in `nova-contesto`, quello che parla di procedure sta in `nova-ricette`: il testo vive accanto al suo dato, il **dove** e' una domanda di finestra | Il blocco di memoria e il blocco delle procedure fanno la stessa cosa — aggiungono roba alla domanda invece che al prompt di sistema — e per la stessa ragione doppia: i cervelli agentici il prompt di sistema lo ricevono solo all'apertura della sessione (quindi il contesto veniva calcolato e buttato: NOVA faceva la ricerca sul grafo e non la leggeva), e il messaggio di sistema e' la prima regione su cui i fornitori tengono la cache, quindi cambiarlo a ogni turno rielabora l'intera conversazione. E' la stessa aritmetica del fondo nel taglio dei messaggi: non cambia cosa il modello legge, cambia quanto spesso si butta via la cache. Ma il **testo** delle procedure ha bisogno dei dati delle procedure, e un testo lontano dal suo dato si aggiorna a meta' (D72): quindi la regola di composizione sta in `nova-contesto` e il testo delle ricette sta in `nova-ricette` |
| D161 | Chi **scrive** un numero non e' chi decide quante cifre tenere: l'arrotondamento sta dove sta nel Python, e non si fa due volte | Il punteggio di somiglianza finisce dentro il testo che il modello legge. La prima stesura arrotondava dentro la funzione che scrive, ma nel Python e' `proponi` ad arrotondare e il testo scrive quello che riceve: il banco l'ha detto subito con un caso a 0,125 — un carattere di differenza. Due cose separate, e tutte e due con la loro trappola. **Scrivere**: `format!("{}", 1.0_f64)` da' `1`, `str(1.0)` in Python da' `1.0`. **Arrotondare**: non `(x * 100).round() / 100`, che introduce un errore capace di far attraversare la mezza cifra al numero sbagliato — misurato, diverge su 0,125, 0,615 e 2,675 — ma la stessa cosa che fa Python, cioe' scrivere il valore binario esatto con due decimali correttamente arrotondati e rileggerlo |
| D162 | Un codice HTTP si spiega in **un posto solo**, dentro `nova-guasti`, non nel client di ogni fornitore | La stessa risposta la ricevono tutti i cervelli a pagamento, e una spiegazione per fornitore vorrebbe dire cinque spiegazioni che divergono. E' il punto 7 dell'attrito — «non ci riesco» deve dire perche' e cosa fare: un 401 che arriva come `401` manda a cercare su internet, un 401 che arriva come «la chiave non e' stata accettata, controllala nelle impostazioni alla voce Cervello» si risolve da solo. Dentro ci sono i due rami che non si indovinano leggendo la specifica HTTP: llama.cpp usa **400** per il contesto sfondato invece di 413, e risponde **500** quando gli arriva un'immagine e lui e' partito senza proiettore — un errore di configurazione travestito da guasto del server, dove dire «di solito passa da solo» manda l'utente ad aspettare una cosa che non succedera' mai |
| D163 | Il **corpo** di un errore non si incolla mai com'e': si estrae il motivo, si maschera e si taglia | Quando la chiave e' sbagliata il fornitore la **rimanda indietro dentro il proprio messaggio**, e da li' finirebbe in chat e nel registro. Il motivo utile e' sepolto nel JSON, quindi si scende — per al massimo quattro livelli, perche' un JSON che si annida all'infinito e' un JSON ostile e qui si sta leggendo la risposta di qualcun altro — poi si passa da `senza_chiavi` e si taglia a duecento caratteri. Il banco confronta ottantacinque spiegazioni e diciannove corpi, uno dei quali contiene una chiave vera, e ha una verifica che si arrabbia **se quel corpo non c'e'**: senza, il controllo «la chiave non compare» sarebbe verde per assenza. Mutazione di prova con `senza_chiavi` tolto: tre verifiche rosse, chiave in chiaro |
| D164 | Il banco ha **tre** colonne, non due: verde, rossa, e «non provabile qui» | Alcune prove escono con codice 2 per dire «questa macchina non ha come provarmi»: `test_tastiera.py` quando non riesce a prendere il fuoco — e non scrive alla cieca (D145) — e `test_scala_rust.py` quando il banco Rust non e' costruito su questo sistema. Il banco pero' guardava solo `returncode == 0` e le contava fra le **rosse**. Su questa macchina non si vedeva, perche' i banchi erano costruiti e il fuoco libero; si e' visto la sera in cui c'era una partita a schermo intero davanti. Sulla macchina di qualcun altro — cioe' esattamente al punto 1 del cancello — meta' della suite avrebbe detto «rossa» parlando di se' e non del codice, e il primo che la installa avrebbe cercato difetti che non esistono. Contarle fra le verdi sarebbe peggio: vorrebbe dire credere provata una cosa che nessuno ha provato. Quindi tre colonne, e le non provabili si **dicono sempre**, anche quando non cambiano il verdetto (D129) |
| D165 | Quando non si puo' distinguere «il codice non ci riesce» da «la macchina in questo momento non risponde», e' **rossa**: «non provabile» si dichiara solo su una premessa che si e' verificata mancante | La prova che scrive in una finestra senza toccare la tastiera puo' fallire in due punti diversi, e non sono la stessa cosa. Se non riesce a mandare il fuoco altrove, la **premessa** della prova non c'e' — Windows non lascia spostare il primo piano, ed e' giusto cosi': quella e' «non provabile», ed e' verificabile guardando chi sta davanti. Se invece la scrittura vera fallisce, da li' non si puo' sapere se sia NOVA o la cavia: chiamarla non provabile sarebbe scegliere l'ipotesi comoda, ed e' il modo esatto in cui una prova diventa verde per assenza. Resta rossa — ma senza traceback, con scritto cosa e' successo e cosa provare prima di cercare il difetto nel codice (ATT-1) |
| D166 | Di un risultato che nomina immagini se ne consegna al modello **una sola**, e solo se ne nominava una sola: molte si dichiarano e non si allegano | La regola «se il risultato nomina un'immagine che sta su disco, quella si guarda» era stata scritta per uno strumento che **produce** un'immagine, e vale anche per gli strumenti che verranno — era il suo pregio. Ma `search_files` restituisce percorsi assoluti, uno per riga: «trova le foto del matrimonio» produceva un risultato che ne nomina venti, e le **prime due** venivano convertite in base64 e allegate alla conversazione, quindi con un cervello a pagamento **uscivano dal PC** al giro successivo. Misurato con tre file finti, non immaginato. Il discriminante non e' la cartella — un utente puo' legittimamente dire «guarda questa foto sul desktop» — ma il **numero**: uno strumento che produce un'immagine ne produce una, un elenco ne nomina tante. Adesso molte immagini producono una riga che dice quante sono e che non sono state allegate (D129), e il modello puo' chiederne una per nome |
| D167 | Una frase falsa detta al modello vale quanto un dato falso | Il testo che accompagnava l'immagine diceva «questa e' la figura **prodotta** dallo strumento» anche quando lo strumento si era limitato a elencarla. Non e' una sfumatura di stile: e' l'unica riga che il modello legge per sapere **da dove viene** quello che sta guardando, e da li' decide se e' roba sua o roba dell'utente. Adesso dice «ha nominato» |
| D168 | `str.splitlines()` di Python e `str::lines()` di Rust **non sono la stessa cosa**, e qui la differenza si paga in cose non imparate | Python taglia anche su `\r`, `\v`, `\f`, `\x1c`-`\x1e`, `\x85`, ` ` e ` `; Rust taglia solo su `\n`. Sembra pedanteria finche' non si guarda **chi** ha scritto il testo: qui e' un modello, e un modello scrive quello che gli pare. Misurato con una mutazione che usa `lines()`: una risposta perfettamente buona separata da ` ` diventa una riga sola, quindi «risposta troppo corta», quindi NOVA **non impara la procedura** e nel registro c'e' scritto che il modello ha risposto male. Un difetto che non rompe niente e fa perdere una cosa per volta |
| D169 | Il prompt che decide **cosa NOVA impara** non si e' ricopiato: si e' catturato prima, poi rifattorizzato, poi confrontato | Millesettecento caratteri dentro un metodo di novanta righe, mescolati alla rete e al filo. Spostarli in un modello con tre segnaposto e' esattamente il genere di cosa che si fa «senza cambiare niente» e cambia una parola — e una parola diversa in un prompt e' un comportamento diverso che nessun tipo intercetta (D112). Quindi: prima si e' catturato il testo esatto che il codice di allora produceva per tre gruppi di argomenti (`_cattura_prompt.py`), poi si e' rifattorizzato, poi si e' verificato che il nuovo producesse gli stessi 1010, 2123 e 978 caratteri. Da li' in poi il confronto continuo lo fa il banco |
| D170 | «Cosa non entra in memoria» si chiede **anche alla provenienza**, non solo alla forma, e i due elenchi si confrontano per intero | Il guardiano legge il testo e riconosce una chiave, un numero di carta, una password. Ma il titolo di una finestra e' una stringa qualunque: non c'e' niente da riconoscere, e proprio per questo passerebbe. Se un turno ha usato uno strumento che guarda lo schermo, ricordarlo in chiaro scriverebbe nel vault i titoli delle schede aperte e dei documenti su cui si sta lavorando — e un vault e' una cartella che la gente sincronizza. Percio' `GUARDANO_LO_SCHERMO` sta accanto al guardiano, che e' dove sta la stessa domanda posta all'altro modo. E il banco confronta **l'elenco intero**, non un campione di casi: un elenco provato per campioni si accorge di uno strumento aggiunto da una parte sola solo se per caso quel caso c'era (D113). Mutazione di prova, uno strumento tolto dal Rust: due verifiche rosse, di cui una dice esattamente quale manca |
| D171 | «Questo indirizzo e' in casa?» si decide sull'**host**, estratto come lo estrae `urlparse`, e sta in `nova-scala` | E' la frase su cui NOVA sta in piedi ridotta a una domanda sola: niente esce dal PC finche' qualcuno non delega davvero. Un server locale non chiede nessuna chiave, e pretenderne una vorrebbe dire rifiutarsi di parlare con un cervello che e' li', acceso e gratuito; ma un confronto per sottostringa chiamerebbe «casa» anche `https://localhost.evil.example.com`. L'host si estrae a mano, senza dipendenze: e' il pezzo che decide cosa esce dal PC, e li' si vuole meno codice di qualcun altro, non di piu'. La regola di `urlsplit` ha una conseguenza che a occhio non si indovina — **l'autorita' esiste solo dopo `//`** — quindi `localhost:8080` senza schema non ha host, `localhost` viene letto come *schema*, e l'indirizzo non e' in casa. Sembra sbagliato e non lo e': quella stringa non e' un URL, e indovinare cosa intendesse chi l'ha scritta e' il genere di gentilezza che qui non si fa. Ventisei indirizzi confrontati contro `urlparse` vero; mutazione per sottostringa: tre rossi |
| D172 | Il ragionamento del modello si separa dalla risposta **anche quando il tag non e' chiuso** | I modelli il ragionamento lo scrivono dentro `<think>...</think>` nel testo, o in un campo a parte. Le due cose vanno tenute separate perche' l'utente legge la risposta: un ragionamento che finisce nel contenuto e' il modello che si contraddice a voce alta davanti a chi ha chiesto qualcosa. Il caso che si vede di piu' e' quello in cui il modello **si interrompe a meta'** del ragionamento: senza il secondo taglio quel troncone finirebbe intero nella risposta, e si nota perche' non finisce nemmeno con una frase compiuta |
| D173 | CANT-4 comincia da cio' che **decide**, non da cio' che avvia: la riga di comando, la scala dei layer, e l'unico errore che vale la pena riprovare | Il grosso di `runtime.py` era gia' in `nova-modelli` prima ancora che CANT-4 cominciasse — i GGUF, i motori, i conti sulla VRAM, gli strati — e chiederselo prima di scrivere ha risparmiato di riportare quattrocento righe (D99). Quello che restava e' il pezzo dove le decisioni si vedono poco e costano molto: un flag in meno vuol dire meta' della memoria sprecata, un flag in piu' vuol dire un binario vecchio che non parte, un numero di layer sbagliato vuol dire un modello che gira dieci volte piu' piano **senza dire niente**. Il processo non si avvia qui: qui si decide cosa gli si dice |
| D174 | Un riconoscitore di errori si porta **con dentro anche cio' che non riconosce** | Le sei parole con cui llama.cpp dice «non ci sta in memoria» vengono da posti diversi — CUDA, Vulkan, l'allocatore di ggml — e riconoscerne una sola vorrebbe dire riprovare con meno layer in un caso su sei. Portandole ne sono saltate fuori due che **non** si riconoscono: la costante Vulkan con i trattini bassi (`VK_ERROR_OUT_OF_DEVICE_MEMORY`, come la scrivono i livelli di validazione — dentro llama.cpp arriva nella forma di `vulkan.hpp`, che invece passa) e il messaggio di ggml quando l'allocazione fallisce senza usare nessuna delle sei parole. Portate **uguali** e scritte in una prova che dichiara il buco invece di nasconderlo (D153): sono i due casi in cui NOVA non riprova e si arrende, e uno che li conosce puo' decidere se allargare la rete |
| D175 | Nel protocollo MCP, **«non rispondere» e' una risposta**: una richiesta senza `id` e' una notifica, e a una notifica non si replica mai — nemmeno per dire che il metodo non esiste | E' la regola che a leggere la specifica in fretta si perde, ed e' quella che pianta i client: chi riceve una risposta a una notifica resta ad aspettare una risposta che non arrivera' mai, oppure la accoppia alla richiesta sbagliata. Il banco confronta anche i `None`, e ha una verifica che si arrabbia se **nessuno** degli scenari e' senza risposta: un banco che prova solo le domande non prova il silenzio |
| D176 | «Non ha funzionato» e «non ci siamo capiti» sono **due buste diverse**, e la differenza e' cio' che l'altro programma usa per decidere se riprovare | Uno strumento che non esiste e' un errore di protocollo (`-32601`); uno strumento che esiste ed esplode e' un **risultato riuscito** con `isError: true`. Confonderli vuol dire che un programma riprova all'infinito una chiamata che non ha senso, o si arrende alla prima cosa che poteva riuscire al secondo tentativo. Stessa cura sul nome mancante: `tools/call` senza `params` non ha nome, e il Python scrive «strumento sconosciuto: **None**» — non stringa vuota. «Nome vuoto» e «nome mancante» sono due errori diversi da riparare, e quel messaggio e' l'unica cosa che chi ha sbagliato la chiamata riesce a leggere. Trovato dal banco al primo giro |
| D177 | Se non si e' potuto **chiedere** il permesso, si **nega** | Quando un altro programma chiede a NOVA di fare qualcosa sul PC, la domanda passa dal demone. Se il demone non risponde, nessuno puo' autorizzare: rispondere «consenti» vorrebbe dire che un guasto di NOVA si trasforma in un permesso, cioe' che un demone spento aggira in silenzio il livello di autonomia scelto dall'utente. E i modi di finire sono **cinque** e non tre, perche' «non ho potuto chiedere» non e' «ha detto di no»: il primo e' un guasto e va detto com'e', il secondo e' una decisione e va rispettata in silenzio — e chi legge deve poter decidere se riprovare, cosa che ha senso in un caso solo |
| D178 | Il JavaScript che gira **nella pagina dell'utente** si estrae, non si ricopia — e gli argomenti ci entrano **sempre** da una funzione sola | E' l'unica parte di NOVA eseguita da un interprete che non e' nostro, su un documento dove l'utente e' gia' autenticato. Un selettore e un testo da scrivere sono dati che finiscono dentro codice: non c'e' un posto in NOVA dove il confine fra argomento e codice conti di piu'. Ci si passa **anche** per un selettore che «sicuramente non ha virgolette», perche' un confine che vale solo per gli argomenti prevedibili non e' un confine. E si scrive come lo scrive `json.dumps` di Python, `\uXXXX` compresi e con le coppie surrogate: le due forme sono tutte e due JavaScript valido, ma un banco che accetta due scritture diverse smette di accorgersi di tutto il resto |
| D179 | La scheda del browser si sceglie **per identificativo prima**, per testo poi | Sbagliare qui non da' un errore: da' il **contenuto di un'altra pagina**, che e' molto peggio. Un profilo nuovo di Edge ha gia' due o tre schede sue — benvenuto, estensioni, ricerca — e la prima della lista non e' quasi mai la tua. Il banco ha apposta il caso che distingue le due regole: un identificativo che e' anche un pezzo dell'indirizzo di un'altra scheda. Mutazione con l'ordine invertito: chiedere la scheda «esempio» ne restituisce un'altra |
| D180 | «Cosa dice questa pagina» si porta con la tabella delle entita' **intera**, estratta da `html.entities`, non con le venti che vengono in mente | Sono duemiladuecentotrentuno nomi, in due forme ciascuno — `amp` e `amp;` sono voci diverse nello standard, e senza punto e virgola valgono solo per un elenco chiuso. Una tabella scritta a mano ne avrebbe venti, e le altre non darebbero **nessun errore**: lascerebbero `&hellip;` e `&rsquo;` scritti dentro il titolo che NOVA mostra, cioe' un titolo sbagliato che nessuno riconosce come tale. E non se ne accorgerebbe nemmeno un banco, perche' i casi li sceglie chi ha scritto la tabella e sceglierebbe gli stessi venti (D113). Stessa regola per le espressioni regolari che dicono cosa, di una pagina, e' testo: estratte — dagli oggetti gia' compilati quando sono costanti di modulo, dall'albero sintattico quando vivono dentro una funzione. L'unica riscritta e' `INVISIBILE`, perche' usa un riferimento all'indietro che il motore di Rust non ha: l'estrattore ne genera l'espansione **e** l'originale, e una prova controlla che dicano la stessa cosa sullo stesso testo |
| D181 | Il riassunto di un risultato di ricerca si cerca **nella finestra di quel risultato**, non «il primo che c'e' piu' avanti» | Trovato dal banco, ed e' un difetto vero, non una differenza di porto. I due raschiatori erano una sola espressione col riassunto in un gruppo facoltativo in fondo: un gruppo facoltativo si prende il primo riassunto che trova, quindi un risultato **senza** riassunto si prendeva quello del risultato dopo — e `finditer` riparte da dove ha finito, quindi si portava via anche quel risultato. Effetto su una pagina vera: un risultato in meno nell'elenco, e un riassunto attaccato all'indirizzo sbagliato. Nessun errore, nessuna riga di log: solo NOVA che descrive un sito e ne linka un altro. Ora le espressioni sono due, e il riassunto vale solo fra la fine di un risultato e l'inizio del prossimo — in Python e in Rust, che restano identici. Mutazione con la finestra aperta fino in fondo alla pagina: rosso |
| D182 | Le abitudini di **Python** che il porto deve rispettare stanno in un posto solo, `nova-pitone` | Dove finisce una riga e cos'e' uno spazio sono due domande su cui le due librerie standard rispondono diversamente, e ognuna e' gia' costata un banco rosso: `str.splitlines()` taglia anche su `\v`, `\f`, `\x1c`-`\x1e`, `\x85`, `\u2028`, `\u2029`, mentre `str::lines()` taglia solo su `\n`; `str.isspace()` dice di si' anche sui separatori `\x1c`-`\x1f`, che `char::is_whitespace` non considera bianchi. Le sapeva `nova-ricette`, contando le righe della risposta di un modello. Alla seconda occorrenza — il testo di una pagina web — si condivide (D62): non per eleganza, ma perche' la stessa domanda in due posti diventa, prima o poi, due risposte diverse (D73). Mutazioni con `lines()` e con `trim()`: rosse tutte e due, e la seconda solo dopo aver aggiunto il caso col separatore **in mezzo** alla riga — in fondo lo toglieva comunque la ripulita finale, e la prova sembrava buona senza esserlo |
| D183 | L'elenco degli strumenti che un cervello agentico puo' usare e' **una stringa sola**, estratta dal sorgente, e si conta | E' la dichiarazione piu' silenziosa di tutto NOVA: trentatre' nomi separati da virgole, e un nome sbagliato **non da' nessun errore**. Da' un cervello a cui manca una capacita' e che non sa dire perche'. E' gia' successo due volte. La prima: l'elenco era scritto come quattro elementi di lista, e «Read», «Glob» e «Grep» finivano sulla riga di comando come argomenti a se' stanti — a seconda della versione del CLI venivano assorbiti o ignorati, e in nessun caso erano davvero fra i permessi. Senza `Read`, NOVA scattava screenshot che non poteva guardare: il file finiva su disco e il modello non lo vedeva mai, perche' `Read` e' anche cio' che apre le immagini. La seconda: senza `--strict-mcp-config` entravano nella sessione i connettori dell'account dell'utente — un centinaio di strumenti inutili e, soprattutto, l'avviso «questi server richiedono autenticazione» da cui NOVA aveva imparato a rispondere «autorizza il connettore da claude.ai» a chi le chiedeva di guardare la posta. Ora l'elenco si estrae, il banco lo confronta col Python e **conta i nomi**: mutazione che lo rispezza in argomenti separati, rosso |
| D184 | Sulla riga di comando di un cervello esterno **l'ordine e' una regola**, non uno stile: prima le opzioni MCP, poi il prompt di sistema — e il prompt, quando si puo', non ci passa affatto | Su Windows `claude` e' `claude.cmd`, un file batch: lo esegue `cmd.exe`, che **rianalizza** la riga di comando. Un argomento che contiene degli a capo — e il prompt di sistema ne contiene una sessantina — la chiude li', e tutto cio' che segue non arriva mai al programma. Misurato con quattro ordini diversi e la stessa domanda: col prompt lungo su una riga sola i server MCP arrivavano, con gli a capo no. Il sintomo era che NOVA perdeva le proprie quarantanove capacita' — fra cui tutte le `ui_*`, cioe' le mani sul browser — **solo nelle sessioni nuove**, perche' solo li' il prompt di sistema viene passato: le sessioni riprese funzionavano, e il difetto sembrava un capriccio. Poi c'e' il tetto: la riga di comando di Windows finisce a 8191 caratteri e il prompt da solo ne pesa 8641, e oltre il limite `cmd.exe` non tronca — **rifiuta**, e non si apre piu' nessuna conversazione nuova. Percio' il prompt si scrive in un file e si passa il percorso: sessanta caratteri invece di ottomila. Due mutazioni, ordine invertito e prompt passato anche alle sessioni riprese: rosse tutte e due |
| D185 | **Cosa non si tocca e cosa non si esegue e' un elenco solo**, estratto da `nova/config.py`, e lo applica un meccanismo solo | Erano due elenchi — uno in Python, uno scritto a mano nella configurazione del demone — e come tutti gli elenchi separati sapevano cose diverse. Al demone mancavano `cipher /w`, che cancella lo spazio libero e quindi rende irrecuperabile cio' che era **gia'** stato cancellato, e `wevtutil cl`, che svuota i registri eventi di Windows: due comandi che stanno insieme nel manuale di chi vuole non lasciare traccia. A NOVA mancavano le due forme Unix. E al demone mancava `C:\ProgramData\Microsoft` fra i percorsi protetti. Nessuna delle tre mancanze era una scelta: mancavano perche' erano due elenchi. Ma il difetto piu' serio era il **meccanismo**: il demone confrontava per sottostringa, e per non bloccare `Get-Date -Format o` con `format ` aveva dovuto aggiungersi una regola sua — «conta solo dove starebbe un comando» — che dall'altra parte non esisteva. Due meccanismi sullo stesso elenco sono due risposte diverse alla stessa domanda: `vssadmin.exe delete shadows` passava di la' e non di qua. Ora l'elenco e' uno, le espressioni regolari sono quelle di Python, e il demone usa `nova-strumenti::guardie`, che un banco confronta gia' col Python. In piu' i predefiniti si **aggiungono** a quelli della configurazione invece di lasciarsi sostituire: un file salvato prima che l'elenco crescesse non e' una scelta dell'utente, e' un elenco congelato — ed e' gia' successo col prompt di sistema, dove una copia vecchia su disco ha tolto a NOVA per mesi una capacita' che aveva. La prova che tiene ferma la riparazione non e' quella sui pattern: e' quella che va a cercare **se ne esiste un secondo** (D135). Deciso con Gio che la stessa regola vale anche per il `config.json` di NOVA, e **solo** per le due liste delle guardie: ovunque altro il salvato vince, ed e' giusto perche' e' roba dell'utente; li' no, perche' e' l'unica lista che **cresce**, e una lista che cresce piu' un file che vince danno un elenco congelato al giorno in cui e' stato salvato. Cio' che viene rimesso finisce in `avvio.log`: aggiungere qualcosa alla configurazione di qualcuno senza dirlo e' l'altro modo di sbagliare |
| D186 | Ogni elenco dichiarato in Rust deve essere **generato**, **gemellato** con quello Python e confrontato voce per voce, oppure **dichiarato senza gemello con scritto perche'** — e a controllarlo e' una prova che li conta tutti | La lezione di D185 non era «ho sbagliato quell'elenco»: era che **nessuno li stava confrontando**. Contati, gli elenchi dichiarati nei crate erano ventuno, e nove non avevano nessuno che li guardasse: le variabili d'ambiente delle cartelle sincronizzate, le estensioni delle immagini, i segnaposto del prompt, i segni di limite d'uso, le parole pesanti dell'MCP, i sei modi in cui llama.cpp dice «non ci sta in memoria», i tipi generici e i prefissi del vault, gli strumenti trasparenti alla ripetizione. Tutti e nove coincidevano ancora — ma coincidevano per fortuna, non per costruzione. Il valore della prova non e' il confronto di oggi: e' che un elenco **nuovo** non puo' entrare senza dichiarare da che parte sta, e che le forme diverse (un insieme di qua e una lista di la', un'alternanza di espressione regolare contro un elenco, le estensioni col punto e senza) sono scritte accanto al confronto invece di essere sottintese. Due deleghe si sono rivelate false appena scritte: `GIORNI` diceva di essere confrontato da un banco che invece lo confrontava con **una copia ricopiata dentro la prova stessa**, e il vero gemello era una variabile dentro una funzione — dove un elenco si nasconde meglio, perche' non lo si trova cercando le maiuscole |
| D187 | Due procedure che sono la stessa cosa scritta due volte si **fondono**, con una soglia molto piu' alta di quella con cui si propongono — e l'archivio torna nell'ordine in cui stava | Il difetto si vedeva nei numeri: ventotto procedure archiviate e solo quattro usate piu' di una volta, perche' «Controllo posta Gmail» e «Controllo ultime email Gmail» si dividevano il contatore. Divise, nessuna delle due arriva alle tre volte che fanno scattare il suggerimento dell'automazione: il gradino successivo non si presenta mai, e non c'e' niente da guardare per capire perche'. La soglia e' 0,75 contro lo 0,30 con cui si sceglie **cosa mostrare**, e la differenza non e' di taratura ma di verso: li' una candidata di troppo costa qualche centinaio di token, qui una fusione sbagliata perde una procedura per sempre. Chi resta e' la piu' usata; a parita', la piu' recente; i passi tenuti sono quelli dell'**ultima riuscita**, per la stessa ragione per cui una registrazione butta i vecchi — se la strada e' cambiata, quella buona e' l'ultima. E alla fine l'archivio torna nell'ordine in cui stava: non e' pulizia, e' che `unisci` gira a ogni registrazione e chi viene buttato quando si supera il tetto e' **chi sta in coda**, quindi un elenco che si rimescola da solo cambia in silenzio anche quello |
| D188 | La mappa dei dati **chiede a ogni modulo dove scrive**, invece di rifare il conto — e una prova va a cercare i posti che la mappa non nomina | «Dove stanno le mie cose» e' la domanda che si fa chi vuole salvarsi i dati, portarli via o cancellarli, ed e' l'unica a cui una risposta incompleta e' peggio di nessuna risposta: **sembra** completa. La mappa era scritta a mano, e aveva tutti e due i modi di sbagliare insieme. Prometteva `pianificate.json`, che nessuno scriveva: siccome l'elenco mostra solo cio' che esiste, quella voce **spariva**, e chi chiedeva non sentiva parlare delle cose che NOVA fa da sola. E taceva su sei posti veri, fra cui i due che contano di piu' — il **profilo del browser**, dove stanno i cookie e le sessioni aperte dei siti su cui NOVA lavora per te, e la cartella delle **schermate**, che sono fotografie di cio' che avevi sullo schermo. Ora ogni voce la dice il modulo che quel file lo scrive: `registro.percorso()`, `ricette._percorso()`, `guasti.percorso_guasti()`, e cosi' via. Un percorso scritto due volte e' un percorso che prima o poi diverge — e fuori da Windows le due copie erano gia' divergenti in tre punti, invisibili perche' su Windows coincidono per caso. Le due che restano scritte a mano sono dichiarate col perche': `segreti.dat` lo scrive il demone in Rust, `procedure.log` non passa da nessuna funzione. La prova non guarda l'elenco: legge il **codice**, cerca ogni percorso che NOVA si costruisce dentro la propria cartella, e pretende che la mappa lo copra o che qualcuno abbia scritto perche' no (D148). Stessa storia una funzione piu' in la': «dove stanno i miei dati» aveva **due** risposte, 4,28 GB per chi lo chiedeva a NOVA e 19,93 per chi disinstallava, perche' il rendiconto in JSON contava il modello scaricato e il racconto no. Quindici gigabyte e mezzo di differenza sulla stessa domanda, trovati non leggendo il codice ma **facendo le due domande e confrontando le risposte** |
| D189 | **Ogni nome di strumento scritto da qualche parte deve esistere**, e una prova lo verifica contro gli elenchi veri: quelli del server MCP di NOVA, quelli del demone, e i nativi dichiarati a mano col perche' | E' la classe di difetti piu' silenziosa che NOVA abbia avuto, ed e' costata due volte. Un nome sbagliato in un elenco di permessi o dentro il prompt non da' **nessun** errore: da' un cervello a cui manca una capacita' e che non sa dire perche'. Senza `Read` fra i permessi NOVA scattava schermate che non poteva guardare — `Read` e' anche cio' che apre le immagini, e il file finiva su disco senza che il modello lo vedesse mai. E le capacita' del demone si chiamano col punto dentro (`ui.find`) e col trattino basso una volta esposte via MCP (`ui_find`): cercarle col punto non trova niente, e il prompt lo dice apposta — ma «lo dice» non e' «e' vero», e adesso e' controllato. Le tre direzioni sono tutte e tre difetti: un permesso che nomina uno strumento inesistente, un prompt che insegna uno strumento inesistente, e un prompt che insegna uno strumento **vietato** — quest'ultimo peggio degli altri due, perche' il modello ci prova, si vede rifiutare, e non ha modo di capire che il problema non e' la sua richiesta. I nativi di Claude Code sono l'unico posto del progetto in cui dei nomi di strumento sono ricopiati a mano: non sono nostri, non c'e' nessun elenco da cui leggerli, e la prova lo dichiara |
| D190 | Leggere la risposta di un fornitore e' una **decisione**, non un parsing, perche' sbagliarla non da' un errore: da' una risposta **vuota** | E per chi guarda NOVA una risposta vuota e' indistinguibile da un modello che non ha saputo rispondere — quindi il difetto non si scopre mai, si attribuisce al modello. I punti dove si sbaglia sono tutti piccoli: la lista delle scelte puo' essere **vuota** (e «la prima di zero» in Python non e' un errore, e' `[{}]` per via di un `or` messo apposta); il ragionamento arriva con **due nomi diversi** — `reasoning_content` e `reasoning`, perche' i fornitori non si sono messi d'accordo — e il primo vince, ma solo se non e' la stringa vuota, dato che in Python `"" or x` da' `x`; il ragionamento puo' anche stare **dentro** il testo, in un `<think>` che il modello a volte non chiude perche' si e' interrotto (D172); e i token contati arrivano come stringa o con la virgola. Su quest'ultimo c'e' una differenza **voluta e dichiarata**: `int("abc")` in Python solleva e fa perdere il turno intero, qui vale zero. Diciotto corpi di risposta confrontati col `chat` vero, chiamato con la rete sostituita; cinque mutazioni, cinque rosse |
| D191 | Il giro dei tentativi sta **dietro un tratto**, cosi' la politica si prova senza accendere niente — e dopo l'ultimo tentativo non si aspetta | Un giro di tentativi che si puo' provare solo con un server acceso e' un giro che nessuno prova, e quello che c'e' dentro non e' plumbing: e' la differenza fra «non ha funzionato» e «non adesso». Quota finita non e' un errore del compito — detto cosi', il router mette in pausa quel gradino e ripiega su un altro fornitore; detto come errore qualunque, il ripiego non parte mai e all'utente finisce sotto gli occhi il JSON del fornitore. Una richiesta sbagliata (400) invece non si riprova: rimandarla uguale tre volte non la raddrizza. E il silenzio si racconta in due modi, perche' le cure sono due: un server in casa che non risponde di solito e' spento, uno su internet o e' giu' lui o non c'e' rete. Il difetto trovato portandolo: si aspettava anche **dopo l'ultimo** tentativo — col modello locale spento erano quindici secondi di attese su ogni domanda prima del messaggio che dice di riaccenderlo, e la risposta era gia' decisa dal primo. Corretto in tutti e due, perche' il banco pretende che siano uguali; ed e' il banco ad avermi preso, non il contrario: l'avevo tolto dal Rust e non dal Python, che e' migliorare invece di portare |
| D192 | Aprire gli appunti di Windows **si riprova**: sono del sistema, non nostri | Chiunque stia copiando qualcosa in quell'istante li tiene in mano per qualche millesimo di secondo, e `OpenClipboard` in quel millesimo fallisce. Provare una volta sola vuol dire che ogni tanto NOVA risponde «gli appunti sono occupati» per un'operazione che sarebbe riuscita dieci millisecondi dopo — e chi guarda vede un difetto di NOVA, non una coda. Dieci tentativi a dieci millesimi: un decimo di secondo in tutto, che nessuno percepisce; oltre non ha senso, perche' se qualcuno li tiene piu' a lungo sta facendo qualcosa e non e' una coda. La politica sta **fuori** dal codice che tocca Windows — `pausa_apertura(tentativo)`, che torna `None` dopo l'ultimo — perche' e' l'unica parte che si puo' provare senza avere degli appunti occupati, e senza «dopo l'ultimo non si aspetta» sarebbe una frase invece che una prova. Trovato non guardando il codice ma inseguendo una prova che era rossa **solo insieme alle altre**: il difetto non era della prova, era di NOVA |
| D193 | Un messaggio che dice che **manca** qualcosa deve dire anche **cosa fare** — un comando, un posto dove indicare il percorso, o almeno cosa continua a funzionare | La regola c'era gia' come consuetudine, ed era buona: «manca sounddevice: si installa con pip install sounddevice», «Claude Code non trovato. Installalo con: npm install -g …», «Non trovo l'orb: se hai installato con install.ps1 dovrebbe stare in bin\». Ma una consuetudine vale solo dove qualcuno l'ha applicata, e i tre messaggi che non la seguivano erano **esattamente** quelli che su questa macchina non legge nessuno: niente Edge ne' Chrome, niente llama-server, niente file GGUF. Sono i primi tre che incontra chi installa NOVA adesso — cioe' la prima delle cinque frasi del cancello della beta. La terza forma della cura, «cosa continua a funzionare», conta quanto un comando: a chi ha appena installato qualcosa, sapere che non e' rotta e' meta' della risposta, e in tutti e tre i casi NOVA funziona lo stesso perche' il cervello locale e il browser sono **uno** dei modi, non l'unico. La prova tiene un elenco dichiarato di cio' che NOVA chiede al mondo fuori — quindici voci, ognuna con cosa succede senza — e pretende tre cose: che ogni assenza abbia una cura, che ogni voce dichiarata compaia in almeno un messaggio (un pezzo che nessuno nomina piu' e' un elenco che invecchia), e che le poche deroghe abbiano scritto il perche' |
| D194 | **Nessun diario di NOVA cresce per sempre**, e la regola che lo impedisce sta in un posto solo: `nova/rotazione.py` | Misurato sul PC di chi lo usa tutti i giorni: `avvio.log` a 2,8 MB e 13.186 righe, trenta volte il file successivo. Non e' un problema di spazio — sono megabyte, non gigabyte — e' che quel diario esiste **apposta** per essere aperto il giorno che qualcosa non parte, e a tredicimila righe non lo apre piu' nessuno. Dentro c'erano due difetti che non si curano a vicenda: 8.812 righe distinte su 13.186 — una arrivava a ripetersi **diciassette volte dentro lo stesso processo** — e nessun tetto. Accorpare non mette un tetto; il tetto non toglie il rumore. Percio' le regole sono tre: due megabyte e **un** solo storico (due sono gia' un archivio che nessuno legge); si pota **prima** di scrivere, perche' su un file che si apre per capire cos'e' successo l'ultima riga e' quella che conta; e una riga uguale alla precedente **dello stesso processo** non si riscrive, mentre fra processi diversi si' — due processi che dicono la stessa cosa sono due fatti, sono partiti tutti e due. Ma la cosa che ho imparato qui non e' la regola: e' che **credevo esistesse in un posto solo** e ne ho scritta la seconda. La prova, scritta per obbligare gli altri undici scrittori a passarci, ha risposto che i posti erano **quattro**, ognuno con la sua idea: due megabyte veri nel vault, 2.000.000 di byte nel registro delle azioni (due megabyte da fruttivendolo, novantasettemila byte di differenza che nessuno avrebbe mai notato), mezzo megabyte nei guasti con lo storico **buttato via** — teneva le ultime duecento righe e cancellava il resto — e un quarto in `kb_setup`. E il file vecchio si chiamava in **tre** modi: `.jsonl.1`, `.1.jsonl`, `.1.log`. Nessuno sbagliato da solo; sbagliati insieme, perche' erano quattro risposte a una domanda sola (D73). Il nome giusto e' quello che teneva il vault, `audit.1.jsonl`, e l'ha stabilito la sua prova diventando rossa: con l'estensione in fondo il file storico si apre ancora con cio' che apre gli altri, e su Windows l'estensione **e'** il programma. Avevo scelto l'altra grafia per abitudine, ed e' un caso in cui la prova gia' scritta sapeva piu' di me. Chi resta fuori lo dichiara col perche' (D148), e sono tre: la raccolta che l'utente accende apposta per guardarci dentro, il diario di una sessione che finisce da se', e il file **dell'utente** scritto perche' l'ha chiesto lui — li' NOVA non pota niente in silenzio. La prova cerca anche la seconda copia della regola (D135), in tutte e tre le forme in cui si scrive a mano: sposta, riscrive, tronca. Tredici mutazioni, tredici rosse; due sono sopravvissute al primo giro e avevano ragione loro — provavo l'ordine fra potatura e scrittura su un file di centosessanta byte, dove nessuno dei due ordini pota, e provavo l'accorpamento «per file» con due righe **diverse**. Il difetto vero uscito da tutto questo non e' il tetto: e' che il registro delle azioni si potava gia', e `leggi` guardava **solo il file vivo**. `cerca` esiste per «cosa ho mandato a quella societa'?» tre settimane dopo: il giorno della potatura quella domanda avrebbe cominciato a rispondere «niente», senza errori e senza righe di log. Ora si legge anche lo storico, e prima di quello vivo — l'ordine e' il tempo. In Rust la stessa regola c'era gia' in `nova-nodi`, con lo stesso tetto e la stessa grafia: nessuno la stava confrontando, e adesso si' (D186) |
| D195 | Il campo del modello **dice quali modelli ci sono**, non solo dove scriverne il percorso | Diceva la verita' ed era inutile: «qui si punta a un file che hai gia'», sopra una casella di testo vuota. Chi non ricorda dove sta il suo GGUF non ha nessun posto dove guardare, e chi ci scrive un percorso sbagliato non lo scopre li' — lo scopre mezzo minuto dopo, sotto forma di llama.cpp che muore. Non c'era niente da inventare: `nova/modelli_trova.py` sa gia' cercare i GGUF nei posti dove finiscono davvero, ordinarli dal piu' adatto, dire quanti GB pesano e se hanno accanto il proiettore che da' la vista; e `verifica_file` sa gia' distinguere «non esiste» da «non e' un GGUF» da «e' un GGUF ma non e' finito di scaricare» — che e' il caso cattivo, perche' il file c'e' e guardando la cartella sembra a posto. Lo chiamavano solo l'installer e la riga di comando. **La capacita' c'era e chi ne aveva bisogno non la vedeva**: e' la stessa forma di D188 e D193, e si trova solo guardando cosa NOVA mostra a chi non l'ha appena installata. Il guscio non ripete nessuna di quelle regole, apre la porta e basta (`nova-shell/src/modelli.rs`, sul modello di `componenti.rs`). La ricerca vera costa una ventina di secondi, percio' sono due bottoni e non un'attesa piu' lunga di nascosto: i posti soliti subito, i dischi fissi a richiesta — e se il tempo scade l'elenco dice di essere parziale, perche' un elenco parziale che sembra completo fa concludere «il mio modello non c'e'», che e' la conclusione sbagliata. La risposta si tiene in memoria finche' la finestra resta aperta, ma non su disco: un modello cancellato deve sparire dall'elenco alla riapertura, non restare li' a farsi scegliere |
| D196 | **Ogni `invoke` della pagina deve corrispondere a un comando che esiste**, e ogni comando registrato dev'essere chiamato o dichiarato — argomenti compresi | E' D189 un piano piu' in la': la' erano nomi di strumenti scritti a mano, qui sono nomi di comandi, e sbagliarli non da' **nessun** errore. Da' un bottone che si preme e non fa niente, che e' il difetto piu' difficile da attribuire: sembra rotto il programma, non il collegamento. Vale in tutte e tre le direzioni, e tutte e tre sono difetti veri — un nome che non esiste, un argomento che la funzione non ha (Tauri accetta il camelCase per i parametri in snake_case, quindi il confronto va fatto sulle due forme), e un comando registrato che non chiama piu' nessuno, che o e' codice morto o e' una funzione persa per strada. Chi resta senza chiamate lo dichiara col perche', e i due motivi buoni sono molto diversi: «lo chiama il Rust» (`mostra_chat`, da `bus.rs`) e «non lo chiama ancora nessuno, e lo sappiamo» — `stato_orb`, il cui commento dice «la chiamera' il demone quando NOVA pensa, ascolta o parla», e finche' resta cosi' l'orb non cambia aspetto: un lavoro a meta' che adesso e' scritto in un posto dove si vede. La lezione vera pero' e' arrivata dal cercatore: la prima versione chiedeva `invoke(` e le pagine scrivono `invoke?.(`, quindi dichiarava morti cinque comandi vivissimi. **Un controllo che accusa il falso e' un controllo che si smette di leggere**, percio' ora conta tutte le occorrenze della parola e pretende di averle riconosciute tutte: una forma nuova diventa rossa qui invece di sparire in silenzio. Sei mutazioni, sei rosse |
| D197 | **Il guscio chiede ai crate, non all'interprete**: se una risposta e' gia' portata in Rust, il pannello non lancia Python per averla | `componenti.rs` lancia `python -m nova.componenti` ed e' giusto — cosa serve e da dove si scarica lo sa quel modulo, e l'installer legge lo stesso. Ma «quali GGUF ci sono» e «Claude Code e' collegato» erano gia' in `nova-modelli::trova` e in `nova-cervelli`, portate e gemellate da un banco: passare dall'interprete per averle vuol dire far partire un processo Python a ogni apertura di pannello, e soprattutto **tenere in vita la copia che il cantiere sta chiudendo**. La regola che ne esce e' piu' stretta di D99: la domanda non e' «esiste gia' qualcosa che fa questo?», e' «esiste gia' qualcosa che fa questo **dalla parte in cui sto lavorando**?». Portarlo davvero ha fatto emergere due funzioni non ancora portate — trovare Claude nel PATH (su Windows npm installa `claude.cmd`, e cercare l'`.exe` per primo vuol dire non trovarlo dove c'e') e leggere che tipo di abbonamento e'. Quest'ultima ha tre trappole di porto tutte silenziose: `x or ""` di Python e' falso anche per `0` e `false`, quindi un `subscriptionType` a zero diventa «sconosciuto» e non «abbonamento 0»; `.strip()` toglie anche i separatori di unita' che `char::is_whitespace` non considera bianchi (D182); e `.replace()` toglie il prefisso **ovunque**, non solo in testa — quest'ultima l'ha trovata una mutazione sopravvissuta, perche' il caso che avevo scritto aveva il prefisso ripetuto all'inizio, dove `trim_start_matches` fa la stessa cosa. Il confine resta quello di sempre: la logica sta nel crate e si prova senza una macchina vera, l'occhiata al PATH e al file delle credenziali sta nel guscio. E il collegamento all'account NOVA non lo fa: apre un terminale e si fa da parte, perche' mettersi in mezzo a una password e' proprio il posto in cui ha promesso di non stare |
| D198 | **Ogni chiave che il pannello scrive deve esistere in `config.py`**, e una prova la confronta con le classi vere | Il campo «Modello» salvava `model.path`. Quel campo non esiste: `ModelConfig` tiene i parametri di generazione, il file del modello sta in `server.model_path` perche' e' cio' che si passa a llama-server. Scegliere un modello dal pannello **non ha mai funzionato**, e nessuno poteva accorgersene guardando: il guscio fondeva la chiave nel JSON, il file per qualche secondo la conteneva davvero, e poi il primo comando di NOVA la buttava via. Nessun errore, nessuna riga di log. E' D189 e D196 un piano piu' in la' — la' nomi di strumenti e di comandi, qui nomi di **campi di configurazione**: sempre una stringa che nessuno confronta con l'altra parte. La prova estrae le sezioni e i campi dalle classi (D112, non da una copia scritta a mano) e pretende che ogni chiave letta o scritta dalle pagine esista. In piu' il nome della chiave sta ora in **un posto solo**: era ripetuto in sette punti, ed e' anche per questo che nessuno l'aveva mai confrontato. Sei mutazioni, sei rosse, e la prima e' il difetto vero rimesso com'era Sei mutazioni, sei rosse, e la prima e' il difetto vero rimesso com'era. **Poi e' successo di nuovo, dall'altra parte**: la fascia di stato la scrive il Rust del guscio, e li' `model.path` era rimasto — il pannello diceva «nessun modello scelto» due righe sopra la riga che quel modello lo nominava, con tanto di percorso. La prova non l'aveva preso perche' cercava solo in `ui/*.html` e `ui/*.js`: una rete tesa dove il pesce era gia' passato. D135 dice che cio' che tiene una correzione e' che non ci sia un secondo posto, e io l'avevo applicata ai **nomi** — la chiave adesso e' scritta una volta sola — senza applicarla ai **lettori**: due programmi che leggono lo stesso file sono due posti, sempre. Ora la prova guarda anche le letture del guscio e pretende che le due parti peschino dallo stesso campo. E per farlo si raccolgono solo le letture che hanno `cfg` come ricevente: la prima versione prendeva qualunque coppia di stringhe del Rust e, per non accusare il falso, saltava le sezioni che non riconosceva — cioe' proprio i casi in cui la sezione e' sbagliata. Un cercatore impreciso si paga sempre in controlli disattivati |
| D199 | **Un comando che legge non riscrive la configurazione** | Il guscio chiede le statistiche della memoria ogni quindici secondi, e ogni richiesta passava da `_prepare_config`, che salvava **sempre**: quattro riscritture al minuto di un file che contiene anche le chiavi API, per ore, senza che nessuno avesse chiesto di cambiare niente. Un file riscritto identico non si vede, ed e' per questo che e' durato: l'unica traccia era l'identita' del file che cambiava a ogni giro in `avvio.log`. Il danno pero' non e' l'usura. `save()` scrive `asdict(self)`, cioe' **solo i campi che le classi conoscono**: ogni chiave estranea sparisce alla prima riscrittura. Sono due difetti che da soli non si vedevano — una chiave sbagliata (D198) e una riscrittura periodica — e insieme facevano una funzione che non funziona senza dare mai un errore. Ora si salva solo se l'autoconfigurazione ha davvero cambiato qualcosa, e il confronto e' su **tutta** la configurazione invece che su un elenco di campi, perche' un elenco scritto a mano non saprebbe del campo che `autoconfigure` imparera' a toccare domani. La prova mette nel file una chiave che nessuna classe conosce e guarda se sopravvive a una lettura |
| D200 | **Un turno che comincia lascia una riga, e una quando finisce** — senza il contenuto della domanda | NOVA ha lavorato due ore su una sola domanda (un modello da 27 miliardi di parametri a 3,3 token al secondo, con il contesto che si riempiva) e in quelle due ore non ha scritto **niente** da nessuna parte. Chi guardava vedeva un programma fermo, e non aveva modo di distinguerlo da un programma piantato: sono due cose diverse e vogliono due reazioni diverse. Cosa e' stato chiesto **non** entra, e non e' una dimenticanza: `avvio.log` sta accanto al programma e per D52 puo' finire sincronizzato col cloud; per capire un blocco servono l'ora, il cervello e la durata, mentre la domanda sarebbe l'unica cosa che da li' non si puo' piu' togliere. Il controllo che tiene ferma la riparazione non e' quello sulle righe: e' quello sugli **attributi**. Tutta la diagnostica sta dentro `except Exception: pass`, e li' dentro un attributo che non esiste non da' errore — fa sparire la riga e basta. Scrivendo questa traccia avevo usato `self.brain_name`, che non esiste: sarebbe stata una diagnostica che tace, cioe' peggio di nessuna diagnostica, perche' la si crede accesa. Ora una prova pretende che ogni `self.x` letto in `Agent` sia assegnato da qualche parte |
| D201 | **Se un cervello funziona glielo si chiede, non lo si deduce** — e di cio' che risponde si mostra la riga vera, non una categoria inventata | Il pannello diceva «pronto» quando il binario esisteva nel PATH. Su questa macchina `gemini` c'e', ha pure `~/.gemini/oauth_creds.json` — quindi anche un controllo sui file delle credenziali avrebbe detto «collegato» — e al primo messaggio risponde `You do not have a valid license of this product`. **Autenticato non vuol dire utilizzabile**, e nessuna euristica sul disco puo' saperlo. Percio' si fa la domanda piu' corta possibile e si guarda: e' l'unica verifica che non mente, perche' e' esattamente cio' che succedera' al primo messaggio vero. Misurato: cinque secondi. La parte delicata non e' lanciare il processo, e' **quale riga far leggere**: lo stderr di una CLI in Node contiene l'avviso sui 256 colori, la frase che conta, e venti righe di stack con dentro percorsi di file — mostrare la prima vuol dire mostrare un avviso, mostrarle tutte vuol dire mostrare uno stack. Quella scelta sta in `nova-guasti::prova`, ed e' provata **sullo stderr vero misurato quel giorno**, non su un esempio inventato. Cio' che NOVA aggiunge di suo e' solo il verdetto che puo' dimostrare — ha risposto, non ha risposto, non e' partito, non ha fatto in tempo — e le chiavi non escono comunque, perche' un messaggio d'errore porta spessissimo il valore che l'ha causato. Stessa regola al primo avvio: la chat non deduce piu' dalla configurazione se c'e' un cervello, lo **chiede**, e se non e' pronto dice il motivo vero invece di una frase generica. Guardare la configurazione bastava per «non ho scelto niente» e diceva «tutto a posto» a chi aveva scelto una CLI senza essersi collegato: cioe' proprio chi installa NOVA adesso, che si vedeva offrire tre prove destinate a fallire in fila |
| D202 | Una CLI predefinita che **smette di valere per qualcuno** non si toglie: si rietichetta, e accanto entra quella che la sostituisce | Il 18 giugno 2026 Gemini CLI ha smesso di servire gli account individuali — Google AI Pro, AI Ultra, piano gratuito — e Google ha indicato Antigravity CLI come la strada. NOVA ha continuato a proporre una voce chiamata «Gemini» per tre mesi: per chi ha un abbonamento personale era un cervello che non poteva funzionare, e il messaggio che riceveva era per giunta quello sbagliato, perche' un `GOOGLE_CLOUD_PROJECT` lasciato in `~/.gemini/.env` spingeva la CLI sul percorso enterprise e faceva rispondere «non hai una licenza valida, contatta il tuo amministratore». Due errori sovrapposti, e quello visibile era il meno informativo. Togliere `gemini` sarebbe stato sbagliato quanto lasciarla com'era: per gli utenti enterprise con licenza Code Assist e per chi usa una chiave API a pagamento quella strada e' aperta. Resta, e l'etichetta dice per chi vale — perche' **l'etichetta e' cio' che si legge prima di scegliere**, e «Gemini» e basta e' il nome che cerca proprio chi ha l'abbonamento personale, cioe' l'unico a cui non va. Nessuna prova puo' sapere che un fornitore ha chiuso una porta: quel fatto sta fuori. Cio' che si controlla e' la forma della dichiarazione (da dove prende la domanda, il tetto di tempo, se fa spendere) e che una voce buona solo per alcuni lo scriva nel nome. Il resto lo dice `cervello_prova` (D201), che su questa macchina ha diagnosticato tutto in cinque secondi e ha perfino **cambiato risposta** quando si e' tolta la variabile: da «licenza mancante» a «questo client non e' piu' supportato per gli individui» |
| D203 | «Non trovato nel PATH» deve dire **anche** «se l'hai appena installato, riavvia NOVA» | Un processo eredita le variabili d'ambiente da chi l'ha avviato: una CLI installata mentre NOVA gira resta invisibile finche' NOVA non riparte. Il messaggio diceva «Installalo», che in quel caso e' la cura sbagliata — manda a reinstallare una cosa che c'e' gia', e chi prova due volte conclude che NOVA e' rotta. Capitato davvero: Antigravity CLI installata alle 13:34 con NOVA in piedi dalle 13:14, e per NOVA `agy` non esisteva. Il caso normale resta il piu' probabile, quindi «installalo» non si toglie: si mettono tutte e due le cure, nell'ordine in cui conviene provarle (D193). Ma la cosa che ha permesso alla frase di essere sbagliata e' un'altra: **il banco non la confrontava**. `perche_non_pronto` esisteva in Rust e in Python e nessuno li metteva uno accanto all'altro — due posti, una domanda, e la deriva e' solo questione di tempo (D73). Adesso il banco confronta anche quella frase, coi nomi che l'utente puo' scegliersi, virgolette comprese |
| D204 | **L'installatore non tiene copie di elenchi: li chiede a NOVA** | Aveva l'elenco delle CLI scritto a mano in PowerShell, e il commento sopra diceva perfino dove stava quello vero (`cli_predefinite()`). Era gia' divergente: Antigravity mancava e Gemini aveva l'etichetta di prima del 18 giugno. Lo stesso file pero' fa gia' la cosa giusta tre volte — la ricerca dei GGUF, il verdetto sul modello, l'avvertenza sulle cartelle sincronizzate: tutte chiedono a Python «perche' due copie della stessa regola sono due regole destinate a divergere». Mancava l'uniformita'. E' il posto peggiore in cui tenere una lista vecchia, perche' l'installatore e' l'unico programma che vede chi non ha ancora niente. Claude Code resta scritto a mano, dichiarato col perche': non e' fra le CLI predefinite perche' ha un modulo suo — sessioni, permessi, MCP — e non una voce in `brains.cli`. Senza Python si offre solo quello e **lo si dice**: meglio proporre meno che proporre una lista inventata li' dentro |
| D205 | **La via breve si offre, non si nasconde in un parametro** | Ventitre' domande sono tante per chi voleva solo provare NOVA. Una via breve c'era gia' — `-Silenzioso` fa rispondere a ogni domanda il suo predefinito — ma viveva in un parametro da riga di comando: chi fa doppio clic non la vede, e chi la vede non la riconosce, perche' «silenzioso» dice **come** si comporta e non **cosa** ottiene. Ora e' la prima domanda, con le parole della cosa che fa. Non e' una modalita' nuova — sarebbe stato un secondo comportamento da tenere allineato a mano (D73) — e' la stessa resa visibile. Ed e' sicura **per costruzione**, non per promessa: la scelta di base sull'IA e' «nessuna per ora» su una macchina senza modelli e «usa quello che hai» su una che ne ha, quindi da li' non si arriva mai a scaricare gigabyte. Allo scaricamento ci si arriva solo scegliendolo, ed e' una prova a guardarlo: se quel predefinito cambiasse, «fai tu» comincerebbe a scaricare al buio |
| D206 | **Un solo nome utente finto in tutto il progetto: `utente`** | Il controllo sui dati personali segnalava qualunque percorso dentro `C:\Users\` scritto con un nome in minuscola, cioe' anche i nomi inventati degli esempi: `gio`, `utente`, quello che capitava. Chi lo vedeva rosso su un esempio imparava una cosa sola - aggiungere un'esenzione - e l'esenzione successiva avrebbe coperto anche una fuga vera. Con un nome solo non c'e' da giudicare se un nome e' inventato: o e' `utente`, o e' di qualcuno |
| D207 | **La configurazione nasce anche quando non c'e' niente da completare** | Da quando leggere non riscrive piu' il file (D199), il salvataggio avveniva solo se `autoconfigure` aveva cambiato qualcosa. Su questa macchina cambia sempre qualcosa - c'e' un modello, c'e' un runtime da trovare. Su una macchina spoglia no: NOVA girava senza mai scriversi una configurazione, e chi voleva correggerla a mano non trovava il file. Il confronto dice se e' **cambiata**, non se **esiste**: servono tutte e due |
| D208 | **Un'esenzione che non copre piu' niente si toglie, e lo dice una prova** | Le esenzioni di un controllo si ereditano: restano scritte quando il motivo e' passato, e allora coprono in silenzio il file che prendera' quel nome domani. La prova dei dati personali controlla anche le proprie esenzioni - se il file non c'e' piu', o non contiene piu' cio' per cui era esente, e' rossa. Alla prima esecuzione ne ha trovata una cosi', ereditata dal vecchio controllo nel file della CI |
| D209 | **Mac e Linux si cominciano dalla CI, non dal codice** | Il nucleo Rust si compila gia' altrove e ogni modulo Windows ha gia' il suo gemello che dice onestamente cosa non sa fare: la parita' e' riempire tredici caselle, non ricominciare. Ma scrivere quel codice senza una macchina che lo guardi e' esattamente cio' che ha lasciato per mesi un filtro che non filtrava e una configurazione che su una macchina spoglia non nasceva. Prima un lavoro su `macos-latest` e `ubuntu-latest`, poi il codice: la luce prima delle mani |
| D210 | **I fogli di calcolo sono file, non l'applicazione Excel** | Leggerli e scriverli da se' funziona su un PC senza Office, su Mac e su Linux, e si prova su un agente spoglio; pilotare l'Excel aperto sullo schermo funziona solo dove c'e' Excel, e si prova solo a mano. Il ponte COM resta possibile **dopo** e dichiaratamente solo per Windows - ma la base e' il file, se no la funzione piu' richiesta nasce gia' legata all'unico sistema da cui stiamo cercando di uscire |
| D211 | **Chi legge una cella dice se guarda il risultato o la formula** | Oggi si apre con `data_only=True`, cioe' si chiede il risultato. Su un file salvato da Excel e' la scelta giusta; su un file scritto da un programma e mai aperto da Excel quel risultato non esiste, e la cella torna **vuota**: misurato, `=A1+A2` diventa `None`. NOVA non sbaglia il numero, nega che ci sia - che e' il tipo di errore peggiore. Si leggono tutti e due e si dice quale si sta mostrando |
| D212 | **Un banco gemello dimostra che due parti sono uguali, non che hanno ragione** | Nella tabella delle cartelle sincronizzate c'e' `icloakdrive`, refuso per `iclouddrive`: una riga che non puo' corrispondere a niente. Sta identica in Python e in Rust - portata fedelmente, refuso compreso - e il banco che confronta i due elenchi li trova d'accordo. E' d'accordo nello sbagliare. Il banco protegge dalla **divergenza**, che e' il guasto che nasce dopo; contro un errore che c'era gia' prima del porto non puo' niente, e va cercato dove il codice incontra il mondo vero |
| D213 | **Si scende di un gradino solo se la memoria c'entra, o se il processo e' morto senza dire niente** | La scala dei layer cura un guasto solo: la memoria video che non basta. La regola scritta era `if not OOM(err) and not auto_tune: break`, cioe' si usciva dal giro solo se **tutte e due** erano vere - e con `auto_tune` acceso, che e' il valore di fabbrica, un flag rifiutato o un GGUF rotto facevano percorrere tutta la scala scrivendo «Memoria insufficiente» a ogni gradino. Una frase mai verificata, ripetuta sei volte, che manda a cercare dove non c'e' niente. Adesso: se ha parlato di memoria si scende; se ha parlato d'altro ci si ferma e si riporta cosa ha detto; se non ha detto niente si scende lo stesso, perche' una memoria video che finisce di colpo fa chiudere il processo dal sistema senza lasciargli il tempo di scrivere - e uno «scaduto» muto e' il caso della memoria condivisa, che non da' errori, accetta tutto e va dieci volte piu' piano |
| D214 | **La prima attesa e' lunga, le altre no** | `startup_timeout` vale 600 secondi, ed e' giusto per il primo tentativo: un modello da decine di gigabyte su un disco lento con la cache di sistema fredda ci mette davvero tanto. Moltiplicato per i gradini della scala pero' fa un'ora di silenzio prima che NOVA dica che non ce l'ha fatta. Dal secondo tentativo in poi quel file il sistema ce l'ha gia' in mano: se non risponde entro due minuti non e' lentezza, e' un guasto |
| D215 | **Chi prova e' separato da chi decide, e il giro sta nel demone** | Il giro di accensione — scendi un gradino, riprova, arrenditi — vive sopra a un tratto (`Tentativo`) che e' l'unica cosa a toccare il mondo. Senza quella cucitura, provare «tre gradini e poi si arrende» vorrebbe dire avere una scheda video che finisce la memoria a comando: si sarebbe provato il giro a mano una volta e mai piu'. Con la cucitura, sette prove deterministiche e sei mutazioni prese, su qualunque sistema. E sta nel demone perche' il demone **possiede** i processi lunghi: prima il giro lo guidava Python — costruisci, avvia, aspetta, rileggi, decidi, riprova — e il demone faceva da braccio senza sapere cosa stesse facendo, quindi chiunque volesse accendere il modello doveva riscrivere tutta la sequenza |
| D216 | **Il giro che conta non e' quello di fila** | Il rilevatore di lavoro inutile guardava una cosa sola: la stessa chiamata, identica, piu' volte di seguito. E' il giro piu' stupido, ed e' l'unico che un modello quasi mai fa. Quello vero alterna - cerca, leggi, cerca, leggi - e li' ogni chiamata e' diversa dalla precedente: la catena si azzerava a ogni passo e il contatore restava a uno **per sempre**. Dodici passi di lavoro inutile, nessun promemoria, e l'utente che guarda NOVA girare. Adesso si guarda anche il **ciclo**: il periodo piu' corto che spiega la coda, fino a quattro chiamate, e si parla al terzo e al quinto giro. Resta un promemoria e mai un divieto, e resta legato agli **argomenti**: `leggi(a) scrivi(a) leggi(b) scrivi(b)` non e' un giro, e' lavoro |
| D217 | **Finire i passi non cancella il lavoro fatto** | Il ciclo ha un tetto - dodici passi - e quando li esauriva diceva «Ho raggiunto il numero massimo di passaggi consentiti» tornando **quella frase al posto di cio' che aveva gia' scritto**. Dodici passi di lavoro - pagine lette, file aperti, pezzi di risposta messi giu' lungo la strada - e all'utente arriva una riga burocratica che non dice nemmeno cosa aveva trovato. E' la stessa forma del taglio dei risultati, gia' curata una volta qui dentro: una perdita silenziosa deve diventare un **rinvio**. Quel che c'e' si consegna, il tetto diventa una nota accanto, e il numero e' quello vero e non una costante nella frase |
| D218 | **Un identificativo non si ricava da qualcosa che puo' tornare indietro** | La coppia sintetizzata quando NOVA delega da sola — l'`assistant` con `tool_calls` piu' il `tool` che risponde — portava `escalation-{len(messages)}`. Dentro un turno bastava: quella lista cresce e basta. Fra un turno e l'altro no: `trim_history` butta cio' che sta subito dopo il messaggio di sistema, la lista si **accorcia**, e il numero torna su uno gia' usato — mentre la coppia che lo portava e' ancora li', perche' il taglio tiene i recenti. Due `tool_call_id` uguali nella stessa trascrizione sono esattamente la trascrizione invalida che quel codice dice di voler evitare, entrata dalla porta di servizio. Un contatore che sale e basta non puo' tornare indietro, perche' non guarda niente |
| D219 | **Un gradino ha una specie, e due delle quattro non sono un indirizzo** | La scala mescola due mondi sotto la stessa parola: con `locale` e `api` si parla in **HTTP** - c'e' un indirizzo, si manda un corpo, arriva una risposta - mentre con `claude` e con le CLI dichiarate si **lancia un processo** e gli si parla su stdin e stdout. Chi costruisce un gradino deve sapere quale dei due sta costruendo, se no ne costruisce uno che non si puo' usare e lo scopre quando qualcuno prova a salire |
| D220 | **I nomi dei cervelli si confrontano senza maiuscole da tutte e due le parti** | Il nome cercato veniva abbassato, le chiavi dichiarate in `brains.cli` no. Una CLI scritta a mano nel file come `"Gemini"` non si trovava, e NOVA - **senza dire niente** - usava il modello locale. Dal pannello non capitava, perche' li' il nome si abbassa mentre lo si scrive; capitava a chi apre `config.json`, che e' una cosa che questo progetto invita a fare. Configurare un cervello e vederne rispondere un altro e' il difetto peggiore di questa famiglia: la risposta arriva lo stesso e sembra giusta |
| D221 | **Il taglio della conversazione non e' un metodo dell'agente** | Erano centocinquanta righe dentro `Agent`, e per provarne una serviva un agente intero con dentro un cervello e una configurazione. Sono il pezzo che decide cosa NOVA dimentica, e i commenti che lo spiegano raccontano quattro difetti gia' pagati: il taglio a token messo dopo l'uscita anticipata (quindi mai eseguito nel solo caso per cui era stato scritto), il fondo mancante (che faceva ritagliare a ogni turno dal trentesimo in poi, rendendo lenta ogni risposta per sempre senza rompere niente), l'accorciatura che si allungava da sola, e la coda che poteva cominciare da una risposta di strumento orfana. Adesso il Python sta in `nova/finestra.py`, si prova senza agente, e `Agent.trim_history` resta dov'era come faccia pubblica. **Non e' una seconda testa**: il gemello in Rust e' `nova-contesto`, che c'era gia', e il banco che li confronta e' `test_contesto_rust.py`, che c'era gia' anche lui — vedi D223 |
| D222 | **`MondoVero` rimette da se' i campi che il taglio non guarda** | `nova_contesto::Messaggio` e' ruolo e testo, e il resto - `tool_calls`, `tool_call_id` - «non lo legge e non lo puo' rovinare»: giusto per lui, ma qualcuno deve rimetterlo. Lo fa `mondo`, e puo' farlo perche' `taglia` toglie **solo dalla testa della coda**: quel che torna e' sempre la prima riga piu' un pezzo finale intero, mai un buco in mezzo, e il `Resoconto` da' i due numeri che servono a ritrovare le posizioni. Quell'assunto pero' e' di un altro crate, quindi i ruoli si ricontrollano riga per riga e una prova lo mette alla frusta su duecentocinquanta forme diverse: se un giorno quella forma cambiasse si vedrebbe li' e subito, invece che in una trascrizione rifiutata dall'API. Si taglia **prima** di chiedere, non dopo il rifiuto: «exceeds the available context size» e' un errore che si previene, non uno da tradurre bene |
| D223 | **Prima di scrivere una funzione si cerca il suo nome in tutto il progetto** | Ho scritto `nova-finestra` da zero - crate, banco, prove, mutazioni - e quel crate esisteva gia' con il nome `nova-contesto`, completo, con il suo banco. Due volte nella stessa giornata, la prima con `versa`/`troncato` che stavano in `nova-strumenti`. La causa e' sempre la stessa: partire da `agent.py`, vedere del Python non portato e concludere «non e' portato» invece di chiedersi se sia portato **altrove**. Un attributo di classe in Python non dice niente su cosa esiste in Rust. La regola e' meccanica, non di giudizio: `grep` del nome della costante prima di scrivere la prima riga. E c'e' un costo che non si vede subito — il mio `nova-finestra` sceglieva l'ultimo massimo dove il Python sceglie il primo, cioe' esattamente la trappola che `nova-contesto` documenta in testa al file di averci gia' sbattuto |
| D224 | **Una prova che confronta due teste non si scrive dentro la terza copia della regola** | `test_strumenti_rust.py` confrontava il Rust con una riscrittura della regola Python messa nel proprio corpo, non con il Python vero. Era l'unica delle tre copie che non poteva accorgersi di niente: `agent.py` poteva cambiare sotto e la prova avrebbe continuato a dire «uguali». Il Python della regola sta in `agent.sostituzione_versata` — pura, fuori dalla classe, col mestiere del file lasciato dov'era — e la prova chiama quella. La stessa domanda vale per ogni banco: **chi sto confrontando davvero?** |
| D225 | **Un nome in maiuscolo sta in un crate solo, o c'e' scritto perche' no** | D223 diceva «cerca prima di scrivere», ed e' un proposito: dipende da me che me ne ricordi, e un'ora dopo averlo scritto non me ne sono ricordato. `test_niente_due_volte.py` non dipende da niente — raccoglie tutte le `pub const` dei crate e pretende che un nome stia in un posto solo, o in una lista di eccezioni **con la ragione scritta**, e una eccezione che non serve piu' fa diventare rossa la prova come una che manca. Provata contro gli errori veri: il crate duplicato di stamattina dava tredici righe rosse al primo `cargo test`, ciascuna con scritto dove stava gia' la cosa che stavo riscrivendo. Le funzioni no, e apposta: `leggi` sta in sei crate e `scrivi` in tre, i verbi si ripetono per natura — un nome in maiuscolo dice di che dominio e', e `MINIMO_ACCORCIABILE` in due posti non e' un caso |
| D226 | **La conversazione non sta dentro il turno** | `MondoVero` teneva i messaggi, la scala e il conto delle deleghe, e moriva con il turno: con quella forma «il secondo turno si ricorda del primo» non era una prova che falliva, era una prova che **non si poteva scrivere**. Adesso c'e' `Sessione`, che tiene le quattro cose che sopravvivono a un turno, e il mondo la prende in prestito. La divisione non e' estetica: dice a chi legge cosa dura quanto, e le tre righe che separano le due cose sono tre difetti gia' visti — il prompt di sistema che si ricompone (e butta la cache del prefisso a ogni turno, rendendo lenta ogni risposta per sempre senza rompere niente), le deleghe che si azzerano (e due deleghe diverse prendono lo stesso identificativo, D218), il gradino che non torna giu' (e una domanda difficile manda fuori casa anche tutte quelle facili che vengono dopo) |
| D227 | **La scala vera si costruisce in un posto solo, e non puo' venire vuota** | `nova_scala::Configurazione` dice **chi** sono i gradini e in che ordine; dove si va a parlare sta in un'altra parte del file di configurazione, e cambia per ragioni sue. `mondo::scala_vera` mette insieme le due meta', ed e' l'unico posto in cui un `mondo::Gradino` nasce: l'ordine lo decide `nova_scala::scala` (non l'ordine delle chiavi, che un programma puo' riordinare — e il pannello lo faceva), con `solo_locale` restano solo i gradini che lo dichiarano (la promessa che niente esce dal PC si mantiene dove la scala si costruisce, non piu' avanti dove qualcuno puo' dimenticarsi di chiederlo), e se non resta niente resta il modello di casa. La chiave dell'API entra **solo** nelle intestazioni del gradino che ne ha bisogno: in casa non se ne manda nessuna, perche' mandarne una vuota e' peggio che non mandarla |
| D228 | **`ora()` non puo' esplodere su una scala vuota** | Faceva `len() - 1` su un vettore che puo' essere vuoto: un panico al primo turno di chi ha configurato male i cervelli. Un panico non e' un messaggio d'errore — e' NOVA che sparisce, senza dire niente a nessuno. Adesso torna `Option`, e chi chiede senza gradini legge «non c'e' nessun cervello configurato a cui chiedere» e non manda niente a nessuno |
| D229 | **Le sezioni della configurazione non si elencano a mano** | `_merge` aveva un dizionario con dentro i sette nomi delle sezioni piu' una riga a parte per `system_prompt`, e `fascicolo` non era ne' nell'uno ne' nell'altra: `save()` lo scriveva, `nova/fascicolo.py` diceva all'utente che il fascicolo «si puo' spostare da `fascicolo` in config.json», e `load()` non lo leggeva mai. Chi lo spostava vedeva NOVA continuare a usare quello vecchio, senza un errore da nessuna parte. Aggiungere `fascicolo` all'elenco non sarebbe stata la cura: era un elenco scritto a mano, e il campo dopo si sarebbe perso allo stesso modo. Adesso i campi si chiedono alla classe, e `test_configurazione_non_perde_niente.py` imposta **tutti** e ottanta a un valore riconoscibile, salva, rilegge e confronta — cosi' non serve ricordarsene (D148) |
| D230 | **I percorsi protetti sono quelli del sistema su cui NOVA gira** | Erano quattro, e cominciavano tutti per `C:\`. Il demone in Rust ne aveva un secondo elenco per Unix, con scritto accanto che era «l'unico senza gemello, perche' NOVA in Python e' di Windows». Non era che su Linux NOVA proteggesse meno: non proteggeva **niente** — `guard_write` scorreva quattro percorsi di cui nessun file Linux sta dentro, e `/etc`, `/boot`, `/sys` erano scrivibili come la cartella dei download. La stessa guardia in due copie, e le due copie sapevano cose diverse: e' D185 visto dall'altra parte. Adesso i due elenchi stanno tutti e due in `config.py`, la classe sceglie quale, l'estrattore li porta tutti e due in Rust, e `test_guardie_predefinite.py` li confronta **tutti e due sempre** — confrontare solo quello di questo sistema vorrebbe dire che su Windows nessuno guarda l'altro, che e' come l'altro era rimasto solo |
| D231 | **`ruota_se_serve` tocca solo i file normali** | Guardava la dimensione e non il tipo. Su Windows una cartella misura zero byte, quindi finiva sotto il tetto e usciva da sola; su Linux e macOS ne misura 4096, cioe' passava il controllo e arrivava al `rename` — che una cartella la **sposta**. Bastava un percorso di registro configurato male perche' NOVA spostasse una cartella dell'utente senza dire niente, e da Windows non si sarebbe visto mai. Vale per tutte e due le teste, Python e Rust |
| D232 | **Le prove Python girano anche su Linux e macOS, in CI** | Il lavoro `altrove` faceva solo `cargo test`: di NOVA in Python fuori da Windows non guardava niente nessuno. Il giorno in cui le si e' fatte girare davvero ne sono uscite tre rosse, e nessuna delle tre era una prova scritta male — erano D230, D231, e due prove che ponevano in windowsese domande che altrove non volevano dire niente (percorsi con la barra rovescia, che fuori da Windows e' un carattere dentro un nome di file, non un separatore). Tre difetti veri, invisibili per mesi perche' nessuna macchina diversa li guardava mai |
| D233 | **Una prova che cambia il mondo lo rimette a posto, anche quando fallisce** | `test_registro.py` metteva la cartella del registro a 0555 per controllare che un registro impedito non si mettesse di traverso, e non la rimetteva. Su Windows non si vedeva — li' `chmod` non fa niente — ma su Linux e macOS la cartella restava in sola lettura fino in fondo, e cinquanta righe dopo la potatura non poteva piu' rinominare niente: rossa li', verde ovunque altro, e con la faccia di un difetto della potatura. Ci sono volute tre sonde per risalire alla riga giusta. Permessi, variabili d'ambiente, cartelle: si rimettono in un `finally`, e si controlla che siano tornati |
| D234 | **La porta di una prova chiede tutto quello che la prova usa** | `test_powershell.py` ci ha messo quattro giri: prima `which powershell`, e su un agente macOS un `powershell` c'era ma NOVA non riusciva a lanciarlo; poi «lo avvio io con il percorso assoluto», che partiva mentre NOVA lo chiama per nome e non partiva — due domande diverse, e quella giusta non era la mia; poi «lo avvia NOVA», che funzionava, ma cinquanta righe dopo la prova chiama anche `run_cmd` e su macOS `cmd` non c'e'. Ogni giro ha guadagnato un'annotazione rossa e un'ora. La porta si scrive guardando cosa la prova **usa**, non cosa viene in mente per primo, e si chiede alla stessa strada che percorre il codice provato |
| D235 | **Una prova non misura la velocita' della macchina** | `test_attesa.py` faceva `sleep(0.35)` e pretendeva almeno quattro battiti da 50 ms. Il conto torna su una macchina scarica e non torna su un agente carico, dove un thread puo' restare fermo mezzo secondo senza che niente sia rotto. La proprieta' da provare era «si ripete da solo», e quella non ha fretta: si aspetta che accada, con un tetto largo. Una prova che diventa rossa perche' la macchina e' occupata insegna a ignorare i rossi, che e' il modo in cui i controlli finiscono spenti |
| D236 | **Il confine di cio' che esce dal PC a farsi giudicare e' un tipo, non una regola scritta** | Scelto da Gio: **tutto tranne i segreti**. Qualunque decisione puo' essere presa fuori casa — quale cervello serve, se vale la pena ricordare, quanto centra un pezzo di memoria, quanto rischia una chiamata, come si spiega un errore — tranne quelle che riguardano credenziali e forme riservate, che restano in casa **per sempre**, perche' chiedere a qualcun altro «questa e' una chiave?» vuol dire mandargli la chiave. E `solo_locale` resta sopra tutto: acceso, non esce niente. Il confine sta in `nova-decisioni`, e non come controllo da ricordarsi a valle: `Fuori::prepara` e' l'unico modo di avere del materiale pronto a uscire, e da li' un segreto **non si costruisce**. E' la lezione del gradino che e' un processo e non un indirizzo (D227), applicata a una porta che da' sulla strada |
| D237 | **Per modificare un `.docx` non serve una libreria di `.docx`** | Misurato sul banco: `docx-rs` fa il giro a vuoto — legge e riscrive senza toccare niente — e perde `word/theme/theme1.xml` (cioe' i caratteri e i colori del documento), `customXml/`, `stylesWithEffects.xml`, `webSettings.xml` e la miniatura; aggiunge quattro parti che non c'erano; cambia altre dieci; e lo stile `Normal` dei paragrafi sparisce. Il file passa da 37 a 105 kB. La ragione non e' un difetto di quella libreria: e' che **ricostruisce il pacchetto dal proprio modello**, e quel che il modello non conosce non esiste. Aprendo invece lo zip e toccando **solo** `word/document.xml`, con ogni altra parte ricopiata byte per byte: zero parti perse, zero aggiunte, una cambiata, e `Normal` al suo posto. E' la stessa scelta che `harness_modifica.py` ha gia' fatto in Python — «si modifica il paragrafo, non il documento» — e vale la pena che sia scritta due volte perche' la tentazione della libreria comoda torna ogni volta |
| D238 | **Del PDF serve **dove**, e questo decide quale libreria** | `pdf-extract` da' il testo e sa dividerlo per pagina, quindi «pagina 12» si puo' dire. Ma su un PDF a due colonne **mescola le due colonne nella stessa riga**: misurato, non temuto. «Terzo blocco» allora non si puo' dire, e quella frase e' la funzione dell'harness, non un vezzo dell'interfaccia. `mupdf` — la stessa libreria che sta sotto PyMuPDF, quindi le stesse risposte che NOVA da' oggi — restituisce blocchi con le coordinate, e le due colonne restano a x=72 e x=320. Il prezzo e' che si compila dai sorgenti C: minuti al primo giro, e sette megabyte di binario. Per questo il banco sta fuori dal workspace, dietro una feature da accendere a mano: un costo che paga chi decide, non chi lancia le prove |
| D239 | **`umya-spreadsheet` regge il giro completo sui fogli** | Leggi-tocca-riscrivi su un `.xlsx` con formule, formato percentuale, intestazione in grassetto colorata e un secondo foglio: la formula `C2-B2` sopravvive, il formato `0.0%` sopravvive, il grassetto sopravvive, il secondo foglio c'e' ancora con dentro quello che c'era. `calamine` legge bene ma **non scrive**, quindi copre meta' di CANT-10 |
| D240 | **L'origine si manda al browser, ed e' esatta** | Da Chrome 111 la connessione al CDP viene rifiutata se l'origine non e' fra quelle dichiarate all'avvio. NOVA il browser lo avvia da se' con `--remote-allow-origins=http://127.0.0.1`, quindi quella e' **l'unica** che passa — e va mandata. Misurato contro un Chrome 141 avviato esattamente come lo avvia NOVA: con `Origin: http://127.0.0.1` risponde, **senza** origine da 403 Forbidden. Sbagliarla in una delle due direzioni non da' un errore che si capisce: da' un browser che non si comanda, e il messaggio grezzo (`Http(Response { status: 403 ... })`) manda a cercare dalla parte sbagliata. Per questo il 403 ha una frase sua, che dice cosa fare |
| D241 | **Il client websocket del CDP e' `tungstenite`, ed e' bloccante** | Ultima scelta di libreria rimasta aperta di tutto il cantiere, e si e' fatta contro un Chrome vero, non contro un server di prova: elenco delle schede, aggancio, `Runtime.evaluate`, due domande di fila sulla stessa connessione, un metodo inventato che dev'essere rifiutato. Bloccante e non asincrona perche' e' la forma del problema: «questo modulo viene chiamato da processi che nascono e muoiono a ogni richiesta, e una connessione che sopravvive al processo non esiste». Dove servono piu' domande legate fra loro c'e' la `Sessione`, perche' `objectId` e domini abilitati vivono **nella connessione** |
| D242 | **Anche i numeri che stanno in due posti si confrontano** | `nova-browser` diceva `PORTA = 9333`. Il Python ne ha due — 9222 per il browser di lavoro, 9223 per quello delle ricerche, separati apposta — quindi quel 9333 non corrispondeva a niente e non era scritto da nessuna parte perche'. Non si era rotto niente perche' non la usava ancora nessuno: il giorno in cui il braccio in Rust si fosse attaccato avrebbe cercato un browser dove non c'e' nessuno, e l'errore sarebbe stato «non trovo il browser». `test_elenchi_gemelli.py` confrontava gli **elenchi** e non i numeri, e un numero sbagliato in un diff non si vede piu' di quanto si veda una voce mancante: adesso confronta anche quelli |
| D243 | **`nova-dati` misura e racconta, i percorsi glieli passa chi li scrive** | La mappa «dove sono i miei dati» non tiene una propria copia dei percorsi: ogni percorso lo dice il modulo che ci scrive dentro, perche' una copia prima o poi diverge e qui divergere vuol dire indicare a chi cerca i suoi dati un file che non c'e' (D188). Il crate ha quel che si puo' provare senza un disco — come si scrive una misura, come si mette insieme il racconto, cosa finisce nel rendiconto, e la domanda «sta dentro questa cartella?» — e prende i posti da fuori. Il giorno in cui un modulo passa in Rust, il suo percorso si sposta con lui e il resto e' gia' li' |
| D244 | **La misura si legge uguale dovunque la si legga** | Lo stesso file lo raccontano tre posti: la chat, il rendiconto che l'installatore stampa mentre disinstalla, e il pannello. Tre misure diverse dello stesso file non sono un dettaglio estetico, sono tre programmi che sembrano non essersi parlati. Quindi le soglie e i decimali sono **quelli**, arrotondamento compreso — 1536 byte sono «2 kB», non «1 kB» — e un banco li confronta su cinquantuno casi, fitti intorno alle tre soglie, che e' dove si sbaglia |
| D245 | **Il `.parte` si aggiunge all'estensione, non la sostituisce** | `modello.onnx` diventa `modello.onnx.parte`, non `modello.parte`: sostituirla vorrebbe dire che due pezzi con lo stesso nome e estensioni diverse si litigano lo stesso file temporaneo, e chi arriva secondo trova mezzo file dell'altro. E un `.parte` rimasto da un tentativo andato male **non si riprende**: non si sa se il server servisse lo stesso file — una versione nuova con lo stesso nome capita — e riprendere sbagliato produce un archivio corrotto che **sembra intero**, cioe' il guasto peggiore, che si scopre al caricamento e lontano da qui |
| D246 | **Un pezzo da estrarre ha una prova che non e' la sua cartella** | Per uno zip il `dove` e' una cartella, e una cartella c'e' sempre: guardare quella vuol dire dire «presente» a chi non ha niente dentro. Ogni pezzo che si estrae dichiara il file la cui esistenza prova che il lavoro e' stato fatto, e una prova di questo esiste apposta perche' un pezzo nuovo che se la dimentica non passi inosservato |
| D247 | **Un file equivalente gia' sul disco vale, e si conta solo quel che manca** | Il demone accetta `ggml-small`, `base`, `medium` o `tiny`: dire «manca» a chi ne ha gia' uno solo perche' il catalogo ne nomina un altro vuol dire fargli scaricare mezzo giga per niente. E il totale da scaricare somma **solo i componenti incompleti**: dire «350 MB» a chi ne ha gia' due terzi e' il modo di far rinunciare qualcuno che avrebbe finito in un minuto |
| D248 | **Una sezione che nel file non e' un oggetto non deve poter impedire l'avvio** | `"safety": "ciao"` in config.json non riportava NOVA ai predefiniti: alzava `AttributeError` dentro `_merge`, che sta **fuori** dal riparo di `load()`. Cioe' un carattere sbagliato in un file che l'utente puo' aprire e correggere a mano non degradava niente, spegneva tutto. Adesso quella sezione resta di fabbrica — guardie comprese — e finisce in `sezioni_ignorate`, che si stampa all'avvio: il danno e' circoscritto alla sezione, e lo si dice |
| D249 | **Un file di configurazione vuoto non e' un file rotto** | Un `config.json` da zero byte — un salvataggio interrotto, un disco pieno — veniva letto come JSON illeggibile. Distinguerli conta da D250 in poi: «rotto» ora vuol dire «non ti riscrivo sopra», e su un file vuoto quella prudenza non protegge niente e lascerebbe NOVA a lamentarsi per sempre di un file in cui non c'e' niente da recuperare. Vuoto vuol dire «non detto», come se il file non ci fosse |
| D250 | **Una configurazione che non si e' saputa leggere non si riscrive** | `_prepare_config` ricarica, completa e salva. Su un file illeggibile `cfg` sono i predefiniti: salvarli vuol dire cancellare la configurazione di qualcuno — **chiave API compresa** — per una virgola di troppo, e cancellarla proprio mentre gli si dice che c'e' un problema. Il file rotto resta dov'e': si puo' ancora aprire, correggere, e recuperarci la chiave |
| D251 | **La diagnostica si confronta guardandola, non togliendola** | Il banco della configurazione toglieva i campi di diagnostica da tutte e due le parti prima del confronto, e li' dentro ci finiva anche la regola «la diagnostica non arriva da fuori»: una mutazione che la spegneva passava verde, perche' la prova si accecava esattamente sul punto che doveva guardare. Adesso quei campi si confrontano **con la fabbrica**, non fra loro |
| D252 | **«Messa a nulla» non vuol dire solo `null`** | Una CLI svuotata dal pannello puo' arrivare come falso, zero, stringa vuota o lista vuota, e una lapide e' una lapide comunque sia scritta. Lo diceva gia' il Python (`if not v`), il gemello lo diceva per caso: una mutazione che riduceva il controllo al solo `null` e' sopravvissuta al primo giro |
| D253 | **Una cella con dentro un conto mai calcolato non si legge vuota** | Un `.xlsx` porta, per ogni cella con una formula, la formula **e** il risultato dell'ultima volta che qualcuno l'ha calcolata. Nessuna libreria calcola niente: leggono la cache, e la cache e' vuota in ogni file generato da un programma invece che da Excel. Cioe' NOVA, leggendo un foglio appena scritto, vedeva celle vuote dove stanno i totali — `data_only=True` da' `None`, e `None` diventava la stringa vuota come una cella davvero vuota. Adesso: se il risultato c'e' si dice il risultato, se non c'e' si dice la formula con l'uguale davanti. Brutto da leggere e vero, invece che pulito e falso |
| D254 | **Un numero si scrive come numero solo quando scriverlo come numero non lo cambia** | Scrivere in una cella il testo che un modello ha prodotto vuol dire decidere, per ogni valore, se e' un numero — e sbagliare e' silenzioso in tutti e due i versi. `007` diventa `7`, `+39 02 1234` diventa un conto, un numero d'ordine di sedici cifre torna indietro arrotondato (oltre 2^53 un intero non ci sta esatto in un `f64`): tre danni invisibili. Un prezzo scritto come testo da' un totale che non somma: un danno visibile. Fra uno visibile e tre invisibili si sceglie quello visibile, e si tiene l'apostrofo di Excel per chi vuole decidere da se'. `1.50` resta un numero: gli zeri in coda dopo la virgola sono un **formato**, non un altro valore |
| D255 | **Un foglio di calcolo si scrive di fianco e poi si rinomina, e il foglio non si crea da se'** | `umya` riscrive tutto l'archivio: una scrittura interrotta a meta' — la corrente, il disco pieno, l'antivirus — lascerebbe al posto del bilancio di qualcuno uno zip troncato che ha ancora il nome giusto. Si scrive `.parte` di fianco e si rinomina, come per il vault e per la configurazione. E scrivere su un foglio che non c'e' e' un **errore**, non un invito a crearlo: un nome sbagliato — `conti` invece di `Conti` — produrrebbe un secondo foglio vuoto accanto a quello vero, e chi guarda il totale in fondo non vedrebbe niente di strano |
| D256 | **I fogli si leggono con le stesse regole ovunque li si legga** | Le regole stavano in due posti — `tools/documenti.py` e `fascicolo.py` — con due separatori, due limiti di righe e due idee di riga vuota. Due letture dello stesso file che danno due testi diversi sono due programmi che sembrano non essersi parlati, ed e' la terza volta che questo progetto ci inciampa (D185, D242). Adesso stanno in `nova/fogli.py`, col gemello `nova-fogli` e un banco che li confronta |
| D257 | **Anche le prove vanno su tutti e due i sistemi, non solo il codice** | `un_file_equivalente_gia_sul_disco_conta` era verde qui e rossa sulla CI: il codice usava `join`, che su Windows mette il backslash, e il finto disco della prova confrontava stringhe scritte con le barre in avanti. Il difetto non era nel codice, era nella prova — ed e' lo stesso inciampo che aveva gia' preso `nova-cartelle`. Una prova scritta per un sistema solo non e' una rete: e' una rete tesa dove i pesci non passano (D209) |
| D258 | **Il pid 0 non e' un processo, e si rifiuta per nome** | `kill` prende un intero con segno: `kill(0, ...)` lo manda a **tutto il gruppo di chi chiama**, `kill(-1, ...)` a ogni processo che l'utente puo' segnalare. Il tipo qui e' un `u32`, quindi i negativi non arrivano — ma lo zero si', e uno zero puo' venire da un campo vuoto o da un `parse` andato male con un `unwrap_or(0)`. E' l'equivalente Unix dei duecentonovantadue processi che su Windows si selezionavano con un asterisco. Rifiutato, insieme al pid 1, che dentro un contenitore e' il contenitore stesso. La mutazione che toglie questo controllo non e' «passata rossa»: **ha ucciso il processo che eseguiva le prove**, che e' la dimostrazione piu' diretta che si potesse avere |
| D259 | **Nel Cestino non si copia e poi si cancella** | Il Cestino e' un `rename`: riesce o non riesce. Copiare su un altro disco e poi cancellare l'originale sono **due** operazioni, e fra le due c'e' un momento in cui un disco pieno o un cavo staccato lasciano mezzo file e nessun originale. Quando il `rename` non si puo' fare e su quel disco non si apre un cestino, la risposta e' «qui l'unica alternativa e' distruggere», e va **detta**, non fatta — la stessa frase che la meta' Windows scrive per i dischi di rete |
| D260 | **La scheda del Cestino si scrive prima del file, e con `O_EXCL`** | La specifica freedesktop lo prescrive, e la ragione e' che quel `create_new` **e' il lucchetto**: due programmi che cestinano nello stesso istante due file con lo stesso nome non possono vincere tutti e due, perche' il secondo fallisce. Scrivendo prima il file e poi la scheda si perderebbe la corsa e si sovrascriverebbe il file di qualcun altro. E il numero va **prima** dell'estensione — `appunti.1.txt`, non `appunti.txt.1` — se no il file nel Cestino non si apre piu' con un doppio clic, e uno che non si apre non si riconosce e non si recupera |
| D261 | **Su Linux la memoria libera e' `MemAvailable`, non `MemFree`** | `MemFree` su Linux e' quasi sempre vicino a zero, perche' il sistema usa tutto quel che avanza per la cache del disco e lo restituisce appena serve. Dire «liberi 200 MB» a chi ne ha otto giga disponibili vuol dire far rinunciare qualcuno a caricare un modello che ci starebbe benissimo — cioe' il difetto peggiore per un progetto il cui primo ostacolo e' «ci sta sul mio PC?» |
| D262 | **Quando manca uno strumento esterno, si dice quale si cercava** | Appunti, volume e notifiche su Linux li tiene l'ambiente grafico, e non c'e' modo di sapere quale c'e' se non provando. «Gli appunti non sono disponibili» manda qualcuno a cercare un guasto; «non trovo ne' `wl-copy` ne' `xclip` ne' `xsel`» gli dice cosa installare (D193). E Wayland si prova **prima** di X11: su una sessione Wayland `xclip` spesso c'e' lo stesso, per Xwayland, e parla con gli appunti sbagliati — chi copia da Firefox non ritroverebbe niente |
| D263 | **Un server MCP si dichiara, non si scopre** | NOVA non va a cercare server MCP sul PC, e non imparera'. Un server descrive i propri strumenti **con parole sue**, e quelle parole finiscono nel prompt: un server trovato da solo sarebbe uno sconosciuto con il permesso di scrivere dentro la testa di NOVA, e nessuno gliel'ha dato. Si dichiarano in `mcp_esterni` nella configurazione, dove l'utente li vede e li toglie. Una voce sbagliata non sparisce in silenzio: finisce fra i rifiutati col motivo, perche' un server che l'utente ha scritto e che NOVA ignora senza dirlo e' il modo piu' sicuro di fargli perdere mezz'ora |
| D264 | **Le parole di un estraneo si citano, non si obbediscono — e cio' che comanda si dice, non si toglie** | La descrizione arriva marcata («fornita dal server X, non da NOVA») perche' dentro un prompt quella e' l'unica cosa che distingue cio' che NOVA sa da cio' che le ha detto un processo di qualcun altro. E cio' che somiglia a un ordine — «ignore previous instructions», «non dirlo all'utente» — si **nomina**: toglierlo di nascosto vuol dire che l'attacco riesce a meta' (la frase sparisce, e nessuno sa che qualcuno ci ha provato) e che uno strumento onesto che nomina una di quelle parole diventa incomprensibile. I sospetti si cercano anche nei **nomi dei campi dello schema**: guardare solo la descrizione lascia aperta la porta di fianco |
| D265 | **I nomi degli strumenti altrui portano davanti quello del server** | Un server che dichiara uno strumento chiamato `Bash` non deve poter coprire il `Bash` di NOVA. `posta__invia`, e il taglio e' al **primo** separatore: cosi' uno strumento che ha il separatore nel nome resta leggibile |
| D266 | **Uno strumento di un altro non e' mai «sicuro»** | `nova_mcp::rischio` dice «safe» a `Read` perche' sa cos'e' il `Read` di NOVA: l'ha scritto NOVA. Di uno strumento altrui sa solo il nome che si e' dato, e cio' che un estraneo dice di se' non e' una prova. Si parte da «moderate». Quel che guarda i **valori** degli argomenti invece vale per chiunque, perche' non dipende da chi li manda |
| D267 | **Quel che entra da fuori ha una misura** | Descrizioni e risposte finiscono nel contesto. Senza un tetto un server puo' riempirlo da solo — e non serve malizia, basta uno strumento che restituisce un file. Duemila caratteri per una descrizione, ventimila per una risposta, cento strumenti per server, e si taglia a **caratteri** e non a byte: tagliare a byte spezza una lettera accentata a meta' |
| D268 | **«Lo strumento dice di no» non e' «la chiamata e' riuscita»** | Nel protocollo MCP l'errore dello strumento non e' un errore del protocollo: arriva come una risposta riuscita con `isError` acceso. La distinzione e' giusta — «non si e' potuto chiedere» e «si e' chiesto e ha detto di no» si risolvono in modi opposti — ma va **guardata**, o diventano tutte e due «riuscito». Trovato dal banco contro un server vero: chiamare uno strumento che non esiste tornava `Ok`, e NOVA lo avrebbe letto come una chiamata andata bene con dentro una frase strana |
| D269 | **L'estensione non e' `Path.suffix`** | Per Python `Path(".gitignore").suffix` e' la stringa vuota: un nome che comincia per punto e' tutto nome. Cosi' `.gitignore` stava dentro `CODICE` — cioe' dentro `LEGGIBILI` — e l'harness lo **dichiarava apribile**, ma `_albero` lo saltava sempre e `_leggi_documento` rispondeva «non so aprire un file senza estensione». Un elenco che dice una cosa e un dispacciamento che ne fa un'altra: si vedeva solo mettendo le due meta' una di fronte all'altra |
| D270 | **Il nome di un blocco e' il numero di riga vero, e non slitta** | `r12` e' la dodicesima riga del file contando da zero, anche se prima ce n'erano tre vuote che non sono diventate blocchi. Rinumerare sarebbe piu' ordinato e vorrebbe dire che l'errore di un compilatore e il blocco dell'harness parlano di due righe diverse — e l'harness esiste per **indicare**, non per raccontare |
| D271 | **A pari punteggio vince chi viene prima nel documento** | L'ordinamento e' stabile dalle due parti. Non e' un dettaglio di implementazione: e' cio' che rende ripetibile una risposta, e una risposta che cambia ordine fra due domande uguali fa dubitare di tutto il resto — compresa la parte giusta |
| D272 | **Un blocco non si riporta a meta'** | Quando il testo da mostrare finisce lo spazio, l'ultimo blocco non entra troncato: non entra. Un blocco tagliato dice una cosa che il documento non dice, ed e' esattamente il contrario di quel che serve a un posto il cui mestiere e' «o quel blocco contiene quella cosa o non la contiene» |
| D273 | **Una proposta non si applica a un file cambiato sotto** | La proposta si controlla contro i blocchi letti quando il documento e' stato **aperto**, e si applica anche mezz'ora dopo. Se in mezzo qualcuno ha toccato il file — l'utente, un altro programma, un `git pull` — le righe a quel numero vogliono dire un'altra cosa, e NOVA ci scriveva sopra **senza dire niente**: la modifica finiva in un punto a caso del testo di qualcuno, e la copia `.prima` conteneva gia' la versione sbagliata. Adesso ogni modifica si porta dietro cosa c'era, e se non c'e' piu' non si scrive: niente, non «le altre si'» — applicare meta' di quel che si e' mostrato e' peggio che non applicare. Il confronto e' sul **contenuto**, non sulla data: una data cambia anche quando il testo e' lo stesso |
| D274 | **Le righe si contano come le conta chiunque altro** | `str.splitlines()` in Python taglia anche sul salto pagina (`\x0c`), sulla tabulazione verticale e su mezza dozzina di confini Unicode. Un `.txt` con dentro un salto pagina — ce ne sono, nei documenti vecchi e in certi sorgenti C — diventava quattro righe dove il file ne ha tre, e da li' in giu' **ogni** numero di blocco era slittato di uno: `r12` non era piu' la riga 12 per nessuno tranne che per l'harness, e la modifica si scriveva una riga piu' su di dove si era mostrata. Un editor, `wc -l` e un compilatore contano i `\n`: contiamo quelli |
| D275 | **Dentro `document.xml` non c'e' un parser XML, c'e' uno scanner** | Sa trovare dove comincia e dove finisce un elemento con un certo nome, contando aperture e chiusure — e basta, perche' tutto quel che si fa e' sostituire testo dentro elementi che esistono gia'. Non si crea struttura, non si sposta niente, non si riordina. Un parser vero servirebbe per fare di piu', e fare di piu' e' esattamente cio' che ha spogliato il documento nel giro col `docx-rs` (D237). Due dettagli che non sono dettagli: `<w:p` non deve combaciare con `<w:pPr`, che sta dentro ogni paragrafo, e un `<w:p/>` vuoto va contato lo stesso — se no i numeri dei blocchi slittano e `p3` indica un altro paragrafo |
| D276 | **Il testo nuovo va tutto nella prima porzione** | Le porzioni di un paragrafo esistono perche' hanno formattazioni diverse: una parola in grassetto e' una porzione sua. Distribuire un testo nuovo fra porzioni vecchie vorrebbe dire indovinare quale pezzo va in grassetto. Mettendolo tutto nella prima e svuotando le altre, il paragrafo prende la formattazione con cui **cominciava**, che e' l'unica scelta che non inventa niente — ed e' quel che fa `python-docx`, quindi le due meta' scrivono lo stesso documento. E `xml:space="preserve"` si mette sempre: senza, Word toglie gli spazi in testa e una riga di codice indentata perde il rientro |
| D277 | **Non «e' verde», ma «e' peggio di prima»** | Se i test erano gia' rossi, «verde dopo» e' irraggiungibile e «rosso dopo» non dice niente: si starebbe rifiutando una modifica buona per colpa di un guasto che c'era gia'. Conta il confronto — quali passavano prima, quali passano adesso. E' la differenza fra un verificatore usabile su un progetto vero e uno che funziona solo se la suite era gia' tutta verde. Se **prima** non si e' potuto provare, si parte da «niente era rotto»: e' il verso prudente, perche' cosi' una caduta nuova si vede invece di essere messa in conto al passato |
| D278 | **Un file di una lingua che il progetto non prova non prende nessun verde** | Provare la suite Rust perche' si e' cambiata una riga di Python e' tempo buttato; provarla per un `.md` e' peggio, e' un verde che non parla di quel file. Quando non c'e' un banco della lingua giusta, la risposta e' «non so come si prova questo», che e' piu' onesta e piu' utile — e per il `.py` si prova pytest **prima** degli script, perche' dove pytest c'e' e' lui che sa raccogliere |
| D279 | **I nomi dei file si guardano senza maiuscole** | `Path.glob("*mmproj*.gguf")` su Windows non distingue le maiuscole — lo fa il filesystem — e su Linux si'. Un proiettore multimodale chiamato `MMPROJ-F16.GGUF`, e i repository li distribuiscono anche cosi', si trovava sul PC di chi sviluppa e non si trovava su Linux: li' NOVA restava **cieca senza dirlo** — nessun errore, nessun avviso, solo `--mmproj` che non viene passato e ogni immagine rifiutata con un 500. E' il terzo difetto di questa forma (D230, D257): il codice andava su tutti e due i sistemi, il **comportamento del sistema sotto** no |
| D280 | **I gemelli girano sulla CI, o non girano** | Le ventuno prove `test_*_rust.py` escono 2 — «non provabile qui» — quando il banco Rust non e' costruito, e nessun lavoro della CI lo costruiva. Quindi a ogni push dicevano «saltata», e le eseguiva solo chi si ricordava di lanciarle a mano: un metodo intero che riposava su una persona che si ricorda. Adesso c'e' un lavoro che costruisce i banchi e li esegue — e il primo giro ha trovato D279 |
| D281 | **Il ricalcolo delle formule si fa in casa, non riscrivendo il foglio** | Nessuna libreria di `.xlsx` calcola: leggono la cache, e la cache e' vuota in ogni file scritto da un programma. La strada che fanno quasi tutti — passare il file a LibreOffice in silenzio — calcola bene e **riscrive l'archivio intero**: misurato sul banco dei fogli, due parti perse, quattro aggiunte, undici su undici cambiate, con un `theme1.xml` nuovo. E' lo stesso comportamento che D237 ha rifiutato per il `.docx`, trovato su un altro formato. Un motore in Rust (`formualizer`) da' gli **stessi identici valori** toccando **una parte su tredici**, senza processi esterni e senza qualche centinaio di megabyte installati sul PC di qualcuno per fare una somma |
| D282 | **Come un motore dice di no conta piu' di quante funzioni sa** | Davanti a una funzione che non sa calcolare, LibreOffice scrive `#NAME?` **nel file consegnato**: un errore che sembra un dato, e che chi apre il foglio deve accorgersi da se' che non c'era prima. Il motore in casa dichiara l'errore nel rendiconto, e davanti a una cosa che non sa fare affatto — le formule che spandono su piu' celle — **rifiuta l'intero foglio senza scriverlo**. E' la stessa frase del Cestino su un disco senza Cestino (D259) e del file cambiato sotto (D273): quando non si puo' fare bene, si dice invece di fare male |
| D283 | **Un ricalcolo verde non vuol dire che le formule sono giuste** | Dice «si calcola», non «e' corretto». Un intervallo spostato di una riga da' un file pulito, senza errori, con dentro numeri sbagliati. Vale per NOVA quando scrive un foglio: prima due o tre formule e si controlla che tirino i valori che devono, poi la griglia — non il contrario |
| D284 | **Un valore di un tipo che la fabbrica non ha non entra — dove chi legge e' tipato** | Il demone converte la sua configurazione dentro una struttura: un `"shell_timeout_s": "ciao"` fa fallire la conversione di **tutta** la configurazione, e si torna ai predefiniti perdendo anche i campi scritti bene. Un intero e un decimale sono due tipi diversi, e non per pedanteria: `serde` rifiuta un `42.0` dove va un intero, e quel rifiuto arriva dopo — quando ormai si perde il file. Un `null` vuol dire «non detto»: resta quel che c'e' di fabbrica, e non si spaventa nessuno. Per NOVA in Python la regola e' **spenta**, perche' li' un tipo sbagliato non fa fallire la lettura ma qualcosa dopo, lontano dalla causa: e' un difetto anche quello, ma e' un altro, e chiuderlo vuol dire cambiare tutte e due le meta' insieme |
| D285 | **La configurazione del demone si legge con le stesse regole di quella di NOVA** | Erano due letture: una in `nova/config.py`, provata e ragionata, e una in `nova-core::config` che era `serde_json::from_str(...).unwrap_or_default()`. Quella seconda perdeva **tutto** il file per un campo storto, e tornare ai predefiniti li' vuol dire `write_roots` vuoto — cioe' il confinamento delle scritture che sparisce, in silenzio, per un campo che non c'entrava niente. E `protected_paths` si lasciava sostituire da un file salvato mentre `forbidden_commands` si univa gia' ai predefiniti: due guardie nello stesso file con due comportamenti diversi (D185, di nuovo) |
| D286 | **Un avviso emesso prima che il logger esista non e' un avviso** | `Config::load()` diceva `tracing::warn!("configurazione illeggibile, uso i default")`, e veniva chiamata **prima** di `tracing_subscriber::fmt().init()` — che e' obbligatorio, perche' il livello del log sta nella configurazione. Quindi l'unico segnale che diceva «ho ignorato quel che hai scritto» non lo leggeva nessuno, mai. Adesso la lettura non parla: quel che e' successo torna nei campi, e lo dice `main` quando c'e' un posto dove dirlo — sullo schermo per chi accende il demone a mano, e nel registro per chi lo trova acceso domani |
| D287 | **Si risponde la versione di protocollo che il client ha chiesto** | NOVA parla MCP da tre posti: il server di `mcp_kb.py`, il demone, e `nova-mcp` che e' il porto del primo. Due su tre rispondevano sempre la stessa versione, chiunque avesse chiesto; il demone no, ed era l'unico che aveva imparato — con dentro il commento di quando un client, sentendosi rispondere una versione che non aveva nominato, aveva mollato il collegamento senza un messaggio d'errore. Il sintomo di quel guasto e' un modello senza nessuno strumento e un registro muto. Adesso la regola sta in `nova-mcp` — si echeggia la versione chiesta se la conosciamo, altrimenti la piu' recente che sappiamo parlare, come vuole la specifica — e il demone la usa da li' invece di tenerne una copia |
| D288 | **Un elenco scritto a mano dentro un file generato non lo guarda nessuno** | La prova degli elenchi gemelli conta *tutti* i `pub const [&str; N]` dei crate e pretende che ognuno sia gemellato, delegato a un banco, o dichiarato senza gemello con scritto perche'. Salta pero' i file generati, e li riconosce da una riga nell'intestazione: «Generato da». `dichiarazioni.rs` diceva «generato, poi mantenuto a mano», cioe' era esattamente il posto dove un elenco poteva nascondersi — e la prima costante che ci ho messo ci si e' nascosta subito. Ora l'estrattore genera anche quella, l'intestazione dice «non si modifica a mano» e spiega perche', e il banco gemello confronta le due liste invece di fidarsi della parola «generato» |
| D289 | **Un banco che confronta il Rust con un'imitazione del Python non confronta il Python** | `test_mcp_rust.py` aveva un `FintoServer` con dentro il `gestisci` riscritto a mano — perche' quello vero stava dentro `ServerKB`, che per esistere costruisce il vault, il router e il browser. Confrontava quindi il Rust con una copia che nessuno confrontava con l'originale: se il server vero fosse cambiato, il banco sarebbe rimasto verde. Il protocollo e' uscito dalla classe ed e' diventato `gestisci_busta(richiesta, strumenti)`, una funzione pura nello stesso blocco che il banco gia' esegue; il finto server tiene solo i **corpi** degli strumenti, che e' l'unica cosa che davvero non si puo' costruire in una prova |
| D290 | **Quel che fa il demone finisce nello stesso registro di quel che fa NOVA** | Il registro delle azioni esiste perche' «si risponde solo di quello che si puo' vedere», e il demone — il pezzo che gira quando non guarda nessuno — non ci scriveva niente: `shell.exec` esegue un comando qualunque e lasciava solo un evento sul bus, che muore col processo. Il giornale del demone non e' la stessa cosa: quello serve ad **annullare**, ed e' un'altra domanda. Adesso ogni comando lascia una riga, e **ogni** vuol dire ogni: distinguere quelli «pesanti» e' un giudizio che sbaglia — `Remove-Item` si riconosce, `python pulisci.py` no — e un registro che tiene solo cio' che riconosce da' l'idea sbagliata di essere completo. Ci finisce anche una `fs.write` di cui non si e' potuta tenere la copia di prima, che e' precisamente un file sovrascritto per sempre |
| D291 | **La potatura dei diari sta in un posto solo per ogni linguaggio** | Due megabyte e uno storico: la regola era gia' in un posto solo dalla parte Python (`nova/rotazione.py`, dopo che quattro copie avevano quattro tetti diversi — D72, D73), e dalla parte Rust stava dentro `nova-nodi`, cioe' dentro il vault. Chiunque altro dovesse potare un file avrebbe tirato dentro tutto il vault, oppure — piu' probabile — si sarebbe riscritto le tre righe: e' successo appena il demone ha avuto un file da potare. Ora sta in `nova-potatura`, e la prova che sorveglia la regola non guarda piu' solo il vault: **conta le copie**, e un secondo tetto scritto a mano in un crate qualunque la fa diventare rossa |
| D292 | **Il fuso e' una domanda di sistema, e scriverlo sbagliato non da' nessun errore** | Il registro delle azioni lo scrivono tutte e due le meta' di NOVA, e la parte Python scrive l'ora dell'orologio di chi sta davanti al computer. Un demone che scrivesse UTC sullo stesso file non romperebbe niente: ogni riga sarebbe soltanto **sbagliata di un'ora o due**, e chi rilegge la propria giornata non ha modo di accorgersene. Il fuso dunque si chiede al sistema (`nova_platform::fuso_secondi`, `localtime_r` o `GetTimeZoneInformation`), che e' l'unico che lo sa, e chiedendolo **per quell'istante**: una riga scritta a luglio non deve cambiare ora perche' la si rilegge a dicembre. La prova passa il fuso invece di leggerlo, se no su una macchina regolata su UTC — cioe' la CI — non distinguerebbe niente |
| D293 | **`json.dumps` di Python mette due spazi, e dove il file lo scrivono in due quegli spazi contano** | `serde_json` scrive `{"a":1}`, Python `{"a": 1}`: la stessa cosa per chi rilegge, un testo diverso per chi confronta. La funzione che imita Python esisteva gia', dentro il crate del protocollo MCP; la seconda volta che e' servita — il registro delle azioni — chi ne aveva bisogno avrebbe dovuto dipendere da quello, o riscriversi i due separatori. E' andata in `nova-pitone`, che e' il posto delle abitudini di Python che il porto deve rispettare (D62) |
| D294 | **Il turno vive nel demone, e non e' una capacita'** | Un turno di NOVA era un **processo Python intero**: il guscio lanciava `python -m nova --ask`, quello importava mezzo mondo, faceva il giro e moriva. Il ciclo in Rust c'era gia' — `nova-ciclo` per l'ordine delle cose, `MondoVero` per il mondo vero, provati contro un cervello finto — e non lo chiamava nessuno. Adesso lo chiama `agente/turno`. Le tre ragioni non sono di velocita': **le approvazioni** vogliono qualcuno che resti in piedi fra la domanda e la risposta (il processo `--ask` non ha nemmeno lo stdin collegato, e la domanda finiva in un EOF); **lo stato che scorre** diventa un evento sul bus invece di una riga su stderr con un marcatore dentro; **le guardie** valgono per costruzione, perche' gli strumenti li esegue il registro delle capacita', che e' dentro il processo che ha la policy. E' un **metodo RPC e non una capacita'** apposta: le capacita' si presentano al modello come strumenti, e uno strumento «fai un turno» e' un modello che chiama se stesso |
| D295 | **Il demone legge la configurazione di NOVA, non una sua** | Quali cervelli, in che ordine, con che chiave e con che prompt sta in `config.json`, dove l'ha scritto l'utente. Il demone ha un file suo (`core.json`) per le cose sue — endpoint, autonomia, guardie — e per il turno legge quello di NOVA. Due configurazioni dei cervelli vorrebbero dire due NOVA sullo stesso computer che rispondono in modo diverso, e nessun modo di sapere quale sta parlando. I gradini si **rileggono a ogni turno**: chi cambia cervello nel pannello deve vederlo valere adesso, non alla prossima conversazione |
| D296 | **Gli strumenti offerti al modello sono le capacita' del demone, sempre nello stesso ordine** | Non c'e' un secondo elenco: sono le stesse che usano la riga di comando, il guscio e Claude Code, con gli stessi nomi (`sys_info`, non `sys.info`) e le stesse guardie. L'ordine e' alfabetico e stabile perche' la prima regione della richiesta e' quella su cui i fornitori tengono la cache: un elenco che cambia ordine a ogni turno la butta via ogni volta, e non si rompe niente — diventa solo lento per sempre, che e' il difetto peggiore di cui accorgersi (D216, di nuovo) |
| D297 | **La rete che aspetta sta fuori dallo scheduler** | `ureq` e' sincrono e una risposta puo' metterci minuti. Aspettarla sul filo dello scheduler vorrebbe dire che per tutto quel tempo il demone non risponde a **nessun altro** — nemmeno al «fermati», che e' esattamente cio' che si vuole poter fare mentre un modello ci mette troppo. `block_in_place` toglie quel compito dallo scheduler; su un runtime a filo singolo (le prove) si chiama la rete e basta, perche' li' non c'e' nessuno scheduler da liberare |
| D298 | **Il guscio lo guarda una macchina** | `nova-shell` era l'unico pezzo escluso dalla CI, con scritto accanto che «vuole gtk e webkit installati». Sono quattro pacchetti (`libgtk-3-dev`, `libwebkit2gtk-4.1-dev`, `libjavascriptcoregtk-4.1-dev`, `libsoup-3.0-dev`), misurati in un contenitore Ubuntu prima di scriverli nel lavoro: con quelli `cargo check -p nova-shell --all-targets` passa. Il prezzo di non averli non era «il guscio non si compila in CI»: era che l'unica parte di NOVA **che l'utente vede** non la guardava nessuna macchina diversa da questa. Su macOS resta fuori finche' qualcuno non la prova davvero: dichiararlo e' meglio che far finta |
| D299 | **L'embedding di casa si porta identico, md5 compreso** | Il vettore da solo non si legge: si legge il suo coseno con gli altri, e quello decide **quali ricordi** entrano nel contesto. Due embedding che ordinano diversamente sono due NOVA che si ricordano cose diverse della stessa persona, e la differenza non si vede mai — si vede solo una risposta un po' peggiore, ogni tanto, senza nessuno che possa dire perche'. Per questo il porto usa `md5`: non serve a niente di crittografico, serve a cadere nella **stessa casella** del Python. Il banco confronta i vettori numero per numero, non «più o meno» |
| D300 | **La memoria del demone e' lo stesso vault, riletto solo dove e' cambiato** | Il vault e' una cartella di `.md` che l'utente apre in Obsidian: una nota scritta a mano deve valere subito, non al prossimo riavvio del demone. E rileggere mille file a ogni turno mentre qualcuno aspetta non e' un'ottimizzazione mancata, e' una risposta lenta. `Deposito::aggiorna` rilegge per impronta e reindicizza solo se qualcosa e' cambiato; il vault sta nel demone e non nella sessione, perche' e' uno per computer e due conversazioni aperte leggono la stessa memoria |
| D301 | **Il confine vale anche dopo che il comando e' partito** | Le guardie di NOVA hanno sempre vissuto dentro il processo che decide: `check_write` confronta percorsi, `comando_permesso` applica espressioni regolari. Servono a dire di no **prima**; dopo non servono a niente, perche' quel che passa il controllo gira con tutti i privilegi di chi l'ha lanciato — e un comando che la regola non ha riconosciuto scrive dove arriva l'utente. Adesso `shell.exec` parte dentro un recinto che tiene il **kernel** (Landlock su Linux): il rifiuto arriva da `EACCES` invece che da una frase nostra, e la restrizione non si puo' allentare nemmeno da dentro. E' la differenza fra una regola e un muro |
| D302 | **Il recinto si stringe su cio' che l'utente ha dichiarato, e dove non ha dichiarato niente non c'e'** | Il confine si costruisce da `write_roots`, cioe' dalla riga in cui l'utente ha detto dove NOVA puo' scrivere. Senza quella riga **non si stringe niente**: inventare un recinto vorrebbe dire decidere al posto suo quali cartelle sono sue, e sarebbe la stessa presunzione che NOVA rifiuta altrove (N1, N9). Dentro il recinto ci va anche una cartella temporanea di quel solo comando, che nasce con lui e muore con lui — senza, meta' dei programmi che appoggiano un file intermedio fallirebbe in modi che non somigliano a un problema di permessi. E **si dichiara sempre**, anche quando non c'e': un confine che si crede di avere e non si ha e' peggio di un confine che manca |
| D303 | **Il demone impara a turno finito, e non fa aspettare nessuno** | Dalla parte Python il processo moriva subito dopo la risposta, quindi l'estrazione della procedura andava attesa con un filo apposta e un tetto di trenta secondi — e quel che non faceva in tempo si perdeva. Il demone resta acceso: impara **dopo** aver risposto, e chi ha chiesto non aspetta niente. Le regole di quando si impara sono le stesse (procedure attive, il turno sopra la soglia di secondi, almeno uno strumento usato) e stanno in `nova-ricette`, confrontate col Python da un banco |
| D304 | **L'identificativo viaggia con la procedura** | Un'automazione nata da una procedura la ritrova per identificativo. Il primo porto lo teneva **fuori** dalla `Ricetta`, in una mappa accanto, e lo riassegnava per titolo dopo la fusione: due procedure che si fondono cambiano titolo, quindi l'automazione sarebbe restata orfana **in silenzio**. Adesso l'`id` e' un campo della procedura e sopravvive alla fusione come fa dalla parte Python, dove il dizionario che sopravvive si porta dietro il suo. Il banco lo confronta insieme al resto |
| D305 | **La strada di una domanda si sceglie prima, non si prova** | Il guscio puo' mandare una domanda al demone o alla meta' Python, e chiede al demone `agente/pronto` — che costa quanto un ping — **prima** di decidere. La strada opposta (provare il turno in Rust e ripiegare se fallisce) sembra piu' semplice e non lo e': un turno che muore a meta' ha gia' eseguito degli strumenti, e rifarlo dall'altra parte li eseguirebbe **due volte**. Si guarda il **primo** gradino della scala, non se ce n'e' uno buono da qualche parte: il turno parte sempre dal basso, quindi una scala che comincia con una CLI non e' una scala su cui puo' cominciare. Con `NOVA_CERVELLO=demone` il ripiego e' vietato, ed e' cosi' che ci si accorge che la meta' Rust e' rimasta indietro invece di scoprirlo fra sei mesi |
| D306 | **Gli avanzamenti passano dal bus, non dalla connessione che ha chiesto il turno** | Il guscio sente `agente.stato` e `agente.strumento` sulla connessione che tiene gia' aperta per tutto il resto, non su quella della domanda. Cosi' un turno partito dalla voce o da `nova chiedi` muove l'orb come uno partito dalla chat — se gli avanzamenti tornassero indietro solo a chi ha chiesto, chi guarda vedrebbe NOVA ferma mentre lavora. Di uno strumento si scrive la **descrizione**, non il nome: `fs.write` e' per il modello, «Scrive un file» e' per la persona, e il demone le ha tutte e due in mano nel momento in cui parte. Cio' che NOVA impara non e' un avanzamento e non si scrive: arriva a risposta gia' letta, e accenderebbe una riga di stato che nessuno spegnerebbe piu' |
| D307 | **La postilla della voce sta in coda alla domanda, in tutte e due le lingue** | Chi parla al microfono riceve un'istruzione in piu' — rispondi come si parla, e chiudi col marcatore — e quella istruzione **non** va nel prompt di sistema: il messaggio numero zero e' la regione su cui i fornitori tengono la cache, e la stessa sessione puo' ricevere un turno dalla voce e il successivo dalla chat. Adesso il testo e' lo stesso byte per byte dalle due parti (`nova_contesto::testi::POSTILLA_VOCE` contro `nova.main.POSTILLA_VOCE`), e il banco del contesto lo confronta insieme agli altri quattro |
| D308 | **Gli strumenti sui file del demone sono un ponte, non una seconda implementazione** | `nova-strumenti::file_disco` ha gia' i corpi, confrontati col Python operazione per operazione da un banco gemello. Le capacita' `fs.edit`, `fs.mkdir`, `fs.move`, `fs.copy`, `fs.delete`, `fs.search`, `fs.grep`, `fs.open` non riscrivono niente: aggiungono le tre cose che il demone ha e la cassetta non puo' avere — le guardie vere dalla configurazione dell'utente, il giornale (come si torna indietro) e il Cestino. Ogni operazione gira fuori dal filo del demone: cercare dentro una cartella grande legge migliaia di file, e farlo sul filo vorrebbe dire un demone che per venti secondi non risponde a nessuno |
| D309 | **Le guardie di NOVA e quelle del demone valgono tutte e due, e non si uniscono** | `config.json` (il pannello) e `core.json` (il demone) hanno ciascuno il proprio `write_roots` e `protected_paths`. Erano due elenchi che non si incontravano, ed e' diventato un problema nel momento in cui gli strumenti sui file sono entrati nel demone: chi scriveva «solo in Documenti» nel pannello non era protetto dal processo che esegue. Adesso valgono insieme, con due regole diverse: i **divieti si uniscono** (piu' divieti = piu' stretto), le **cartelle autorizzate si incastrano** — unirle autorizzerebbe piu' di ciascuno dei due elenchi, cioe' il verso opposto a quello che chi scrive una guardia si aspetta. Senza incastro non si scrive da nessuna parte, e si dice con un posto che non esiste invece che con una lista vuota, perche' una lista vuota vuol dire il contrario |
| D310 | **Quel che non si annulla lo dichiara, e finisce nel registro** | Il giornale serve a tornare indietro; il registro delle azioni serve a rispondere di quel che si e' fatto. Una copia **sopra** un file che c'era, e una cancellazione definitiva, non si annullano: invece di lasciare un'assenza nel giornale, si annota `NonSiPuo` con scritto perche', e la riga va anche nel registro. Simmetricamente, una cartella che c'era gia' **non entra** nel giornale: offrire di «annullarne la creazione» vorrebbe dire offrire di cancellare una cartella dell'utente che NOVA non ha mai creato. E annullare la creazione di una cartella la toglie solo se e' rimasta vuota |

## 8. Roadmap

### 8.1 La riformulazione

NOVA non e' un assistente con dei permessi: e' **un runtime personale per
agenti con accesso diretto al sistema operativo**. La domanda che guida il
lavoro cambia di conseguenza.

Non piu': *«come impedisco a NOVA di fare cose pericolose?»*
Ma: *«come faccio in modo che NOVA possa fare qualunque cosa, sapendo
esattamente cosa sta facendo, senza rompersi e potendo tornare indietro?»*

Le capacita' restano costanti; varia quanto processo decisionale si delega
(N9). Percio' non si costruiscono guardie: si costruiscono **annullamento,
anteprima, interruzione e memoria dei fatti**.

### 8.2 Cosa esiste gia'

Va detto prima di pianificare, altrimenti si riscrive cio' che funziona.

| Pezzo | Stato | Dove |
|---|---|---|
| Registro capacita' con rischio dichiarato | **fatto** — 41 capacita': 22 safe, 12 moderate, 7 dangerous | `nova-core/src/caps*.rs` |
| Bus eventi con sottoscrizione per argomento | **fatto** | `nova-core/src/bus.rs` |
| Processi come oggetti persistenti | **fatto** — pid, stato, riavvii, log | `supervisor.rs` |
| Nessuna morte silenziosa del supervisor | **fatto** — quarantena, `proc.gave_up` -> orb rosso | `supervisor.rs`, `bus.rs` |
| Albero di accessibilita' come canale d'azione | **fatto** | `nova-platform/src/windows_uia.rs` |
| Verifica della conseguenza | **primitiva presente** — `ui.attendi` aspetta l'effetto | `caps_ui.rs` |
| Credenziali cifrate, valore fuori dal modello | **fatto** — parametro `segreto:` | `segreti.rs` |
| Approvazione con attesa, scadenza, campanello | **fatto** | `caps_approvazione.rs` |
| Livelli di autonomia | **presente ma statico** | `SafetyConfig.autonomy` |
| Interruzione di cio' che e' in corso | **fatto** — `azione.ferma`, ogni capacita' e' interrompibile | `interruzione.rs`, `server.rs` |
| Annullamento delle scritture su file | **fatto** — giornale su disco, sopravvive al riavvio | `giornale.rs`, `annulla.*` |
| Freno visibile nell'interfaccia | **fatto** — bottone quando lavora, Esc contestuale | `ui/index.html` |
| Anteprima (`prova=true`) come contratto del registro | **fatto** — chi non sa rispondere rifiuta, non esegue | `capability.rs`, `server.rs` |
| Leggere documenti (PDF, Word, Excel) | **fatto** | `tools/documenti.py` |
| Vista | **fatto su tutti i cervelli** — le immagini viaggiano nei messaggi | `immagini.py`, `agent.py`, `runtime.py` |
| Agire nel tempo, anche a ripetizione | **fatto** | `tools/tempo.py` |
| Accorgersi dei cambiamenti e reagire | **fatto** — sondaggio, non notifiche | `osserva.rs`, `bus.rs` |

Circa meta' di quella che una roadmap scritta da fuori chiamerebbe «Fase 1»
e' gia' in piedi. Il lavoro vero e' altrove.

### 8.3 I tre pezzi che vengono prima di tutto

Rendono ogni cosa successiva meno rischiosa da costruire, e nessuno dei tre
toglie a NOVA un solo potere.

**A. Annullamento (N2).** Ogni capacita' che modifica dichiara la propria
inversa; il demone tiene un giornale delle operazioni. Un'operazione su 400
file si annulla con un comando solo.

> Difetto trovato e corretto: `files.py:194`. In uno **spostamento** con
> `overwrite`, la destinazione veniva distrutta con `rmtree()`/`unlink()`: chi
> chiede di spostare non ha chiesto di cancellare cio' che c'era. Ora finisce
> nel Cestino, e se non ci riesce si ferma invece di distruggere.
>
> Nota di onesta': la prima stesura di questo documento parlava di **tre**
> percorsi distruttivi. Guardandoli davvero, due erano legittimi —
> `files.py:262` e' `delete_path(permanent=True)`, cioe' una cancellazione
> definitiva **richiesta**, e `shell.py:101` rimuove un file temporaneo creato
> dallo strumento stesso. Il difetto era uno solo. Un elenco di problemi piu'
> lungo del vero e' un modo di sembrare rigorosi.

Windows regala meta' del lavoro: cestino e punti di ripristino. Il guadagno
non e' la prudenza, e' che NOVA puo' **osare**.

**B. Anteprima universale.** `prova=true` esiste oggi in **una sola**
capacita', `segreti.importa` — ed e' la ragione per cui quell'importazione e'
andata bene: 20 credenziali mostrate prima di scrivere un byte, e in anteprima
si e' visto che una password stava finendo come *nome del servizio*. Quel
difetto l'ha trovato la modalita' prova, non una revisione.

Va promossa a contratto del registro. Il valore piu' grande non e' per
l'utente: e' che **NOVA puo' verificare il proprio piano prima di eseguirlo**.

**C. Interruzione (N7).** Non e' un freno, e' un acceleratore. Senza stop ogni
azione va giustificata prima: NOVA diventa timida, l'utente la tiene ad
autonomia bassa, e a quel punto non serve a niente. Con lo stop **tentare
costa poco**, e un agente che puo' tentare e' molto piu' capace di uno che
deve avere ragione al primo colpo.

### 8.4 Poi

**Task come oggetti di prima classe.** Oggi esiste una conversazione, non
esistono task: se la chat si chiude non prosegue niente, e «riprendi quello
che stavi facendo» non ha un referente. La macchina a stati non e'
contabilita': e' **la** funzionalita' che rende reale il co-working — NOVA sul
secondo schermo mentre l'operatore sta sul primo, e il controllo da telefono.
Il pezzo che la rende viva: un task bloccato deve poter **parcheggiarsi e
chiedere** invece di fallire.

**Registro eventi strutturato, che NOVA rilegge.** Non per il debug umano: per
la sua memoria. Oggi la memoria e' scritta dal modello che riassume se stesso,
ed e' per questo che la scheda del bug del supervisor diceva `status: attivo`
mentre meta' era gia' corretta. Un modello che si racconta e' una fonte
inaffidabile su se stesso. Una memoria episodica **derivata dal registro** e'
fatta di cose accadute, non di cose narrate — e abilita la domanda che nessun
agente sa rispondere onestamente: *«perche' hai aperto Edge?»*.

**Autonomia negoziabile a tempo.** Non `autonomia = 2`, ma *«per questo task
mi servirebbe autonomia 3 per dieci minuti, me la concedi?»*. L'infrastruttura
c'e' gia': l'autonomia diventa una risorsa che si concede, non un interruttore
dimenticato acceso.

**Pianificatore.** Obiettivo -> piano -> passo -> azione -> osservazione ->
verifica. **Dopo** A, B e C: un pianificatore ad autonomia alta senza
interruzione e senza annullamento e' precisamente lo scenario che fa paura, e
a ragione.

**Memoria procedurale, con cautela.** Si promuove a procedura solo cio' che ha
**superato una verifica di conseguenza**, non cio' che e' semplicemente
accaduto tre volte. Altrimenti si automatizzano i propri errori.

### 8.5 Cosa NON si fa adesso, e perche'

**Sub-agent.** Moltiplicano i modi di sbagliare prima che ne esista uno solo
per capire cosa e' andato storto. Vengono dopo il registro eventi.

**Percezione a schermate come canale principale.** Violerebbe N6: l'albero di
accessibilita' funziona su finestre in secondo piano, ed e' la ragione fisica
per cui NOVA non ruba il posto.

**Riscrivere l'Agent Python in Rust.** Il confine e' gia' giusto: Python
ragiona, Rust agisce. Si sposta quando sara' stabile, non per eleganza.

## 9. Non-obiettivi

- Non si spegne VBS/Secure Boot per comodità.
- Non si chiedono esclusioni antivirus durante l'installazione.
- Non si promette parità fra modello locale e modelli a pagamento.
- Non si supporta la scrittura BIOS su schede senza mappa verificata.

## 9-bis. Il banco: riparare se stessa senza rompersi

NOVA ha le mani sul proprio codice da sempre: vive in una cartella che sa
leggere e scrivere. Quello che mancava non era il permesso, era **un posto dove
provare** una correzione prima che diventi il programma in esecuzione.

Il giro e' in `nova/banco.py`, esposto al modello in `nova/tools/riparazione.py`:

1. **si copia** — un albero di lavoro git a parte, con dentro esattamente il
   codice che sta girando, comprese le modifiche non ancora committate e i file
   nuovi. Un banco che non contiene il difetto e' tempo perso;
2. **si misura la partenza** — quali prove passano *adesso*. Senza questo
   numero «le prove passano» non dimostra niente;
3. **si lavora li'** — con gli stessi strumenti di sempre, sul percorso del banco;
4. **si confronta** — nessuna prova verde diventata rossa, nessuna prova
   sparita, niente fuori dal perimetro;
5. **si applica** — mettendo da parte gli originali, e registrando la
   riparazione perche' si possa annullare anche a NOVA spenta.

### Perche' non contraddice N1

N1 dice che NOVA non ha confini di capacita': niente sandbox, niente
recinti. Il banco sembra il contrario e non lo e', perche' non limita cio' che
NOVA puo' fare **sul PC** — limita solo il momento in cui una modifica al suo
**stesso codice** diventa reale. E' la differenza fra sequestrare gli attrezzi
a un falegname e un falegname che prova un incastro su uno scarto prima di
tagliare la trave. Sul banco NOVA puo' fare qualunque cosa, comprese quelle che
la romperebbero: e' esattamente a questo che serve.

### Cosa il banco non promette

Le prove coprono cio' che coprono. Una modifica puo' passarle tutte e rompere
qualcosa che nessuno prova: il banco riduce il rischio, non lo annulla. Per
questo la rete vera non e' il verde delle prove — e' che gli originali restano
da parte e `riparazione_annulla` funziona a distanza di giorni, senza bisogno
che NOVA sia accesa o funzionante.

## 9-ter. Le procedure: non rifare la fatica due volte

Il costo di una richiesta la prima volta non e' il modello: e' l'**esplorazione**.
«Controlla le ultime mail» la prima volta significa provare una strada, scoprire
che non va, provarne un'altra, trovare quella giusta. La seconda volta, senza
memoria, si rifa' tutto da capo.

`nova/ricette.py` tiene le procedure; l'agente le legge prima e le scrive dopo.

**Cosa si registra.** I passi: «apro X, cerco Y, leggo Z». Mai il risultato:
una memoria che risponde con i dati di ieri e' peggio di una che non risponde.

**Chi la scrive.** Il modello stesso, a turno finito, con una chiamata al
cervello veloce in sottofondo. Non la si deduce osservando le chiamate agli
strumenti perche' con un cervello agentico quelle chiamate non passano di qui:
Claude Code usa i propri strumenti per conto suo e consegna solo la risposta.
Chiederglielo funziona con tutti i cervelli.

**Come si ritrova.** Confronto lessicale con pesatura per rarita', piu' due
regole di parentela fra parole (una contenuta nell'altra, oppure sei caratteri
iniziali uguali) che coprono «email»/«mail» e «silenzia»/«silenzioso». Non
embedding: quello predefinito e' a hash e non capisce il significato.

**Come si usa.** La procedura finisce in coda alla domanda, sotto gli occhi del
modello, con scritto esplicitamente che e' come e' andata l'altra volta e non
come deve andare oggi. Non parte niente da sola.

**Quando NON si registra.** Sotto gli otto secondi (non c'era fatica da
risparmiare), senza strumenti usati (era una conversazione), a interruttore
spento. Le procedure sono al massimo sessanta: oltre, un archivio diventa
rumore e il rumore fa proporre la strada sbagliata.

## 9-quater. Le automazioni: quando la procedura diventa uno strumento

Una procedura e' un appunto: il modello la legge e poi decide passo per passo,
e **ogni decisione e' un giro di modello**. Un'automazione toglie il modello di
mezzo per la parte meccanica.

Il giro: NOVA scrive il **corpo** di una funzione, `nova/automazioni.py` ci
mette attorno il guscio (lettura dei parametri, cattura degli errori, formato
dell'uscita), la esegue in una cartella d'appoggio con i parametri di prova, e
**solo se gira** la sposta fra le automazioni vere. Da quel momento compare
nell'elenco degli strumenti come `auto_<nome>`.

L'iniziativa nasce dai dati che ci sono gia': quando una procedura risulta
rifatta tre volte e non ha ancora un'automazione, il blocco che finisce sotto
gli occhi del modello glielo fa notare. Niente euristiche inventate, solo il
contatore.

### I tempi, misurati

| | tempo |
|---|---|
| eseguire un'automazione (dal registro) | **0,04-0,07 s** |
| turno di modello senza strumenti | ~3 s |
| **richiesta risolta da un'automazione** | **~8-12 s (2 turni)** |
| la stessa richiesta esplorando da zero | decine di secondi |

L'automazione in se' e' gratis. Il pavimento restano **due** turni di modello,
non uno: il primo per decidere di chiamarla, il secondo per raccontare il
risultato. Non si scende sotto senza cambiare qualcosa d'altro - vedi le
questioni aperte.

## 10. Questioni aperte

- **Il secondo turno.** Una richiesta risolta da un'automazione costa due giri
  di modello: uno per chiamarla, uno per commentare cio' che ha risposto.
  Misurato: 12,4 s per «quanto spazio ho sui dischi», di cui 0,07 s di lavoro
  vero. Per arrivare ai tre secondi servirebbe che l'uscita dell'automazione
  fosse **gia'** la risposta all'utente, restituita senza ripassare dal
  modello. E' fattibile - un campo «risposta diretta» nel manifesto - ma
  toglie al modello la possibilita' di accorgersi che il risultato non ha
  senso, e va deciso sapendo cosa si baratta.
- **Dove vanno i secondi** (misurato su questa macchina, cervello Claude Code):

  | | tempo |
  |---|---|
  | avviare Python e importare tutta NOVA | 0,25 s |
  | avviare Node / il CLI di Claude | 0,05-0,20 s |
  | eseguire uno strumento (il lavoro vero) | millisecondi |
  | **un turno di modello** | **~3 s** |
  | turno con una chiamata a strumento (2 turni) | 8-9,5 s |
  | preparare e consultare la memoria | ~0,5 s |

  Chiamare una funzione non costa niente: costa **decidere di chiamarla**, e
  ogni decisione e' un giro completo di modello. Da qui la regola pratica:
  ~3 secondi di base, ~3-5 secondi per ogni passo che richiede un pensiero.
  Le procedure imparate non rendono il passo piu' veloce - **tolgono passi**,
  ed e' li' che sta il guadagno: dieci turni di esplorazione contro due di
  esecuzione sono un minuto contro nove secondi.

- Per scendere sotto i tre secondi su una richiesta gia' nota bisognerebbe
  eseguire i passi **senza passare dal modello**, e chiamarlo una volta sola
  alla fine per formulare la risposta. E' realistico - il tetto sarebbe il
  singolo turno, cioe' i ~3 s misurati - ma apre la domanda vera: quando una
  procedura e' abbastanza collaudata da eseguirla alla cieca. Oggi la risposta
  e' mai, e il modo per cambiarla e' lo stesso schema del banco: N esecuzioni
  con lo stesso esito, anteprima, e annullamento gia' pronto.
- Il riconoscimento lessicale sbaglia in modo prevedibile: due richieste che
  usano parole diverse per la stessa cosa non si trovano ancora («posta
  elettronica» contro «inbox»). Il rimedio serio e' un embedder vero, che c'e'
  gia' come opzione (`kb.embedder = "llama"`) ma richiede un secondo server.
- Il banco prova quello che le prove provano. `test_voce.py` e' rosso da prima
  per una falsa segnalazione, e la copertura sul guscio Rust e sull'interfaccia
  e' zero: una riparazione che tocca `core/` o `ui/` passa la verifica senza
  che nessuno l'abbia davvero provata. Prima di allargare il banco a quelle
  parti serve almeno un `cargo check` dentro il confronto.
- Se una riparazione debba poter modificare `nova/banco.py` stesso. Oggi puo':
  il processo ha gia' i moduli in memoria, quindi non si taglia il ramo sotto i
  piedi in corsa, ma il giro successivo userebbe il codice nuovo per giudicare
  se stesso.
- La lingua e' fatta a meta' e si sa quale meta': l'interfaccia ha il
  dizionario italiano e inglese, le altre nove lingue offerte fanno rispondere
  NOVA nella lingua giusta ma lasciano i menu in italiano. La tendina lo dice
  invece di lasciarlo scoprire dopo.
- I componenti scaricabili sono quattro (voce, ONNX, espeak, ascolto). Il
  modello del cervello no: si sceglie ancora dall'installer o a mano. E' il
  prossimo pezzo che dovrebbe passare dal pannello.
- Se il primo avvio guidato debba stare dentro NOVA invece che
  nell'installer: un installer che fa dieci domande perde meta' della
  gente alla quarta, e le stesse domande poste dall'interfaccia si
  possono rifare.
- Costo e tipo del certificato di firma: chi lo intesta in un progetto open
  source, e chi lo rinnova.
- Se il modello locale debba essere proposto per primo a chi ha l'hardware
  adatto, o restare la seconda scelta.
- Formato e verifica delle mappe IFR contribuite: come si accetta il
  contributo di uno sconosciuto per una cosa che, se sbagliata, non fa
  avviare un PC.
- Come si comporta il «conferma sempre» durante un lavoro lungo in
  co-working, quando l'utente non sta guardando.

- Il giornale delle operazioni: quanto indietro tenerlo, e cosa fare delle
  operazioni non invertibili (un processo ucciso non si resuscita). Serve un
  modo onesto di dire «questo pezzo non si annulla» **prima** di eseguirlo.
- L'anteprima universale su capacita' che chiamano il mondo esterno: cosa
  vuol dire «prova» per una richiesta HTTP che ha effetti sul server altrui.
- Se l'autonomia concessa a tempo debba scadere sul tempo o sul task: dieci
  minuti passano anche mentre NOVA aspetta una risposta dell'utente.
- **Nipoti orfani.** `kill_on_drop` uccide il figlio diretto di `shell.exec`,
  non i suoi discendenti: un comando che ne avvia altri lascia processi vivi
  dopo un «ferma». La cura sono i **job object** di Windows — si assegna il
  figlio a un job con `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` e cade tutto
  l'albero insieme. Finche' non c'e', «fermare» e' vero per il caso comune e
  parziale per quello annidato, e va detto invece che lasciato scoprire.
- **L'annullamento copre poco.** Oggi vale per `fs.write` e per lo
  spostamento che sovrascrive. Restano fuori le operazioni delle capacita'
  Python (copia, rinomina in blocco) e tutto cio' che tocca l'interfaccia:
  `ui.set_text` cambia un campo e nessuno sa cosa c'era prima. Serve che
  ogni capacita' che modifica dichiari la propria inversa, altrimenti
  «annulla» diventa una promessa che vale solo a volte — e non si sa quando.
- **L'anteprima copre quattro capacita' su 46.** `fs.write`, `shell.exec`,
  `annulla.*` e `segreti.importa` sanno dire cosa succederebbe; le altre
  rifiutano onestamente. Va bene come punto di partenza — meglio un rifiuto
  chiaro di un'anteprima finta — ma le piu' utili mancano ancora: `ui.click`
  e `ui.set_text` dovrebbero dire *quale elemento* toccherebbero e in quale
  finestra, che e' proprio il controllo che si vorrebbe fare prima di lasciar
  premere un pulsante a qualcun altro.
- ~~La vista dipende dal cervello.~~ **Corretto.** Avevo scritto che con una
  chiave API NOVA sarebbe rimasta cieca: era sbagliato, e in un modo che vale
  la pena ricordare. I modelli **vedono** — Qwen, GPT e gli altri sono
  multimodali da tempo. Cio' che mancava non era la vista del cervello ma il
  **tubo**: nessuno costruiva un messaggio con l'immagine dentro. Attribuire ai
  modelli un limite che era nel nostro codice avrebbe portato a scrivere «NOVA
  e' cieca qui» invece di costruire dieci righe.

- **Niente riconoscimento ottico.** Un PDF scansionato viene rifiutato con un
  messaggio onesto, ma resta illeggibile. Vale anche per il testo dentro le
  immagini catturate dallo schermo.
- **L'osservatore guarda una cartella sola, non i sottolivelli**, e vive nel
  demone: si spegne quando si spegne lui. Per «tienilo d'occhio anche domani»
  servirebbe che le osservazioni si scrivano su disco come il giornale.
- ~~Il modello locale vede solo se il proiettore c'e'.~~ **Chiuso**:
  l'installer scarica `mmproj-F16.gguf` insieme al modello e lo conta nello
  spazio richiesto. Se il download fallisce lo dice, invece di lasciare la
  vista spenta in silenzio.

  E chi porta un modello suo, senza passare dall'installer? Quel caso ora
  degrada da solo. `runtime.proiettore_accanto()` risponde a una domanda
  sola — «c'e' il proiettore accanto a questo GGUF?» — e la stessa risposta
  serve a due decisioni: se passare `--mmproj` alla riga di comando, e se
  allegare una figura alla conversazione. Tenerle separate voleva dire
  lasciarle divergere.

  Quando la risposta e' no, la schermata si scatta lo stesso (il file su
  disco ha valore comunque) ma al modello arriva una riga di testo al posto
  dell'immagine, che gli dice di non fingere di averla guardata e di usare
  `ui.tree`. Se l'immagine parte comunque — un server acceso da altri, un
  endpoint esterno — `llama-server` risponde **HTTP 500** con
  `image input is not supported [...] provide the mmproj`, e a quel punto
  NOVA sfila le figure dalla conversazione e riprova una volta: senza
  quello il messaggio con l'immagine resterebbe in coda e farebbe fallire
  **tutti** i turni successivi, non solo il suo.
