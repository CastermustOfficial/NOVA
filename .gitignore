__pycache__/
*.pyc
*.pyo

# runtime llama.cpp scaricato da get_cuda_runtime.ps1 (~560 MB)
runtime/

# la memoria e' personale: togli questa riga se vuoi versionarla
vault/
!vault/.gitkeep

# log e configurazione locale
*.log
config.local.json

# build Rust (~1 GB)
core/target/


# materiale di lavoro delle revisioni fra modelli
revisioni/
verifica_incrociata.log
verifica_incrociata.err

# chiavi: non devono mai finire nel repository
*.key
.env
segreti.json

# Il core compilato o scaricato dalla release: non e' sorgente.
bin/
avvio.log
azioni.jsonl*
avvisi.jsonl
pianificazione.json
_architettura.pdf
_vetrina*

# Le copie dei binari che funzionavano, prima di sostituirli. Restano sul
# disco di chi ricostruisce e non nel repository: sono un paracadute
# locale, non una versione.
salvagente/
