__pycache__/
*.pyc
*.pyo

# runtime llama.cpp scaricato da get_cuda_runtime.ps1 (~560 MB)
runtime/

# la memoria e' personale: togli questa riga se vuoi versionarla
vault/
!vault/.gitkeep

# log e configurazione locale
*.log
config.local.json

# build Rust (~1 GB)
core/target/


# materiale di lavoro delle revisioni fra modelli
revisioni/
verifica_incrociata.log
verifica_incrociata.err

# chiavi: non devono mai finire nel repository
*.key
.env
segreti.json

# Il core compilato o scaricato dalla release: non e' sorgente.
# La barra davanti e' obbligatoria: senza, «bin/» vale per QUALSIASI cartella
# che si chiami cosi', a qualunque profondita' - e in un progetto Rust
# «src/bin/» e' dove stanno i sorgenti degli eseguibili. Ne e' sparito uno per
# davvero: compilava qui e non sarebbe mai arrivato nel repository.
/bin/
avvio.log
# ...e il suo storico: la potatura ne lascia sempre uno accanto.
*.1.log
azioni.jsonl*
avvisi.jsonl
pianificazione.json
_architettura.pdf
_vetrina*

# Le copie dei binari che funzionavano, prima di sostituirli. Restano sul
# disco di chi ricostruisce e non nel repository: sono un paracadute
# locale, non una versione.
salvagente/

banco_prestazioni.json

banco_modello.json

# scratch: i messaggi di commit lunghi passano da un file, PowerShell rovina le virgolette
_msg.txt
_probe_*.py
_suite.txt
_cargo.txt
_build.txt
_nt.txt
_nte.txt
_mu.txt
_mue.txt
_t.txt
_te.txt
_misura*.py
_conta.py
_suite.py
_kv.txt

# Scarti delle prove, finiti nel repository per sbaglio: i due file dove la
# shell scarica uscita ed errore, il bersaglio della prova tastiera con i suoi
# eventi, e un nome nato da un apostrofo di troppo in una riga di comando.
_shell_*.txt
_tast_prova.txt*
_giro_prove.py
importante'

# L'editor dell'harness: lo porta `nova-shell/build.rs` dal registro npm, a
# versione fissata e con l'impronta controllata (D336). Non e' sorgente.
core/crates/nova-shell/ui/vendor/
